## v8.4.22 - 文件卡片布局与操作状态修复

- 标签入口改为卡片右下角圆点；已打标签显示对应颜色，未打标签仅在悬浮或选中时显示空心圆。
- 标签菜单取消复选框，选择“不打标签”即可清除标签。
- 创建标签改为悬浮或点击展开的二级菜单，创建后立即应用。

## v8.4.1 - 标签即收藏轻量化版

文件管理页新增文件／文件夹收藏、收藏视图、标签创建与颜色管理、标签筛选，以及右键、详情和批量标签操作。收藏和标签保存在轻量索引中，不会增加图谱正文读取。相关交互已拆分为独立组织模块。

## v8.3.5 - 账号菜单层级修复

- 修复文件管理页右上角账号菜单被文件卡片和缩略图遮挡的问题。
- 为顶部工具栏建立明确堆叠层级，使账号菜单始终位于文件内容之上。
- 顶部栏仍低于右键菜单、详情抽屉、模态框和处理中遮罩，不影响其他浮层。

## v8.3.4 - 文件管理稳定性与性能补丁

本版本减少文件管理页的重复完整性扫描，修复多选右键与失效选择问题，并增强非法拖放反馈、二级菜单键盘操作和当前图谱初始定位。

## v8.3.3 - 可见多选与移动菜单补丁

- 右键菜单的“移动到”区域明确增加“选择其他位置…”入口。
- 最近目标文件夹最多显示 4 个，不会随文件夹数量无限扩展。
- 顶部增加“选择”按钮，进入多选模式后每个项目显示可点击复选标记。
- 批量工具栏支持移动、导出、移入回收站和取消选择。
- 保留 Ctrl/Command 与 Shift 多选快捷操作。
- 批量删除增加父子层级去重和失败回滚。

## v8.3.1 - 轻量移动与批量操作

- 文件与文件夹可直接拖拽到目标文件夹、面包屑或左侧“我的文件”。
- Ctrl/Command 点击和 Shift 点击可进行多选，拖动任一已选项目即可批量移动。
- 右键“移动到”下方显示根目录和最近使用的目标文件夹；“选择其他位置”继续提供完整目录树。
- 移动后显示轻提示并可在 5 秒内撤销。
- 批量移动失败时自动回滚已完成的项目。
- 选中父文件夹及其子文件夹时，只移动父文件夹，避免破坏原有层级。

## v8.3.0 - 文件夹管理

文件管理页现支持多级文件夹、面包屑导航、文件/文件夹移动、文件夹内联重命名，以及文件夹与其内容的回收站恢复。旧图谱没有 `folderId` 时自动视为根目录文件，无需迁移。

## v8.2.8 - 文件管理稳定性补丁

文件名内联编辑现在可安全跨越后台列表刷新，不会在尚未输入完成时被自动提交；存储空间统计也增加了异步防串线保护，账号切换或连续刷新时不会显示旧账号结果。

## v8.2.7 - 内联重命名

文件管理页现在可在文件名原位置直接重命名：Enter 保存、Esc 取消、失焦保存；不再弹出重命名对话框。重命名会同步图谱标题与页签名称，并适用于网格和列表视图。

## v8.2.6 - 手动保存

图谱页面左上角保存图标现可点击立即保存，并支持 Ctrl+S / Command+S。自动保存机制继续保留。

## v8.2.4 - 页签会话状态修复

关闭的图谱页签会按用户保存在当前浏览器会话中。进入独立文件管理页再返回编辑器时，不会重新显示已经关闭的页签；从文件管理器主动打开文件时会自动恢复对应页签。

## v8.2.3 - 文件预览性能优化

- 移除文件详情栏中的重复缩略图。
- 预览仅在图谱结构发生变化时重新生成。
- 文件列表缩略图使用视口懒加载，降低大量文件时的 DOM 和绘制负担。

## v8.2.2 - 文件管理交互与动态图谱缩略图

- 修复文件卡片第一次单击后被重新渲染，导致浏览器无法识别连续双击的问题；现在双击卡片可直接打开图谱。
- 右上角头像升级为可操作账号菜单，提供身份信息、返回编辑器、系统设置、管理员用户管理和登录/退出入口。
- 图谱缩略图改为依据实际节点位置、节点颜色、关系线和核心节点标题生成，不同文件会呈现不同结构。
- v2 文件索引新增轻量 `preview` 元数据；新建与保存时自动更新，旧文件进入文件管理页后会在空闲时一次性补齐。
- 缩略图最多保存 18 个节点和 30 条关系，只渲染轻量 SVG，不在文件列表中反复加载完整图谱正文。
- 优化深色和浅色主题下的缩略图层次、标签、节点光晕、连线和悬浮效果。

## v8.2.1 - 文件管理回归修复

- 修复 100 字符文件名发生重名时无法正确追加序号的问题；批量导入同名长标题不再陷入无限循环。
- 修复未打开的导入文件和副本也被列入“最近打开”的问题；仅实际打开或设为当前文件时记录打开时间。
- 修复内容缺失的回收站文件恢复后被错误移回正常文件列表的问题；恢复前会验证内容并保持事务一致。
- 修复双击或用键盘操作卡片“更多”按钮时意外打开图谱的问题。
- 文件批量导入期间增加忙碌遮罩和重复操作保护，并减少逐文件重复刷新。
- 登录账号在当前页或其他标签页发生变化时，文件管理页会同步切换用户空间和身份显示。
- 完善图谱内容缺失时的错误提示，避免显示上一次操作遗留的错误信息。

## v8.2.0 - 文件管理功能第一阶段

- 新增独立页面 `file-manager.html`，文件管理与图谱编辑器解耦，方便后续持续扩展。
- 程序页签栏最左侧房子按钮改为文件管理入口；进入前会保存当前图谱，保存失败则取消跳转。
- 文件管理页提供最近打开、我的文件和回收站三个入口。
- 支持网格/列表视图、关键词搜索以及按更新时间、最近打开、名称、大小和创建时间排序。
- 支持新建、打开、重命名、创建副本、导入 ZIP/JSON、导出学习包和移入回收站。
- 回收站支持恢复、永久删除和清空，沿用 v2 存储层 30 天自动清理策略。
- 文件卡片和详情栏显示更新时间、节点数、关系数、文件大小、标签及当前打开状态。
- 左侧展示图谱文件库大小、回收站大小、节点/关系总数和浏览器存储配额估算。
- 文件列表只读取 v2 轻量索引；仅在打开或导出时读取完整图谱正文。
- 支持浅色/深色主题切换并记住用户选择，同时提供桌面端与移动端响应式布局。
- 导入继续执行 ZIP 安全限制，并额外验证 `nodes` / `links` 结构和节点、关系数量上限。

# 通用知识点关系图谱工具｜模块化开发版

这是从上传的单文件 HTML 拆分出来的低风险模块化版本。


## v7.9.6 维护优化记录

- 将首页“搜索定位”按钮的事件绑定迁回 `src/10-graph-editor.js`，避免 `src/20-flashcards-toolbar.js` 与图谱搜索产生隐式耦合。
- 为图谱搜索增加轻量缓存索引，节点字段只在索引失效后统一拼接和转小写，降低大图谱连续输入搜索时的重复计算。
- 合并左上角图谱信息悬浮栏相关 CSS，移除 v7.9.5 追加式覆盖块，降低后续样式维护成本。
- 保持 v7.9.5 的界面行为：图谱信息栏只显示图谱名称，搜索入口在图谱信息栏内，详情窗口不显示尺寸字段。


## v7.9.7 用户偏好与高性能模式

- 首页左上角图谱名称悬浮栏新增“偏好设置”入口。
- 新增浏览器级用户偏好存储 `kg_graph_user_preferences_v1`，不会写入图谱文件或学习包。
- 支持“精致 / 平衡 / 高性能 / 自定义”视觉模式。
- 支持单独关闭阴影、玻璃模糊、动画过渡、关系线标签和卡牌渐变背景。
- 高性能模式通过根节点 `kg-perf-*` class 统一覆盖 CSS，避免逐节点修改 DOM，适合大图谱和低性能设备。


## v7.9.8 多选拖动连接线延迟刷新

- 针对多选卡牌拖动卡顿，新增“拖动时延迟刷新连接线”偏好项。
- 多选拖动过程中默认只移动卡牌，暂停 SVG 关系线 path/text 的实时几何刷新。
- 松手后统一刷新一次关系线，避免 50 张左右图谱中关系线密集时拖动卡顿。
- 拖动中会临时淡化连接线，减少连接线停留在旧位置带来的视觉干扰。




## v7.9.10 手动大图模式开关

- 偏好设置中的“大图模式”改为真正的手动开关，默认开启。
- 开启后，任何数量卡牌的图谱都会使用大图渲染策略。
- 关闭后，当前图谱按普通图谱模式显示，关系线较多时可能变卡。
- 切换开关后会立即重新渲染首页图谱，无需刷新页面。


## v7.9.11 大图模式选中标识与框选删除

- 修复大图模式下卡牌选中状态不明显的问题，单选和框选都会显示外框与“已选”标识。
- 在大图模式的局部关系/只看相关状态下，已选卡牌会保持可见。
- Delete / Backspace 现在支持删除框选的多个知识点，并同步删除相关关系线。


## v7.9.12 快捷操作增强

- 支持 Ctrl/Command + 点击卡牌进行复选，再次点击已选卡牌可取消选择。
- 支持 Ctrl/Command + C 复制当前选中的卡牌到应用内部剪贴板。
- 支持 Ctrl/Command + V 在鼠标所在画布位置粘贴卡牌；只复制卡牌本身，不复制关系线。
- 支持 Ctrl/Command + Z 撤回删除和粘贴等快捷操作，主要用于误删恢复。
- 删除单张卡牌、框选批量删除、删除关系线和粘贴卡牌都会写入撤回栈。


## v7.9.13 箭头编辑模式 / 手型演示模式

- 默认进入箭头编辑模式：可点击卡牌和关系线、拖拽卡牌、框选、复制粘贴、编辑弹窗；左键不再拖动画布。
- 按住空格键可临时进入手型演示模式，松开后恢复箭头编辑模式。
- 在图谱区域按住鼠标右键可临时进入手型演示模式，松开后恢复，并禁用图谱区域默认右键菜单。
- 工具栏“居中”按钮右侧新增箭头/手型切换按钮，可长期切换编辑/演示模式。
- 手型模式下卡牌和关系线不触发选择、拖拽、编辑等操作，只用于平移浏览画布。


## v7.9.14 编辑模式直接框选

- 编辑模式下，空白画布区域按住鼠标左键拖动即可框选卡牌，不再需要按住 Shift。
- 单击空白画布仍会清空选择并关闭详情。
- 手型演示模式保持不变，仍用于拖动画布浏览。


## v7.9.15 选中状态圆点标识

- 已选卡牌不再显示“已选”文字。
- 单选和多选卡牌右上角改为显示卡牌自身颜色的小圆点。
- 大图模式和普通模式保持一致的轻量选中标识。


## v7.9.16 连线起点提示位置优化

- 双击卡牌设为连线起点后，“连线起点”提示移到卡牌外部上方，避免遮挡卡牌内容。
- 提示文字保持单行显示，并根据卡牌宽度自动缩小字号。
- 小卡、默认卡和大卡分别做了上方间距微调。


## v7.9.17 连线选中状态与快捷线型

- 单击关系线后，选中关系线会显示更明显的高亮状态。
- 选中关系线旁边新增“实线 / 虚线”快捷按钮。
- 可直接在线旁切换线型，无需回到左侧工具栏。
- 画布平移、缩放或关系线端点移动时，快捷按钮会跟随关系线位置更新。


## v7.9.18 连线快捷按钮位置与图标优化

- 关系线快捷线型按钮改为纯图标，不再显示“实线 / 虚线”文字。
- 快捷按钮显示在关系线旁边，并尽量靠近鼠标点击位置。
- 画布缩放和平移后，快捷按钮仍会跟随点击处所在的关系线位置。


## v7.9.19 双击连线就地编辑文字

- 双击关系线可在线旁直接输入 / 修改线上文字。
- Enter 保存，Esc 取消，输入框失焦自动保存。
- 输入为空并保存时，会清空线上文字。
- 修改前会写入撤回栈，可通过 Ctrl / Command + Z 撤回误改。
- Alt + 双击关系线保留原完整关系线编辑弹窗入口。


## v7.9.20 大图模式连线文字编辑修复

- 修复大图模式下双击可见连线无法弹出线上文字输入框的问题。
- 大图模式下已渲染的主干线 / 骨架线现在可单击选中、双击就地编辑文字。
- 保持手型演示模式规则不变：手型模式下连线仍不触发编辑。


## v7.9.21 普通模式关系文字按需显示

- 普通模式下，关系线文字默认不再全部显示，减少大量 SVG 文本渲染带来的卡顿。
- 单击关系线时，该关系线文字会显示。
- 单击卡牌或框选卡牌时，与所选卡牌相连的关系文字会显示。
- 大图模式保持原有按局部关系显示文字的策略。


## v7.9.22 工具栏快捷键梳理

- V：切换箭头编辑模式 / 手型演示模式。
- N：新增知识点。
- T：打开模板。
- F：重点聚焦。
- K：记忆闪卡。
- Q：考题训练。
- 1 / 2：小卡 / 大卡。
- S / D：实线 / 虚线。
- C：打开线条 / 卡牌颜色选择器。
- - / +：缩小 / 放大。
- 0：居中显示。
- L：心流模式。
- G：主干线 / 骨架线。
- R：只看相关。
- 工具栏按钮的鼠标悬浮提示会自动追加对应快捷键；输入框、弹窗和文字编辑状态下不会触发这些快捷键。


## v7.9.23 工具提示简化与高风险快捷键移除

- 工具栏悬浮提示统一简化为“功能 + 快捷键”，例如“新增 N”“选择/手型 V”。
- 取消导出、导入、清空图谱的单键快捷键，降低误触风险。
- 导出、导入、清空图谱仍保留工具栏按钮操作。
- 其余常用编辑和视图快捷键保持不变。


## v7.9.24 工具栏提示清理

- 修复工具栏按钮悬浮时同时出现黑色自定义提示和浏览器原生 title 提示的问题。
- 心流、主干线、只看相关三个动态按钮的提示改为简短格式：`心流 L`、`主干线 G`、`相关 R`。
- 选择 / 手型按钮保持简短提示：`选择/手型 V`。
- 导入、导出、清空仍不绑定快捷键，降低误触风险。


## v7.9.25 居中选中卡牌

- 点击工具栏“居中”按钮或按 `0` 时，若当前选中了卡牌，会优先将选中卡牌居中并缩放到合适大小。
- 多选 / 框选多张卡牌时，会以选中卡牌组的整体范围居中并适配缩放。
- 未选中卡牌时，保持原有全画布居中逻辑。


## v7.9.26 居中视角平滑过渡

- 点击“居中”或按 `0` 时，不再瞬间跳转视角。
- 会从当前视角平滑过渡到目标视角，并包含缩放过程。
- 选中单张 / 多张卡牌居中、全画布居中、空画布居中均使用同一套平滑过渡。
- 若系统开启“减少动态效果”，会自动退回即时定位。


## v7.9.27 大图模式悬浮详情

- 大图模式下，鼠标悬浮卡牌约 650ms 后会自动显示详情窗口。
- 鼠标快速划过卡牌不会触发详情，减少大图模式下频繁重绘。
- 若开启主干线 / 骨架线，悬浮卡牌仍会保留局部关系预览。
- 拖拽卡牌、框选、手型 / 临时手型、居中视角动画、线上文字编辑时，不触发悬浮详情。


## v7.9.28 聚焦画面柔和过渡

- 重点聚焦、心流模式、只看相关、主干线 / 骨架线切换时，卡牌明暗、关系线透明度和画布背景会缓起缓停。
- 重点聚焦切换不再重建卡牌 DOM，改为保留现有卡牌并做状态过渡，视觉更柔和。
- 过渡期间关系线会做轻量淡入，减少突然闪变。
- 若系统或性能偏好开启“减少动画”，会自动禁用这些过渡。


## v7.9.29 图谱页签拖拽排序

- 首页顶部图谱页签支持鼠标拖拽排序。
- 拖拽页签到目标页签左侧 / 右侧后松手，即可调整顺序。
- 排序结果会持久化保存，刷新页面或切换图谱后仍保持。
- 拖拽关闭按钮不会触发排序；拖拽结束后不会误触打开页签。
- 新建图谱默认追加到当前页签列表末尾。


## v7.9.30 图谱页签拖拽让位动画

- 优化图谱页签拖拽排序体验。
- 拖拽经过目标页签时，相邻页签会向左 / 向右轻微让位，形成“被挤过去”的反馈。
- 插入位置蓝色竖线仍保留，同时目标页签会有轻微高亮。
- 拖拽结束后自动清理动画状态，不影响点击切换页签。


## v7.9.31 图谱标题原处编辑与文件信息

- 首页左上角图谱名称改为单击后直接在原位置编辑，不再弹窗。
- 标题输入 Enter / 失焦保存，Esc 取消。
- 双击图谱名称仍打开弹窗，但弹窗仅保留“标题”可编辑。
- 弹窗新增只读信息：创建时间、最后更新、文件大小、知识点数量、文件字数。


## v7.9.32 超大图谱完整适配视野

- 修复超大图谱（例如 1000 张卡）在“居中 / 全画布适配”后仍有卡牌超出屏幕的问题。
- 主画布最小缩放从原来的约 0.2 降到 0.035，允许超大图继续缩小到完整视野。
- 全画布居中时增加更舒适的屏幕留白，避免卡牌贴边。
- 滚轮缩放、工具栏缩放按钮、快捷键 `0` 会使用同一套超大图缩放范围。
- 兼容旧的 `fitBoundsToView(options)` 调用，避免相关卡牌聚拢适配被同名全局函数覆盖影响。


## v7.9.33 左下角缩放与全屏控制

- 将放大 / 缩小按钮从左侧浮动工具栏移到画布左下角。
- 左下角缩放条包含：缩小、实时缩放百分比、放大、全屏。
- 缩放百分比会随滚轮、按钮、居中适配、动画过渡实时更新。
- 点击百分比会将画布缩放恢复到 100%。
- 悬浮 / 聚焦百分比区域时会弹出滑轨，可拖动滑轨控制缩放比例。
- 新增画布全屏按钮，可进入 / 退出当前画布全屏。


## v7.9.34 缩放栏交互与更新日志

- 新增独立更新日志文件 `CHANGELOG.md`，后续版本优化会同步记录。
- 左下角缩放滑轨默认隐藏，点击缩放百分比后出现。
- 滑轨不再因鼠标悬浮 / 移开而自动显示或隐藏，方便连续拖动调整。
- 缩放功能栏更靠近画布左下边缘，释放画布空间。
- 消息提示移动到缩放功能栏右侧，避免与缩放按钮重叠。


## v7.9.35 关系线路径形态与圆弧快捷菜单

- 关系线新增路径形态：曲线、直线、折线。
- 旧图谱没有 `pathStyle` 字段时，默认仍按曲线显示，保持兼容。
- 点击关系线出现的快捷菜单改为圆弧形：上半部为实线 / 虚线，下半部为直线 / 折线 / 曲线。
- 快捷菜单出现时增加弹出缩放动效。
- 临时相关画布也会按关系线的路径形态显示。


## v7.9.36 关系线圆弧菜单轻量化

- 去掉关系线快捷菜单的背景圆盘，仅保留五个样式按钮。
- 上半部实线 / 虚线按钮改为圆弧排布，不再水平并排。
- 下半部直线 / 折线 / 曲线继续保持圆弧排布，并微调间距。
- 保留菜单弹出缩放动效和当前样式高亮。


## v7.9.37 关系线圆弧菜单加入文字与颜色

- 关系线圆弧菜单新增 `T` 按钮，点击后打开关系文字就地编辑框。
- 新增颜色圆点按钮，点击后可快速修改当前关系线颜色。
- 实线 / 虚线 / T / 颜色 / 直线 / 折线 / 曲线共七个按钮统一沿圆弧排布。
- 文字编辑不再主要依赖双击连线，入口更稳定。


## v7.9.38 关系线菜单对称布局与双虚线

- 关系线快捷菜单调整为更对称的圆弧排布。
- 顶部线型改为三颗：长虚线、实线、短虚线，实线位于中间。
- 中部左右分别为 `T` 文字编辑和实时颜色圆点。
- 颜色圆点改为菜单内原生色盘输入，点击后色盘出现在菜单按钮旁，不再跳回左侧工具栏。
- 底部继续保留直线、折线、曲线路径形态。


## v7.9.39 关系线菜单正圆排布与虚线直角化

- 修复关系线快捷菜单看起来偏椭圆的问题，八个按钮改为同一半径的正圆排布。
- `T` 文字按钮增加浅蓝底色，颜色按钮直接使用当前关系线颜色，避免左右两侧按钮看起来像空白按钮。
- 长虚线 / 短虚线的小段端点改为直角，不再使用圆角端点，视觉更简洁，也减少额外绘制成本。

## 如何运行

方式一：直接打开 `index.html`。  
方式二：用本地服务器运行，更接近正式开发环境：

```bash
python3 serve.py
```

然后打开：

```text
http://127.0.0.1:8000/index.html
```

## 目录结构

```text
kg_graph_modularized/
  index.html
  question-training.html
  knowledge-recall.html
  question-bank.html
  user-management.html
  system-settings.html
  serve.py
  styles/
    main.css
    global-shortcuts.css
    question-bank-admin.css
    question-training.css
    knowledge-recall.css
    user-management.css
    user-center.css
    account-menu.css
    home-file-library.css
    system-settings.css
    subscription.css
  src/
    29-auth-core.js
    30-auth-guards.js
    33-user-center.js
    34-role-permissions.js
    35-user-management-service.js
    35-user-management.js
    36-system-settings.js
    37-subscription-plans.js
    37-subscription-orders.js
    37-subscription-redeem-codes.js
    37-subscription-core.js
    39-global-shortcuts.js
    41-account-menu.js
    ...
  docs/
    MODULE_MAP.md
    MODULE_AUDIT.md
    AUTH_CORE_REFACTOR.md
    USER_MANAGEMENT_SERVICE.md
    BASELINE_CLEANUP.md
    SCRIPT_LOAD_ORDER.md
    SUBSCRIPTION_MODEL.md
    BASELINE_REFACTOR_A.md
    BASELINE_REFACTOR_B.md
    BASELINE_REFACTOR_C.md
```


## 脚本加载基线

当前活跃页面的本地脚本已统一使用 `defer`。加载顺序遵循：

```text
认证核心 KGAuthCore
→ 角色权限 KGRolePermissions
→ 订阅基础层（plans / orders / redeem-codes / core）并汇总为 KGSubscription
→ 全局组件（微信登录 / 用户中心 / 全局快捷栏）
→ 页面业务脚本
```

首页中 `src/30-auth-guards.js` 需要包装图谱和工具栏函数，所以仍保留在图谱编辑与工具栏脚本之后、启动入口 `src/90-bootstrap.js` 之前。

## 订阅模型基线

第七步后，学员订阅套餐已统一为：

```text
free / monthly / quarterly / half_year / lifetime
```

旧版占位套餐 `free_student/basic_student/pro_student` 会通过订阅基础层自动兼容映射。订阅仍只对学员 `student` 生效，管理员和教师/教研绕过订阅限制，游客只保留公开示例体验。详细说明见 `docs/SUBSCRIPTION_MODEL.md`。

## 本次拆分做了什么

- 保留原 HTML 结构，不重写页面 DOM，降低功能回归风险。
- 从 `<style>` 拆出 `styles/main.css`。
- 从原来的单个巨大 `<script>` 拆成多个按功能命名的 JS 文件。
- 删除外链 Cloudflare beacon 统计脚本，避免重复加载和离线不可用问题。
- 把原来依赖 IIFE 函数提升的启动语句移动到 `src/90-bootstrap.js`。
- 历史单文件与临时解析文件已从当前整理版中移除，避免误维护。


## 本次界面调整

- 将原顶部操作按钮迁移为屏幕左侧悬浮图标菜单。
- 悬浮菜单可通过顶部手柄拖拽，位置保存在浏览器本地。
- 顶部保留图谱信息、升级会员和账号胶囊；使用说明、用户中心、登录/退出集中到账号下拉菜单。
- 登录后双击顶部图谱信息文字即可编辑；移动端仍保留底部操作栏。
- 所有原按钮 ID、数据结构和业务功能保持兼容。


## 基线重构 C / C-1 / C-1.1

首页学习包文件处理已拆到 `src/21-home-package-service.js`，早期快照文件库服务位于 `src/22-home-file-library.js`。

当前版本继续新增：

- `src/23-graph-file-store.js`：正式图谱文件库服务层。
- `src/24-graph-file-autosave.js`：3 分钟 dirty 自动保存、切换与离开前保存。
- `src/25-graph-file-tabs.js`：固定宽度文件页签、新建与切换交互。
- `styles/graph-file-tabs.css`：对标式双层顶部布局。

旧单图谱 localStorage key 继续保留并镜像写入；首次进入用户空间时会迁移为第一个图谱文件。

C-1.1 修复版进一步修复了存储失败误报成功、保存失败仍切换/退出、标题与文件名不同步、旧悬浮工具栏位置重叠、文件数量上限截断等问题。详细说明见 `docs/BASELINE_REFACTOR_C1_1_FIX.md`。

C-1.4 将画布顶部左右信息模块改为直接悬浮定位，移除横跨画布的整行背景容器。详细说明见 `docs/BASELINE_REFACTOR_C1_4.md`。

C-1.4.2 修复了解压到新目录后自动新手引导遮罩拦截左右悬浮模块点击的问题，并为悬浮模块增加显式画布事件隔离。详细说明见 `docs/BASELINE_REFACTOR_C1_4_2.md`。

## 下一步建议

当前版本是“安全拆分版”。后续可以继续做更彻底的工程化：

1. 把 `state`、`storage`、`sanitize` 抽成显式 API。
2. 把 graph / flashcard / question-bank 改成独立模块对象。
3. 建立统一事件总线，减少跨文件直接改全局状态。
4. 用 Vite 或 Parcel 管理构建，最终输出单文件发布版。
5. 增加 Playwright 冒烟测试，覆盖新增节点、导入导出、闪卡、题库管理。

## 深度知识回忆页面

- 新增 `knowledge-recall.html`，用于从考题进入独立的“知识寻宝地图”。
- 在考题破案模式的题目页点击“深度回忆”，会把当前题目写入本地缓存并打开独立页面。
- 深度回忆页以完整题目和选项为中心卡片，点击题目/选项中的关键词后，会在关键词附近生成三维圆形探索按钮。
- 圆形按钮显示对应知识点的首字，点击按钮后弹出引导式提问；选择能回忆到的知识点后，新按钮会以动画方式生成，并与父按钮自动连线。
- 画布支持拖拽、滚轮缩放、双击回到中心题目；每次生成新节点后会自动居中跟随。
- 预设知识网位于 `src/85-knowledge-recall-data.js`，交互逻辑位于 `src/86-knowledge-recall.js`，样式位于 `styles/knowledge-recall.css`。

### 新增：独立题库认知标注管理页

打开 `question-bank.html` 可进入独立题库管理页面。该页面面向项目管理类认证科目，默认支持 PMP、CSPM、P2、ACP、NPDP，并预留 PgMP、PfMP 等扩展入口。

当前支持：
- 题库列表与题目列表管理
- 题目基础信息、选项、答案与解析维护
- 题干关键词标记，并生成与深度回忆页面兼容的 `stemParts` / `clues`
- 知识点绑定，并生成与训练页兼容的 `concepts`
- 推理步骤维护，支持后续深度回忆和推理链训练扩展
- 设为当前训练题
- 一键打开深度回忆预览
- JSON 导入、导出与模板下载


### 题库管理页本轮增强

- 题库管理页左侧已改为“题库管理 / 题目管理”标签栏，题库选择和题目选择分区更清晰。
- 关键词标记、知识点绑定新增“编辑”按钮，已添加条目可再次修改。
- 关键词来源支持题干与选项，选中题干或选项文本时会出现“标记关键词”小按钮，并可通过悬浮操作台快速保存。

### 题库管理页布局优化

- 题库信息已内聚到左侧“题库管理”栏，中间题目编辑区不再显示题库信息表单。
- “题目管理”栏新增搜索与按章节/领域/难度/题型归集，适合大量题目快速定位。
- 关键词标记、知识点绑定、推理逻辑已整合为“认知标注工作台”标签栏。
- 左侧新增布局导航，可快速跳转题库管理、题目管理、题目基础、关键词、知识点、推理逻辑和完成度。

### 题库管理页体验优化

- 新增“删除题库”入口：题库信息区、右侧当前题库操作区、题库列表项均可删除当前/指定题库。
- 删除内置 PMP 示例题库或示例题后，会记录本地隐藏状态，刷新后不会反复自动恢复。
- 顶部导出按钮改名为“导出当前题库”，并新增“导出全部题库”。
- 右侧“项目管理类科目扩展”已改为“科目快捷管理”，可直接按科目筛选、新建题库或启用 PgMP/PfMP 扩展科目。
- 标注完成度改为可点击工作流：未完成项会显示“去补齐”，点击后自动跳到对应的题目基础、关键词、知识点或推理逻辑区域。
- 题库列表项新增“导出 / 删除”小操作，题库级管理更清晰。

## 组卷与发布（新增）

题库管理独立页面 `question-bank.html` 已新增“组卷与发布”模块，支持从同一科目下的多个题库按知识领域/主题配额抽题，生成综合训练试卷。

典型流程：

1. 在 PMP、CSPM、P2、ACP、NPDP 等科目下维护题库和题目。
2. 进入题库管理页的“组卷发布”。
3. 新建试卷，选择科目，例如 PMP。
4. 设置目标题量，例如 180。
5. 按知识领域/主题填写配额，例如范围、进度、成本、质量等。
6. 点击“按配额组卷”。
7. 检查试卷题目预览。
8. 点击“发布试卷”。
9. 回到首页打开“考题训练”，在“综合试卷”下拉框中选择已发布试卷并开始训练。

试卷数据保存在浏览器本地 localStorage，key 为 `kg_exam_papers_v1__<scope>`；当前选择的试卷保存在 `kg_exam_current_v1__<scope>`。


## 2026-07 题库管理主工作区布局调整

- `question-bank.html`：左侧收敛为纯纵向导航；题库列表、题目列表、组卷发布和题目基础编辑迁移到中间主工作区。
- 中间主工作区新增三个主标签：题库管理、组卷与发布、题目基本信息。
- `styles/question-bank-admin.css`：新增主标签栏、双栏题库/题目管理区和左侧窄导航响应式样式。
- `src/65-question-bank-admin.js`：新增主工作区标签状态 `activeMainTab`，并调整左侧导航跳转逻辑。
- `question-training.html`：独立考题训练页面，替代首页弹窗模式，可选择已发布试卷进行综合训练。

## 2026-07 用户管理系统（新增）

- 新增 `user-management.html`，作为独立用户管理页面。
- 新增 `styles/user-management.css` 与 `src/35-user-management.js`。
- 首页左侧悬浮菜单新增“用户管理”入口；题库管理页、考题训练页也新增跳转入口。
- 当前版本仍基于浏览器 `localStorage`，用于本地多用户原型管理：
  - 用户列表、搜索、角色筛选、状态筛选
  - 新建用户、编辑用户资料、重置密码
  - 设置角色：管理员、教师/教研、学员、只读用户
  - 设置状态：正常、暂停、已归档
  - 用户归档、恢复、删除账号
  - 用户数据概览：图谱节点、关系线、题库、题目、试卷
  - 操作日志：注册、登录、退出、创建、编辑、归档、恢复、导入导出等
  - 用户 JSON 导入、导出
- `src/30-auth-guards.js` 和 `src/72-question-training-page.js` 已同步增强：
  - 注册用户自动补齐角色、状态、科目、资料字段。
  - 登录时更新最近登录与最近活跃时间。
  - 已暂停或已归档账号将无法继续登录。
- `src/30-auth-guards.js` 与 `src/72-question-training-page.js` 已逐步接入 `src/29-auth-core.js`，优先复用统一的用户读取/保存、密码校验、会话和日志能力。

> 注意：这仍是本地浏览器版用户管理，不具备服务器级安全与跨设备同步。正式网络版需接入后端账号、数据库、权限和审计日志。


## 角色权限与主题模块

本版本新增 `src/34-role-permissions.js`，统一管理管理员、教师/教研、学员、只读用户的前端权限模板和角色主题色。教师/教研允许进入题库管理、维护题目、组卷并发布/取消发布试卷；用户管理和角色主题修改仅限管理员。首页与独立训练页会显示当前登录角色，并按角色自动切换主题色。

## 2026-07 角色权限与只读示例题修复

- 修复“只读用户仍可看到题库管理 / 用户管理入口”的问题：首页悬浮工具栏、考题训练页顶部入口、题库管理页顶部入口会按当前角色自动隐藏无权限按钮。
- 只读用户保留“考题训练”入口，但只能完整操作内置 PMP 示例题；其他题目、试卷或深度回忆会被拦截。
- 示例题判断改为同时校验 `bank-pmp-demo` 与 `pmp-agile-change-001`，避免仅凭题目 ID 误判。
- 修复唯一管理员被暂停后可能无法进入用户管理的问题：只有 `active` 状态管理员才算有效管理员。


## 微信扫码登录说明

本版本新增 `src/32-wechat-login.js`，在登录弹窗中提供“微信扫码登录”入口。由于当前项目仍是纯前端 localStorage 架构，微信登录分为两种模式：

- 本地演示扫码：用于原型体验，会创建/登录一个本地微信演示账号。
- 正式微信开放平台：在用户管理页配置 AppID、redirect_uri 和后端 code 换取 openid/unionid 的接口后，可跳转微信开放平台扫码授权页。真实上线时，AppSecret 与 token 交换必须放在后端完成，不能写在前端。

用户管理页新增“微信登录配置”面板，可设置是否启用演示扫码、正式微信模式、首次登录自动创建用户、默认角色和默认科目。

## 本次更新：用户分页与题库/组卷列表体验优化

- 用户管理左侧用户列表新增分页控件，支持上一页/下一页与每页 10/20/50/100 人切换；批量勾选改为“全选当前页”。
- 题库管理页的题库列表新增序号，便于后台核对与沟通。
- 题目列表区域强化为独立可滚动区域，适合 10 题以上、多题库场景；双击题目可直接进入“题目基本信息”标签。
- 组卷与发布页的试卷列表改为横向卡片网格，减少纵向占用，提升多试卷管理体验。

## 订阅功能进度

当前基线已完成订阅功能第八步 UI 基础层：

- 用户中心显示“我的订阅”。
- 用户管理页支持管理员手动开通、续期、停用学员订阅。
- 系统设置页支持编辑订阅套餐展示配置。
- 管理员和教师/教研绕过订阅限制，订阅仅聚焦学员角色；游客不进入订阅体系。

当前仍为纯前端 localStorage 原型，正式收费前必须接入后端支付和订阅校验。


## 订阅第七步补充修复

- 用户中心续费入口会自动关闭用户中心，再打开会员权益弹窗。
- 首页「使用说明」右侧新增「升级会员」入口。
- 会员权益弹窗可作为后续订阅购买流程的统一入口。
- 订阅第七步补充：会员权益弹窗支持点击卡片购买，权益展示文案和每日训练文案可在系统设置中配置。


### 第八步 A：权益限制接入

当前版本已开始把学员订阅权益接入真实功能入口：免费学员首页图谱卡牌上限 50，深度回忆单题知识点上限 30；付费会员不限制这两项。学习包导入/导出不再按次数限制，所有学员均可使用。详见 `docs/SUBSCRIPTION_STEP8A.md`。



## 订阅第八步 B

已新增本地订阅申请 / 管理员确认开通流程，详见 `docs/SUBSCRIPTION_STEP8B.md`。当前仍不接真实支付，适合作为前端原型和内测流程。


### 订阅卡密

- 新增卡密兑换与后台卡密管理说明：`docs/SUBSCRIPTION_CARD_CODES.md`。
- 系统设置中的卡密列表已分页显示，每页 10 条。

## 基线重构 C-1.2

首页右上角登录区域已改为账号胶囊下拉菜单：已登录只显示用户名称，未登录显示“访客只读”；菜单包含用户中心、帮助中心，以及登录或退出登录。原外置“使用说明”和“退出”按钮不再显示。实现文件为 `src/41-account-menu.js` 与 `styles/account-menu.css`，详细说明见 `docs/BASELINE_REFACTOR_C1_2.md`。


## C-1.3 更新

- 文件页签高度缩减至约 80%，悬停显示关闭按钮。
- 关闭页签不删除文件，当前页签关闭前自动保存并切换。
- 访客可通过全局快捷入口只读查看考题训练与深度回忆页面。
## C-1.4.1 悬浮模块交互修复

- 修复左右悬浮模块被画布拖拽事件拦截，导致账号菜单、升级会员和图谱信息无法点击的问题。
- 悬浮模块内滚轮操作不再缩放画布。
- 画布空白区原有拖拽、缩放和双击操作保持不变。


## C-1.4.3 会员权益弹窗优化

- 会员卡片按实际文案高度自然排版，避免短文案产生大面积空白。
- 全部会员方案保持单行展示，超出区域可横向滚动或滑动查看。
- 卡密区域改为“卡密使用”，删除说明文字并压缩整体高度。

## C-1.4.4：移动端账号入口与触摸页签修复

- 移动端账号入口固定吸附在屏幕顶部右侧，入口简写为“账号”。
- 下拉菜单在移动端使用“用户 / 帮助 / 升级 / 登录或退出”的两字短标签。
- 桌面端继续显示完整账号菜单文字。
- 触摸设备及无 hover 设备的文件页签关闭按钮保持常显。
- 页签栏为移动端账号入口预留空间，避免入口遮挡文件页签。
## v7.9.40 关系线菜单紧凑与虚线比例优化

- 关系线快捷菜单缩小为更紧凑的正圆排布，八个按钮继续保持对称。
- 修复文字按钮被旧样式隐藏的问题，按钮内明确显示 `T` 图标。
- 短虚线按线宽生成正方形小段；长虚线按线宽生成约 4:1 的长方形小段，并保持直角端点。
- 关系文字编辑框移除外围白色底板、白边和额外留白，仅保留输入区域。

## v7.9.41 缩放平滑恢复与 4%-400% 范围

- 点击左下角缩放百分比恢复 100% 时，改为从当前视角平滑过渡，不再生硬跳变。
- 主画布缩放范围调整为 4%～400%，滑轨、滚轮、按钮、触控缩放统一使用该范围。
- 旧文件中的视口缩放读取也同步兼容 4%～400% 范围。

