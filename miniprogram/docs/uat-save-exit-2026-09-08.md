# 保存退出故障修复与 UAT 自检（2026-09-08）

## 原因与修复

1. 开发者工具实际 `/pause` 响应为 422，错误 `INVALID_RUNTIME_STATE_FIELD`，字段 `pendingSelections`。本地小程序发送暂选项，但 UAT 后端缺少对应契约。
2. 保存失败后重试成功，页面直接退出，未清理旧 revision 草稿；重新进入会误报进度冲突。现只在服务端确认暂停/结束成功后清理该会话旧草稿。失败时仍保留草稿，正常跨设备冲突检测不变。
3. 同步 PC 一致性所需的会话快照与多选/超时处理，并保留 UAT 已有的领域权重修复。未整包覆盖网页或其他后端模块。

## 验证证据

- 修复前：新增 UAT 实测脚本调用真实小程序 Page、service 和公网 UAT 接口，在保存步骤复现 422。
- 草稿回归：先观察“重试成功后再次进入仍弹冲突”测试失败，修复后通过。测试不自动接受冲突弹窗。
- `cd miniprogram && npm test`：195 通过。
- 后端相关四组测试：97 通过；叠加当前 UAT 代码的候选会话服务另跑 72 项，通过。
- `miniprogram/tests/manual/uat-save-exit.mjs`：普通、挑战、学霸三种模式均通过真实 UAT 流程：后台保存、暂选多选、标记、答题卡、暂停退出、同客户端恢复、第二客户端恢复、取消退出、交卷、结果和记录、跨用户隔离、结束练习。
- 丢失暂停响应及交卷响应均在服务器真实保存后模拟，重试不重复结算，恢复不再误报冲突。
- 独立测试账号完成后撤销测试令牌并归档，保留其测试记录；未修改真实学员会员权限或替学员交卷。
- 开发者工具现场：原会话成功保存退出，首页出现继续入口，重新打开回到第 4/10 题；服务端核对保留 3 道已答题和第 4 题暂选 B。

## 部署边界

- 后端修复提交：`19016b9d57ee8065d9423ef6b72452e51158747a`，已快进推送 `uat`；保留功能分支 `codex/mini-save-exit-uat`，未变更 `main`。
- UAT 镜像：`lszl-kg-backend:uat-mini-save-19016b9`。基于当前实际镜像仅替换两个做题服务，未构建或 promote 网页 release。
- 远端备份：`/home/ubuntu/lszl-kg-uat/.deploy-state/mini-save-exit-19016b9/backup`，含原服务源及 UAT Compose。
- 继续使用独立小程序凭据及 mini Compose overlay；微信、PC 支付、数据库连接环境变量保持不变，正式容器未重启。
- 小程序重试草稿修复在当前开发者工具使用的本地代码中，已自动重新编译；尚未上传体验版或正式版。当前工作区其余未提交小程序改动没有被批量提交。

## 尚不能据此宣称

以上覆盖本次保存/恢复/交卷主流程，不代表小程序所有业务已完整验收，也不代表真机表现、全部会员状态、错题复仇及正式发布审核已验证通过。用户无需承担开发排障；这些未验证项仍由开发继续检查，不能以本次自检替代最终业务验收。
