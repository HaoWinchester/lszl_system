# 十账号本地业务联调报告

批次：`mini10_0905_073042_ec15`。账号后缀为 `_01` 至 `_10`，均为本地独立学员。

## 结论

- 10/10 账号完成本轮业务断言；另发现 1 处会员状态展示问题，尚未修改业务代码。
- 小程序真实服务层经 HTTP 发出 775 次请求；360 次跨账号读取／写入检查全部被拒绝。
- 10 个账号均通过 PC Cookie 登录接口读取同一份报告，答对数均为 2/3。
- 现有小程序回归 153/153 通过，`git diff --check` 通过。

## 方法与边界

执行真实小程序 Page 处理函数、领域逻辑、HTTP 和服务适配代码，对接正在运行的 `http://127.0.0.1:5173` 与本地 `kg_graph_dev` 数据库。导航、存储和计时等微信宿主能力由测试宿主模拟；不伪造接口成功响应。

这不是 10 台真机或 10 次开发者工具手工点击验收，不能证明原生点击热区、动画、渲染及系统键盘均正常。真实微信 code 交换和支付按用户要求暂停；测试会话由现有服务签发。PC 验证是 Cookie 接口验证，不是 PC 浏览器视觉验收。

每个账号都模拟一次“真实交卷提交成功后，客户端丢失响应”，随后调用页面的同步重试入口；最终报告与个人页完成数验证没有重复完成记录。此项为主动故障注入，不是真实弱网设备实测。

## 账号与最新报告

所有账号共用前缀 `mini10_0905_073042_ec15`。

| 后缀 | 权限场景 | 模式 | 最新完成会话 |
| --- | --- | --- | --- |
| 01 | 免费 | 普通 | ps_7b8fee9ba98246b9b27d414450d16729 |
| 02 | 免费 | 挑战 | ps_5b93bc99d50f45389a2b6a592fe04c0f |
| 03 | 有效月度 | 学霸 | ps_94a4edcbac224a11be4839359cc2ef6f |
| 04 | 有效月度 | 普通 | ps_21d7497aaea146a39d5123a1eb803fb5 |
| 05 | 有效季度 | 挑战 | ps_433c20c3bef64ab8805fbc60373b016a |
| 06 | 有效半年 | 学霸 | ps_5ac14c062cd34b6d843ef9d6954cecfc |
| 07 | 有效终身 | 普通 | ps_ae18fa957f8046db972cae97a82416b2 |
| 08 | 明确 expired 的月度会员 | 挑战 | ps_880b0bb47aed4fe4b929caae68acc49a |
| 09 | status=active 但期限已过的季度会员 | 学霸 | ps_36556d44bf3b467487e6d2e16d037298 |
| 10 | 暂停月度会员 | 普通 | ps_525ea1c214ef4bd483b09a285d17c5b4 |

## 每个账号覆盖的链路

1. Bearer 身份验证、会员权益、会员页面加载、套餐切换（不创建订单）。
2. 免费／会员目录；有权限可开始会员卷，无权限由服务端拒绝。
3. 设置页开始练习 → 标记 → 答题卡开关 → 单选答错 → 下一题 → 保存退出。
4. 删除该测试会话的本机草稿，切换到同账号第二个有效令牌 → 恢复服务端题号、已选答案和标记。
5. 多选 A+C → 下一题 → 最后单选 → 自动交卷 → 响应丢失 → 原提交重试 → 报告答对 2/3、错 1、未答 0。
6. 报告解析打开／关闭、再练一次路由、练习记录筛选、个人页完成数量和经验刷新。
7. 错题重答错误 → 阅读纠错 → 获取真实同知识点变式题 → 验证正确 → 安排延时复习。没有跳过等待期冒充最终掌握。
8. 对另外九个账号的会话、报告、纠错详情分别越权读取，以及越权保存进度；每组均为 404，共 10×9×4=360 项。
9. 退出使当前令牌失效，另一登录会话仍然有效；随后退出第二个会话。
10. PC 账号密码登录接口读取同一份报告并退出。

## 待修问题

第 10 个账号的订阅状态为 `paused`，权益正确为关闭，但会员页面显示“待确认 / 会员权限待确认，请重新同步”。这不是同步故障，反复同步也不能恢复；应明确展示暂停状态并给出正确处理说明。

定位：`miniprogram/domain/subscription-view.ts` 的状态映射只识别 disabled/suspended/expired/active，遗漏后端支持的 paused。个人页复用该映射，同样受影响。本轮为测试任务，未擅自修改业务实现。

## 数据与复跑

新增并保留 10 个测试学员、1 个专用出卷教师、免费／会员各 1 张测试卷及其真实学习记录，便于复查。脚本调试时已完成的较早记录保留，最新会话见上表；没有修改既有真实学员数据。测试令牌不写入文件或报告。

从 backend 目录运行：

```sh
.venv/bin/python tests/manual_ten_mini_accounts.py mini10_0905_073042_ec15
```

复跑会重设这十个专用账号的随机密码、吊销它们之前的测试令牌并创建新会话；不会修改 PC／小程序配置，不部署、不合并、不推送。

脚本初次调试曾修正 Node TypeScript 执行参数、测试输入缺少 paperId、权限拒绝状态预期及 PC 登录请求的测试协议版本；这些属于测试夹具修正，不计作产品缺陷或产品修复。
