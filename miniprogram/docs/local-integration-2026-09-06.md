# 小程序本地真实登录联调 — 2026-09-06

## 范围

- 仅本地验证。UAT、正式服务器均未同步代码、迁移数据库或重启。
- 当前工作区为 detached HEAD，基线 `f2eadc5`；未合入任何分支。
- 新小程序从工作区根目录的本地 `project.config.json` 导入，`miniprogramRoot` 为 `miniprogram/`。开发工具项目名称：幻谱知习 · 本地联调。
- 原 `miniprogram/project.config.json` 的 AppID 保留，不覆盖 PC 登录或付款凭证。
- 小程序凭证仅保存在 `backend/.env.wechat-mini.local`（权限 0600），已排除 Git、Docker 构建与常规 rsync。禁止打印文件内容。
- `docker-compose.mini-uat.yml` 是未来 UAT 使用的独立覆盖配置，本次未使用其部署。

## 初始准备验证（历史记录）

- 新小程序凭证通过微信 `stable_token` 校验，仅输出成功状态，未输出 token 或密钥。
- 本地数据库迁移版本已经是 `f4c8b6d9e120`，本次未运行迁移。
- 本地后端监听 `127.0.0.1:5173`，健康检查数据库正常。
- 演示登录关闭；无效微信 code 返回 `WECHAT_CODE_INVALID`，没有生成演示身份。
- 后端完整测试：665 passed，1 个 python_multipart 依赖弃用警告。
- 小程序测试：176 passed。
- 独立 UAT 配置测试：2 passed，验证基础 PC 环境变量、数据库和端口不变，缺少独立凭证文件时拒绝启动，演示登录强制关闭。
- 微信开发者工具：新 AppID 项目导入成功，显示登录页；Errors 0，Warnings 4。
- `git diff --check` 通过。

## 本地启动

在当前工作区 `backend/` 使用已安装的 Python 依赖环境运行（无需重新安装）：

```python
import logging
import uvicorn

# 微信 code exchange 的 GET URL 含服务器凭证，不能开启 HTTP 请求明文日志。
logging.getLogger('httpx').setLevel(logging.WARNING)
logging.getLogger('httpcore').setLevel(logging.WARNING)
uvicorn.run('app.main:app', host='127.0.0.1', port=5173,
            env_file='.env.wechat-mini.local', log_level='warning')
```

此文件不影响原 `.env` 或 PC 支付配置。开发工具的本地 HTTP 调试设置不得用于正式环境。

## 后续联调

1. 已完成：用户确认协议并发起真实微信登录。
2. 已完成：绑定开放平台后重新登录，自动匹配本地已有 PC 账号，直接进入首页；昵称显示一致。新微信用户自动建号尚未实现。
3. 验证会员权益、试卷权限、作答提交、记录和跨端同步。
4. 本地结果向用户汇报，获得确认后再部署 UAT；UAT 手机验收不能由本地自动化测试替代。

会员支付仍未开放，本次没有发起真实扣款，也没有上传体验版或提交正式审核。

## Ponytail 审查修复与验证

- PC 更新微信资料保留小程序身份字段；小程序通过 UnionID 匹配已有账号后，复用现有绑定函数补存小程序 OpenID，并保留 PC 身份与自定义显示名称。
- 草稿写入异常不再中断选题、判定和内存作答；页面明确提示本机草稿未保存，下次写入成功后恢复正常状态。未同步答案仍可能因直接关闭小程序而丢失，提示用户保存退出或交卷。
- 首页移除未展示的 overview、experience 两个请求；移除未消费的全局认证／网络状态和结果页废弃样式，合并重复样式声明。
- 定向回归先失败后通过：身份字段保留、跨 PC／小程序登录、存储失败与恢复、首页不依赖无用统计请求。
- `npm test --prefix miniprogram`：180 passed。
- 在 `backend/` 使用已有 Python 环境执行 `python -m pytest tests/ -q`：667 passed，1 个既有 python_multipart 弃用警告。测试使用独立、可丢弃的 PostgreSQL 数据库。
- `git diff --check`：通过。
- 微信开发者工具：首页已有登录态正常，结果页和解析弹层可打开／关闭；未观察到运行错误。存储失败通过隔离用例模拟，未在用户设备填满存储；未执行 PC 全页面浏览器登录或真机完整回归。
- 本地后端已重新启动加载修复，健康检查正常。未修改 PC 登录／支付凭证，未提交、合并、推送或部署；会员支付和新用户自动建号仍待单独联调。

## 导航栏截图反馈修正（10:23）

- 本次仅调整截图红框对应的公共导航：修复 `button[class]` 覆盖返回按钮左侧负边距的问题，取消多余 gap；保留原返回按钮宽度，标题使用 32rpx / 500 字重并取消字间距。
- 普通导航与作答工具栏统一为 44px 高；记录、错题页移除重复导航覆盖。保留正文、统计数字字号、主题与业务行为，不改 PC 或后端。
- 11 个注册页面均检查导航源码；10 个普通导航页共用同一规则，作答页保留进度／退出／答题卡专用布局。
- 开发者工具 iPhone 12/13 (Pro) 模拟器逐页查看：首页、记录、结果、练习设置、错题复仇、我的、会员中心、外观与阅读、试卷库。二级页箭头和标题紧凑对齐，返回路径正常；结果页已实见新样式，检查时 Errors 0。
- 登录页及作答页本轮仅代码与自动化检查，未退出当前账号或新建练习；未进行真机／多机型完整视觉回归。最后用户将前台切至另一旧项目「幻谱学习」，未继续操作旧项目。
- 回归用例先失败后修正通过；小程序全量测试 183 passed，导航／排版专项 10 passed；`git diff --check` 通过。
- 查看新样式请使用当前工作区对应「幻谱知习 · 本地联调」项目；未提交、推送或部署。

## 多选题自动解析停留修复（历史方案，已由下方简化方案替代）

- 原因：原 `onNext` 判定多选答案后立即跳题，解析虽然已生成，但只能返回原题看到。
- 普通练习开启自动解析时，多选题底部为「查看解析」；未选时禁用，选择仍可增减，点击后判题并留在本题，再点「下一题」。最后一题展示解析后为「交卷」，不会立即提交；已完成但尚未交卷时可返回检查。
- 关闭自动解析、显示答案浏览以及挑战／学霸流程不增加解析停留步骤；判题、经验、生命和后端结算逻辑不变，答题卡显式交卷入口不变。
- 六个新增行为回归覆盖：本题解析停留与防重复判题、未选／取消全选、末题交卷失败重试、挑战、学霸、开关切换。调整旧用例明确在自动解析关闭时验证直接翻页／自动交卷。
- `npm test --prefix miniprogram`：189 passed；`git diff --check` 通过。
- 本地后端停止后已重新启动，健康检查与数据库正常。开发者工具编译时出现 `wx://not-found` 空白页，重新打开项目后恢复，Errors 0；已打开原有单选练习，解析组件及底部按钮显示正常。用户操作界面后停止点击，未完成真实多选题点击链路／真机回归，这部分仅隔离自动化验证；未新增练习或修改所选答案。
- 未修改后端／PC 配置，未提交、推送或部署。

## 移除自动解析，统一由显示答案控制

- 按用户最新决定移除小程序「自动解析」开关及「查看解析」停留步骤；多选选择后直接下一题，保留原判题与交卷行为。
- 普通练习解析仅由「显示答案」控制；关闭后作答、翻页再返回均不会自动展开解析。已判题的对错反馈保留，挑战／学霸模式限制不变。
- 旧草稿中的 `autoExplain` 不再控制小程序展示；共享运行态仍透传该字段，避免覆盖 PC 偏好，未修改 PC 或后端。
- 回归先出现 3 项预期失败，修正后 `npm test --prefix miniprogram` 为 188 passed；覆盖旧设置两种取值、单选不自动展开、多选直接翻页及显示答案切换。
- 已重新打开当前工作区「幻谱知习 · 本地联调」，进入既有未作答练习，实测关闭「显示答案」收起答案与解析、再次打开恢复，并恢复原开关状态；页面不再出现自动解析开关，开发者工具 Errors 0。未选择答案或交卷。
- 多选直接翻页由隔离行为测试验证，尚未做真机回归。仅本地修改，未提交、推送或部署。

## 合并作答辅助操作行

- 按用户确认的布局将「显示答案」移至「标记此题」左侧，共用右对齐辅助操作组，字号、垂直中心、触控高度统一；移除独立设置行，题目自然上移。底部答题卡／上一题／下一题不变。
- 将“去掉”限定为隐藏常驻「本地草稿」文案，保留本机保存机制；左侧仍显示正在保存、已保存、离线、进度冲突、草稿未保存等状态，不隐瞒异常。
- 新增两项契约先失败后通过，覆盖操作行归属、普通练习限制和各保存状态可见性。`npm test --prefix miniprogram`：192 passed；`git diff --check` 通过。
- 微信开发者工具 iPhone 12/13 (Pro) 模拟器截图确认同排居中、独立设置行已消失；实点显示答案开／关、标记／取消标记正常，均恢复原值，停留第 2 / 10 题。未改所选答案或交卷，Errors 0。
- 保存异常的可见性由自动化验证，未在真实练习中制造断网或存储失败；未做真机／多机型视觉回归。仅本地修改，未提交、推送或部署。

### 截图反馈后的比例修正

- 用户指出上一版隐藏保存提示后出现左侧大块空白，原生开关体积过大。根因是操作组 `margin-left: auto` 与原生 switch 默认尺寸；“同排”契约未覆盖实际分布与比例。
- 操作组改为占满剩余空间、两端分布；本地草稿隐藏时显示答案贴左、标记贴右。非本地保存状态仍显示在左侧，不删除保存／异常反馈。移除辅助行额外上下 padding。
- 原生 switch 保留，视觉缩放 0.7 并置于 38×24px 容器；外层 label 保留 96rpx 触控高度。标记按钮 margin 使用足够优先级，避免全局 button reset 影响仅标记模式的右对齐。
- 两项新增布局契约先失败后修正；全量 `npm test --prefix miniprogram` 194 passed，`git diff --check` 通过。
- 开发者工具 iPhone 12/13 (Pro) 截图核对左右分布、开关大小和行高；实点 label 开启解析、开关本体关闭解析，恢复原值，未改答案或交卷。未进行真机／多机型验证，未提交或部署。
