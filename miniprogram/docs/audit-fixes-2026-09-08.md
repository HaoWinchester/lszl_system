# 小程序审计问题修复与验证

## 修复范围

- 补齐纠错恢复 GET 接口；重进纠错不重复答原题、不自动标记已读，保持账号隔离。
- 错题作答和变式验证按 requestId 去重；同键不同答案拒绝；数据库写锁后重新读取状态，避免并发重试返回旧计数。
- 去重仅接受服务器生成的专用事件 ID，公共学习事件接口禁止伪造这两类事件；旧客户端事件不被当作凭证；验证记录还需匹配当前账号和错题。
- 增加 `sessions/active?summary=true`，仅查询列表必需字段，不查询整卷 JSON；首页、记录、我的统一使用摘要。PC 默认完整接口保持兼容，暂停记录原有经验/耗时不会被摘要清零。
- 选项展示仅去掉与当前选项 ID 匹配的明确前缀，保留答案 ID、原始数据、API/H.264 等正文。中英文统一处理。
- 试卷库可继续加载第 101 份及后续试卷，触底加载与按钮入口共用处理；失败保留列表、重试原页、防重复请求，并明确筛选已载入范围。
- 常规 UAT 部署统一带小程序配置 overlay；新增只读 `--check-config`，缺失配置立即失败，不输出展开后的密钥。PC 登录、支付配置不变。

## 验证方法

按系统化调试、测试驱动及前端需求质量门禁执行。新增测试先复现失败，再实施修复；独立代码审查提出的事件信任边界和历史数据合并问题也已补测试。开发者工具确认选项编号不再重复，运行错误为 0，未修改原学员的答案。

本地最终验证：小程序 199 项通过；后端全量 671 项通过（415.17 秒，1 个既有依赖弃用警告）；相关后端 93 项通过（含旧缓存重试、历史事件、伪造事件和跨账号记录防护）；部署配置 4 项通过。

## UAT 部署与复测（2026-09-08）

- 修复代码提交：`1c5cf277fb371ee5e1791a05e49a4f7f686e126f`，已推送 `codex/mini-audit-fixes` 与 `uat`。报告补充提交不改变部署代码版本。
- UAT 镜像：`lszl-kg-backend:uat-mini-audit-1c5cf27`，镜像 ID `sha256:9f9b873e942f52599c4279757ac665b65047bbce6f6810b28e7729bf37bdc18b`。三个源文件的镜像内 SHA-256 与候选一致。
- 部署前备份：`/home/ubuntu/lszl-kg-uat/.deploy-state/mini-audit-1c5cf27/backup/`，包含 `code.tgz`、`uat.dump`、原 Compose 与 `manifest.txt`；代码归档和 25 MB 数据库备份均非空，并分别通过归档目录和 `pg_restore --list` 校验。保留旧镜像可回退。
- 重建的仅是 UAT backend；健康检查与数据库正常，Alembic current/head 均为 `f4c8b6d9e120`。正式容器 ID 和启动时间与部署前一致。
- 原备份和当前 Compose 的环境变量映射逐项一致，运行容器环境也与镜像默认值加 Compose 配置完全一致；比较过程中不输出值。PC 微信服务与小程序微信服务文件哈希均未变化。
- 独立测试账号覆盖未开通、有效会员、已过期三种状态：**34 组通过、0 失败，共 300 个已记录请求**。原始脱敏结果见 [UAT 修复后审计数据](uat-experience-audit-fixed-2026-09-08.json)。另有 PC cookie 登录/报告对照请求不计入 300。
- 通过场景包括会员到期展示及后端访问限制、页面数据加载、离线刷新保留数据和恢复、快速重入防重复请求、交卷判分及 PC 结果一致、纠错退出恢复、无变式时退出、请求去重及伪造事件拒绝、180 题保存恢复、单设备退出和第二设备登录态保留。
- 测试产生的 400/401/404/422 是断言要求的拒绝场景，不是未处理的业务失败。测试令牌已撤销，三名综合测试用户已归档，保留测试记录供追溯；未修改原学员的答案和权限。
- 保存退出专项另建三名独立测试用户，**普通、挑战、学霸三种模式全部通过**，各覆盖后台保存、保存退出、多选暂存、标记与答题卡、第二端恢复、取消退出、交卷、响应丢失重试、报告记录、账号隔离及放弃。专项用户也已撤销令牌并归档，见 [保存退出复测数据](uat-save-exit-fixed-2026-09-08.json)。

### 响应时间与数据量

以下是本机经公网访问 UAT 的页面逻辑数据准备耗时，不含真实手机原生渲染、动画帧率或微信冷启动；样本较少，不能当成容量测试结论。

| 项目 | 样本数 | P50 | P95 |
| --- | ---: | ---: | ---: |
| 首页数据准备 | 15 | 462 ms | 564 ms |
| 记录数据准备 | 15 | 224 ms | 555 ms |
| 我的数据准备 | 15 | 556 ms | 1052 ms |
| 试卷库数据准备 | 3 | 904 ms | 1017 ms |

180 题暂停记录的 active 列表响应由基线最大 **735,384 字节**降为 **476 字节**，减少约 **99.94%**；列表不再携带整卷，进入练习时仍获取完整内容。完整 180 题开始/暂停/恢复分别测得 577/723/483 ms；完整练习响应仍约 884 KB，并非所有接口都缩小到摘要级别。

本轮记录页与我的页中位数并未比基线更低，因此不宣称“所有页面速度都提升”。本次已消除确定的列表过量传输；试卷库约 1 秒、我的页偶发约 1 秒的数据准备仍是后续性能观察点。

## 集成边界

当前工作区原有小程序页面与公共组件尚未提交。UAT 候选包含这套已经通过测试的本地小程序基线及其依赖，不把新增页面引用到的组件漏掉；不是只提交几个孤立页面。保留原工作区和本地项目配置，未改 PC 微信登录/支付服务。

实际 UAT 后端更新仅替换 learning 路由、learning_service、practice_session_service 三个文件，保留原镜像其他内容、现有页面 active release、数据库与独立凭据；不新增数据库迁移，启动时仍执行既有迁移检查并核对 head 一致；不修改 main，不上传微信体验包或正式版。

## 尚未代表通过的项目

开发自检与 UAT 接口复测不是用户 UAT 验收。真实手机低端性能、系统杀进程恢复、不同网络切换、完整隐私文档以及体验包上传仍不能据此宣称完成。当前用户未关联微信身份时的绑定/注册行为也未在本轮改动。
