# v8.6.0.2 Fix Report

## Fixed

1. The home-path pulse now follows the latest available learning frontier recorded in `currentNodeId`.
2. Non-contiguous legacy progress can expose earlier remediation gaps without creating multiple pulse rings.
3. Activity packages now validate `createdAt`, `updatedAt`, package chronology, duplicate IDs, conflict IDs, and content hashes.
4. Browser regression tests no longer perform a real placement-test exit navigation.

## Regression coverage

- Normal sequential unlock
- Non-contiguous legacy progress
- Exactly one current pulse ring
- Fully completed course with no pulse ring
- Package hash tampering
- Duplicate and conflicting activity IDs
- Invalid and reversed package timestamps
- Partial English field fallback
