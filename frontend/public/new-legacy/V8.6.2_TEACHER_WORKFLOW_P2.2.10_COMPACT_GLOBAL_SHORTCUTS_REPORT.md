# v8.6.2 P2.2.10 全局快捷栏轻量化

基线：P2.2.9。

## 本轮完成
1. 全局快捷栏中的“考题训练”改为“多题归纳”。
   - 点击进入 `question-workspace.html`。
   - 继续沿用 `useTraining` 权限，不修改角色权限模型。
   - 图标改为多卡片/多题画布语义。

2. 全局快捷栏改为纯图标轻量模式。
   - 纵向栏宽度约 52px。
   - 快捷入口约 38×38px。
   - 不显示“全局快捷 / 横向排布 / 纵向排布”等文字。
   - 不显示各快捷入口文字标签。
   - 保留 title 悬浮提示、权限判断、拖拽与横/纵布局切换。

未修改题目、课程、学习进度、成绩、解锁或知识联想数据。
