# v8.6.2 P2.2.9 当前题目重置与完成状态强化

基线：P2.2.8。

## 本轮完成

1. 单题页面右上角工具栏新增“重置当前题目”图标按钮。
   - 位于 Aa 字体按钮之后、当前卡之前。
   - 复用现有 `resetQuestionTrainer()`。
   - 会重置当前题目的作答状态、五步学习 Session 和卡牌完成状态。
   - 与原“重置”按钮职责区分：新图标重置题目学习状态，原文字“重置”仍只重置五张卡片布局。

2. 完成卡牌顶部状态显著强化。
   - 完成后的卡片标题栏改为纯绿色 `#16a34a`。
   - STEP、标题、步骤数字改为白色。
   - “已完成”状态改为更大的白色半透明标识，并增加勾选符号。
   - 整题完成后第 5 张卡也进入 `done` 状态，因此五张完成卡都会使用统一绿色标题栏。

未修改题目、课程、成绩、解锁、题库或知识联想数据。
