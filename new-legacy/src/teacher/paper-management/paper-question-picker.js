'use strict';
(function(global){const root=global.KGTeacherDomains=global.KGTeacherDomains||{},Core=root.Core;function create(options={}){function add(paper,refs){const existing=new Set((paper.questions||[]).map(ref=>`${ref.bankId}::${ref.questionId}`)),added=[],duplicates=[];(refs||[]).forEach(ref=>{const key=`${ref.bankId}::${ref.questionId}`;if(existing.has(key)){duplicates.push(ref);return}existing.add(key);paper.questions.push({...Core.clone(ref),order:paper.questions.length+1,score:Number(ref.score||1)});added.push(ref)});paper.updatedAt=Date.now();return Core.result(true,{paper,added,duplicates},[],duplicates.length?[`跳过 ${duplicates.length} 道重复题。`]:[])}function remove(paper,keys){const set=new Set(keys||[]),before=paper.questions.length;paper.questions=(paper.questions||[]).filter(ref=>!set.has(`${ref.bankId}::${ref.questionId}`)).map((ref,index)=>({...ref,order:index+1}));paper.updatedAt=Date.now();return Core.result(true,{paper,removed:before-paper.questions.length})}return Object.freeze({add,remove})}root.PaperManagement=root.PaperManagement||{};root.PaperManagement.PaperQuestionPicker=Object.freeze({create})})(globalThis);
