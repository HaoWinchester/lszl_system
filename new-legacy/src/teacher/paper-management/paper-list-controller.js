'use strict';
(function(global){const root=global.KGTeacherDomains=global.KGTeacherDomains||{};function create(options={}){let state={category:'ALL',status:'ALL',search:'',page:1,pageSize:Number(options.pageSize)||18,selectedIds:new Set(),selectedId:''};const statusOf=p=>p.status==='archived'?'archived':p.status==='published'?'published':'draft';const rows=()=>{const q=state.search.trim().toLowerCase();return (options.getPapers?.()||[]).filter(p=>{if(state.category==='UNCATEGORIZED'&&p.categoryId)return false;if(!['ALL','UNCATEGORIZED'].includes(state.category)&&p.categoryId!==state.category)return false;if(state.status!=='ALL'&&statusOf(p)!==state.status)return false;return !q||String([p.name,p.subject,p.description,options.categoryName?.(p.categoryId)||''].join(' ')).toLowerCase().includes(q)}).sort((a,b)=>Number(b.updatedAt||0)-Number(a.updatedAt||0))};const snapshot=()=>{const all=rows(),pages=Math.max(1,Math.ceil(all.length/state.pageSize));state.page=Math.min(Math.max(1,state.page),pages);return {...state,selectedIds:[...state.selectedIds],rows:all.slice((state.page-1)*state.pageSize,state.page*state.pageSize),allRows:all,total:all.length,pages}};return Object.freeze({snapshot,setFilter:patch=>{state={...state,...patch,page:patch.page??1};return snapshot()},select:id=>{state.selectedId=String(id||'');return snapshot()},toggle:id=>{id=String(id||'');state.selectedIds.has(id)?state.selectedIds.delete(id):state.selectedIds.add(id);return snapshot()},clearSelection:()=>{state.selectedIds.clear();return snapshot()}})}root.PaperManagement=root.PaperManagement||{};root.PaperManagement.PaperListController=Object.freeze({create})})(globalThis);
