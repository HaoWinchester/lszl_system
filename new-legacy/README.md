## v8.6.0

- 建立 Activity Schema v1：活动库使用稳定活动 ID、`schemaVersion: 1`、中英文内容槽位、独立答案结构、解释结构和元数据。
- 新增旧活动确定性迁移器与兼容物化层；现有运行器继续使用原题型接口，无需推倒重写。
- 单项选择、关键词识别、连线配对、开放表达和翻牌记忆完成专用 Schema 适配；正确答案改由稳定 optionId、segmentId 和 pairId 表达。
- 深度回忆、多题归纳、知识图谱和综合挑战先接入兼容适配器，保留原有运行能力与活动 ID。
- 数据层支持 `zh | en | bilingual` 三种题目语言模式；节点练习页和跳级测试页新增语言切换，英文缺失时安全回退中文。
- 新增 Activity Schema JSON Schema、字段说明、活动库校验、导出包和导入包校验接口。
- 修复历史进度出现非连续完成节点时产生多个 available 节点和两个脉冲环的问题；解锁重算与路径渲染均保证只有一个当前位置。
- 版本升级为 v8.6.0，课程数据版本升级为 11，学习进度格式保持 schemaVersion 4。

## v8.5.4.4

- 修复节点答题页主操作按钮在禁用状态下自动隐藏深色底座的问题。
- 禁用状态继续保留统一的 4px 深色立体底座，仅通过降低透明度表达暂不可操作。
- 禁用按钮悬浮或按下时保持原位，不产生亮度变化或下沉反馈。
- 同步覆盖跳级测试主按钮与角色主题场景，防止页面局部 `:disabled` 样式清空底座。
- 新增普通节点与跳级测试初始禁用状态的浏览器回归检查。

## v8.5.4.3

- 修复节点答题页主操作按钮在普通状态下深色底座被旧 `button.primary` 样式覆盖、仅悬浮时恢复的问题。
- 修复学习路径“参加跳级测试”入口按钮的同类问题，普通、悬浮和键盘聚焦状态均稳定保留 4px 深色底座。
- 从学习路径、节点运行器和结果页按钮中移除旧版 `primary` / `secondary` 标记类。
- 在全局样式末尾增加统一按钮组件保护层，角色主题和旧按钮规则不能再覆盖组件样式。

## v8.5.4.2

- 建立统一的 `.ui-button` 操作按钮组件，主按钮统一为 160×48px、13px 圆角和 4px 深色底座。
- 移除全局 `button:hover` 上移规则，所有普通按钮悬浮时保持原位，避免页面间样式互相覆盖。
- 跳级测试页将“提交答案 / 下一项 / 查看结果”合并为同一个固定按钮元素，只切换文字和动作，彻底消除位置与尺寸跳跃。
- 节点答题页底部改为固定主按钮槽位；出现“提示＋继续”时，提示按钮位于左侧，主按钮始终固定在右侧。
- 跳级测试、普通节点、复合节点和完成结果页的主/辅助操作按钮统一高度、宽度、字体、圆角、悬浮、聚焦、禁用和按压反馈。
- 主按钮按下时下沉 4px 并收起深色底座，松开后恢复；答案选项按下时提供 2px 轻量反馈，选中状态不再上移。
- 为选择题、关键词、连线、翻牌、多题归纳和知识图谱选项接入统一的 `ui-option-control` 交互规则。
- 新增固定槽位、单按钮状态切换、全局无悬浮位移和按压反馈回归检查。

## v8.5.4.1

- 修复部分跳级测试页面按钮悬浮时受全局 `button:hover` 影响而上移、阴影被替换或选中状态显示异常的问题。
- 跳级测试的开始、提交、下一项、答案选项、关键词、连线和结果页按钮均使用页面内独立悬浮规则。
- 学习路径中的跳级选择弹窗按钮也取消悬浮位移，主按钮的深色底座保持稳定。
- 跳级测试通过后，节点统一使用普通完成样式，不再显示虚线边框、“跳级通过”状态或节点级完成方式。
- 节点进度记录移除 `completionMethod` 与 `completionType`，进度结构升级为 `schemaVersion: 4`；旧数据会自动迁移为统一完成状态。
- 轻量保留 `placementTests[partId]` 测试记录，用于未来学习分析，不影响学员端和教师端界面。
- 节点悬浮说明简化为只显示节点名称，移除副标题、状态、预计时长等附加信息。

## v8.5.4

- 完成 v8.5.3 缺陷审查：综合挑战、统计持久化、管理员预览、部分完成和三类复合节点未发现阻断性业务缺陷。
- 新增“部分跳级测试”：学员从每个部分的第一个节点选择“正常开始”或“参加跳级测试”。
- 每个部分固定配置 12 项代表性客观任务：10 道单项选择、1 个关键词识别和 1 个连线配对。
- 跳级测试答对至少 10 项方可通过；失败只记录成绩，不改变任何节点解锁状态。
- 测试通过后，当前部分全部节点以 `completionMethod: "placement_test"` 完成，并自动开放下一部分首节点。
- 通过跳级的节点仍可返回练习；重新完整学习某节点后，该节点会切换为 `completionMethod: "normal"`。
- 已正常完成的节点不会被后续跳级测试覆盖，正常学习与跳级记录可在同一部分中准确共存。
- 存储结构升级至 schemaVersion 3，并兼容迁移旧 `completionType: "passed_by_test"` 数据。
- 新增独立的 `placementTests` 测试记录，保存尝试次数、最近成绩、最好成绩、通过时间和最近 10 次历史。
- 路径页新增跳级入口徽标、学习方式选择弹窗，以及“跳级通过”节点状态。
- 新增 `guided-learning-placement-test.html`、专用样式和自动判分运行器。
- 修复跳级节点回头正常学习时仍沿用跳级完成时间的问题；完成方式改变时会刷新完成时间。
- 修复浏览器测试夹具中的空阶段按钮、冻结过渡、帧调度和受限 Chromium 资源累积问题。
- 新增跳级配置、失败不解锁、通过整部分开放、旧数据迁移、正常记录保护、完成方式切换及桌面矩阵回归。

## v8.5.3

- 将每个部分第 12 个“部分挑战”占位节点正式升级为 `part_challenge` 综合挑战节点，同时保留既有节点 ID，兼容原有学习进度。
- 每个挑战固定配置 8 个活动：4 道单项选择、1 个关键词识别、1 个连线配对、1 个开放表达和 1 个复合任务。
- 复合任务按部分在深度回忆、多题归纳和知识图谱之间轮换，允许普通题型与复杂任务在同一挑战中运行。
- 挑战活动通过 `challengeConfig` 结构化配置，记录当前部分、前置节点、固定活动 ID、活动数量及最终全对规则，为后续教师后台编辑预留接口。
- 错误活动继续进入队尾，所有活动最终答对后才完成挑战；挑战节点完成后，整个部分同步完成。
- 新增活动级首答记录，不再使用“正确次数 ÷ 总尝试次数”替代首答成绩。
- 新增 `firstAttemptTotal`、`firstAttemptCorrect`、`firstAttemptAccuracy`、`typePerformance`、`weakestType` 和 `challengePartId` 等挑战统计字段。
- 新增挑战运行状态栏，显示已完成任务数和当前题型。
- 新增专属“本部分挑战完成”页面，展示首答正确率、活跃用时、最长连对、最薄弱题型及分题型表现。
- 管理员挑战测试继续保持预览隔离，不写入学员正式解锁进度。
- 新增挑战配置、错题回队列、首答统计、分题型表现、薄弱题型和部分完成状态自动化回归。

## v8.5.2

- 新增固定结构的“知识图谱”复合节点，并将每部分第 10 个节点由“综合配对回忆”升级为知识图谱；继续沿用原节点 ID，保持已有进度数据兼容。
- 一个知识图谱活动包含 7 个知识节点、7 条基础关系和 4 个待完成项目，符合 5～8 个节点、4～7 条关系、2～4 个任务的第一版范围。
- 新增三个内部阶段：补全缺失知识点、选择节点之间的关系、找出错误连接。
- 知识点补全和关系选择使用结构化选项；错误连接可直接点击图谱中的关系标签。
- 每项任务答错后进入当前阶段队尾，全部任务最终正确后才完成节点。
- 每项任务支持两级渐进提示；提示使用次数统一计入节点统计。
- 正确率、活跃用时、最长连对、总尝试次数和提示次数继续复用现有节点运行框架。
- 新增 `knowledge_graph` 活动插件、课程数据结构、固定小图谱布局、响应式样式和专用 SVG 图标。
- 自动化回归新增知识图谱完整流程，并继续覆盖管理员测试、椭圆节点、外围脉冲环、深度回忆和多题归纳。

## v8.5.1.1

- “处理顺序表达”节点改为混合题队列：前 4 道为单项选择，最后 1 道为简答。
- 最后一题简答只要求填写非空内容，不再要求至少 30 字；提交后直接展示参考答案。
- “题干结构”节点更名为“题干结构图”，每道题首次答错并进入队尾后，第二次出现时开放“提示”按钮。
- 管理员账号可直接打开全部阶段和节点；管理员测试不写入学员解锁进度，避免来回测试污染学习记录。
- 首页所有节点由正圆调整为轻微横向椭圆，桌面端与移动端分别优化比例。
- 取消当前节点按钮本体的呼吸缩放，改为按钮外围独立脉冲环，节点尺寸和位置保持稳定。
- 新增混合题节点、题干结构提示、管理员全节点访问、椭圆比例和外围脉冲环回归测试。

## v8.5.1

- 新增“多题归纳”复合节点插件：完成三道相关原题 → 固定分类画布 → 排列通用判断规则。
- 原题答错后自动进入当前复合任务队尾，全部源题正确后才进入归纳画布。
- 分类画布支持拖放，以及“选中卡片 → 放入分类”的无拖放操作；错误时只反馈分类错误数量，不暴露具体答案。
- 通用规则排序支持拖动和上移 / 下移按钮；连续答错 2 次、4 次后分别开放一级、二级渐进提示，提示使用计入节点统计。
- 多题归纳完整接入节点进度、正确率、活跃用时、最长连对、提示次数和完成持久化。
- 演示课程每个部分第 11 个节点升级为多题归纳，仍保持 3 个阶段、9 个部分、108 个节点和每部分 12 个节点。
- 新增 `multi_question_induction` 活动数据结构、独立 SVG 图标与 Chromium 完整流程回归测试。
- 修正浏览器回归夹具，使顶部阶段按钮在真实 class、全局样式和主题切换条件下验证悬停不位移、深色底层不中断。

## v8.5

- 修复首页顶部阶段按钮悬停时深色立体底层消失的问题。
- 悬停状态显式覆盖全局 `button:hover` 的位移与阴影规则，按钮保持原位，深色底层保持可见。
- 移除阶段按钮悬停时的 `filter` 合成效果，避免部分浏览器重绘时出现图层闪隐。
- 桌面端与移动端使用统一的深度变量，分别保留 6px 与 5px 的立体底层。

## v8.4.30.25-light-alpha

- 深度回忆的线索识别、知识判断和路径排序全部接入渐进式提示：同一子任务答错 2 次开放第一条，答错 4 次开放第二条；提示使用计入节点完成统计。
- 新增 `KGGuidedLearningIconRegistry`，首页节点由活动类型映射为统一内联 SVG；未知类型自动使用默认图标，节点数据不再保存单字图标。
- 首页节点统一为独立“正面＋底座”结构，移除悬浮上移；点击时正面下压，当前节点使用更深的部分色和轻量缩放呼吸，已完成节点仅以亮色表示并取消小勾。
- 新增 8 色部分主题循环；部分标题越过横向视口激活线时，顶部标题、说明和主题色同步切换。顶部阶段按钮移除箭头图标和圆形背景。
- 预留 `recompleted` 金色二刷视觉状态，不改变当前完成与解锁数据协议。
- 回归测试新增三环节提示使用、SVG 图标回退、节点按压状态、完成勾移除、部分颜色滚动联动和浏览位置恢复验证。

## v8.4.30.24-light-alpha

- 新增 `KGGuidedLearningActivityRegistry`，标准活动与复合活动统一通过插件注册、渲染、提交和工作量接口运行。
- 节点数据新增 `runMode: standard | composite` 与 `contentForNode()`，路径入口保持不变，节点页可按内容模式加载不同运行组件。
- 完成第一种复合节点“深度回忆”：关键线索识别 → 3 道知识判断 → 推理路径排序。
- 深度回忆的知识判断答错后进入该阶段队尾；线索和排序答错只提示误选、遗漏或错误位置数量，不展示正确答案。
- 推理路径同时支持鼠标拖动和上移 / 下移按钮，移动端无需依赖拖放手势。
- 复合节点的全部子任务接入统一顶部进度条、正确率、活跃用时、最长连对和节点完成持久化。
- 演示课程每个部分第 7 个节点改为深度回忆测试节点；课程仍为 3 个阶段、9 个部分、108 个节点，活动库扩展为 84 个固定活动。
- 69 个源代码 JavaScript 文件通过语法检查；Node 数据测试和 Chromium 回归覆盖插件注册、双运行模式、深度回忆错题循环、排序重试、完成统计及严格解锁。

## v8.4.30.23-light-alpha

- 开放文本题增加渐进式提示：连续答错 2 次后出现“查看提示”，连续答错 4 次后可开放第二条提示；提示只在当前节点内存中生效。
- 节点完成面板新增本轮正确率、活跃用时和最长连对；完成统计随节点结果一次性保存，不保存未完成会话。
- 移除节点页右上角活动数量和完成面板说明段落；顶部只保留退出、标题与进度条。
- 将原“主动回忆”替换为可自动判定的翻牌记忆游戏；每个游戏固定配置 3～5 对卡片，错误配对盖回，正确配对保持翻开。
- 普通节点支持教师固定配置 5～8 个活动；当前演示节点分别配置 5、6、7 或 8 个活动，不启用随机抽题。
- 活动库扩展为 83 个固定活动；课程仍为 3 个阶段、9 个部分和 108 个节点。
- 67 个 JavaScript 文件通过语法检查；Node 数据测试和 Chromium 回归覆盖渐进提示、翻牌配对、完成统计、可变活动数量及严格解锁。

## v8.4.30.22-light-alpha

- 完成节点学习页第一批开发：统一顶部“退出＋节点标题＋进度条”和底部固定操作/反馈栏。
- 接入单项选择、关键词识别、连线配对、开放文本、主动回忆五种普通题型。
- 单选题采用“选择后检查”，答错不展示正确答案；答对显示简短反馈并可展开详细解析。
- 关键词题保持自然连续题干，预切分片段默认无边框、无间距差异；明确选择数量，答错仅提示误选与遗漏数量。
- 配对题改为先完成全部连接后统一检查，答错只提示错误组数，再次出现时清空全部配对。
- 开放文本按教师配置的核心要点与可接受表达判断，答错提示命中数量和缺失方向，再次出现时清空回答。
- 主动回忆支持 15 秒倒计时、提前查看参考内容和三级自评；只有“完全想起”才计入完成。
- 所有普通题型统一使用内存错题队列；未完成节点不写入存储，全部正确后才点亮节点并解锁下一节点。
- 12 个演示节点中循环配置五种普通题型；当前课程共 80 个可复用固定活动。
- 更新 Node 数据测试和 Chromium 内存页面回归，覆盖五种题型、详细解析、错题循环与完成持久化。

## v8.4.30.21-light-alpha

- 部分分割线去掉完成数量比例，只保留竖排“第 N 部分”和部分标题。
- 去掉胶囊形边框、阴影和按钮感，分隔区仅保留竖向文字与细线。
- 取消节点之间的 SVG 曲线连线，继续保留节点沿中心线上下曲线节奏分布。
- 减少每个部分额外 SVG/path 元素，长阶段包含上百节点时滚动渲染更轻。

## v8.4.30.20-light-alpha

- PC 学习路径改为单一横向滚动轨道，阶段内所有部分从左到右连续排列，不再纵向堆叠。
- 节点按横向 S 型轨迹排布，支持滚轮横移、鼠标拖动、触控板滑动和左右翻页按钮。
- 每个阶段预设 3 个部分，每个部分 12 个节点；当前演示课程共 3 个阶段、9 个部分、108 个节点。
- 路径页只渲染当前阶段的 36 个节点，节点练习仍使用独立页面。

## v8.4.30.18-light-alpha

- 学习路径首页改为参考游戏化课程路径的轻量布局：顶部整块阶段切换卡片，点击后打开阶段列表并选择查看阶段。
- 移除平铺阶段标签和部分卡片；部分之间改用灰色分割线、部分标题、目标与完成数量衔接。
- 移动端节点按纵向 S 型轨迹排列，PC 端按横向 S 型轨迹排列；短路径自动居中，长路径支持横向滚动。
- 已完成节点和当前可学习节点使用绿色立体按钮，后续锁定节点使用灰色按钮；当前节点显示轻量标题提示。
- 节点继续使用独立练习页面，不在路径页加载题目正文；未加入 NPC 角色图标。
- 阶段选择器在移动端使用底部弹层、PC 端使用居中弹层，并展示阶段完成 / 可学习 / 未解锁状态。
- “回到当前位置”改为轻量悬浮定位按钮，方向会根据当前节点相对视口的位置自动变化。
- 更新浏览器内存回归测试，验证阶段选择、部分分割线、S 型节点结构和严格锁定状态。

## v8.4.30.17-light-alpha

- 引导学习模式重构为“课程 → 阶段 → 部分 → 节点 → 活动”的固定课程编排。
- 首页改为轻量纵向阶段路径，只加载节点元数据和完成状态；题目内容进入节点后再加载。
- 新增独立节点练习页 `guided-learning-node.html`，普通节点固定 5 个同类型活动，节点内使用进度条展示完成度。
- 中途退出、刷新或关闭不保存任何节点内进度；答错活动移到队尾循环，全部正确后才保存完成节点并严格解锁下一节点。
- 第一版移除动态抽题、自动穿插、待复习状态、XP 和未完成恢复；保留部分 / 阶段跳级测试的数据接口。
- 示范课程包含 3 个阶段、6 个部分、12 个节点和 60 个固定活动；新增架构说明与数据存储回归测试。
- 67 个源代码 JavaScript 文件通过语法检查，引导学习 v2 数据与严格解锁测试通过；Chromium 仍受当前容器策略影响，未声明真实浏览器交互验证通过。

## v8.4.30.16-light-alpha

- 以 v8.4.30.13-alpha 为基线，重建轻量画布支线：固定右侧可拖动解析面板、缩放期间隐藏多选悬浮栏、缩略图视口增量更新、相邻关系线更新、平面纯色卡片、不透明分组、紧凑模式完整题干、稳定多试卷题目标识。
- 修复关系颜色选择器在原生选色窗口打开后丢失活动关系的问题；颜色预览即时更新，确认后持久化并进入撤销历史。
- 新增游戏化引导学习首页 `learning-path.html`：PC 横向路径、移动端纵向路径，默认进入学习模式并保留自由模式。
- 新增课程、阶段、节点、活动和用户进度模型；示范章节覆盖了解、理解、分析、应用、整合五阶段与 12 个节点。
- 新增单选、关键词点击、连线配对、文本输入、知识回忆五类可配置活动，以及解锁、完成、重试、复习和刷新恢复。
- 引导学习进度独立存储，不写入多题画布 schema；画布继续保持 schema v6。
- 66 个源代码 JavaScript 文件通过语法检查，38 项 Node 测试通过；Chromium 因当前容器策略无法完成 localhost 导航，未声明浏览器交互验证通过。

## v8.4.30.13-alpha

- 修复多题卡 CSS 最小宽度 380px 与存储最小宽度 260px 不一致，导致手动缩窄后 DOM、布局数据和关系线端点发生偏差的问题；历史窄卡读取时自动归一化。
- 关系几何改为优先读取卡片实际渲染宽高，避免 CSS 最小尺寸、自动高度或内容扩展造成连线偏移。
- 归纳卡在手动调宽后也会重新计算自动高度，长正文缩窄时不再越过卡片底部。
- 快速归纳增加轻量空位搜索，仍优先放在选区下方，但会避开已经存在的题目卡和归纳卡。
- 修复智能整理使用固定列距和通道宽度造成宽卡重叠；网格及按主题、状态、类型整理均改为按实际单元宽高排布。
- 原位编辑在保存、失焦或 Esc 取消时主动清理键盘监听，避免长时间反复编辑后积累无效事件。
- 工作区 schema 保持 v6；新增 v8.4.30.13 专项回归测试与 Chromium 极端宽卡、归纳避让、尺寸一致性验证。

## v8.4.30.12-alpha

- 修复完整/紧凑切换和高亮局部刷新后 `record.node` 与 Canvas Kernel `record.layout` 引用分离，导致卡片移动而关系线停留在旧位置的问题。
- 关系几何增加实时 DOM 布局兜底，单卡、批量和分组拖动期间持续更新，释放后保持正确连接。
- 修复完整模式自动高度写入后未真正应用到卡片的问题；高度测量同时参考标题栏、正文滚动高度，并在布局稳定后复测。
- 卡片右侧新增轻量宽度拖动条；宽度可手动调整并持久化、撤销/重做，完整模式高度继续自动适配，避免用户缩小高度造成内容裁切。
- 框选后生成的归纳卡默认放置在所选卡片正下方并水平居中；下方空间不足时才回退到上方。
- 修复归纳卡标题双击被标题栏拖动手势截获；保留双击编辑，并新增铅笔图标作为明确的单击编辑入口。
- 题目卡的解析、定位、移除、完整/紧凑切换，以及归纳卡编辑操作改为 SVG 图标，保留悬停说明和无障碍标签。
- 工作区 schema 保持 v6；新增 v8.4.30.12 专项回归测试与 Chromium 真实鼠标交互验证。

## v8.4.30.11-alpha

- 解析面板恢复直接展示题目解析、正确答案、判断主线等正文；仅“显示内容”配置默认收起。
- “显示内容”入口移至解析标题栏并只显示调节图标，勾选项变化时局部刷新正文，不重建或关闭解析面板。
- 修复真实鼠标单击关系线时被画布框选手势吞掉、快捷菜单无法显示的问题。
- 关系线、关系菜单、关系文字编辑器和解析面板均从框选/平移手势命中范围中排除。
- 双解析面板定位增加上、下候选方向，并避让其他题目卡，防止第一个解析面板遮挡第二张卡的“解”按钮。
- 关系菜单修改样式后保留原点击锚点；关系颜色改为确认后一次性写入，避免连续生成撤销记录。
- 工作区 schema 保持 v6；新增 v8.4.30.11 专项回归测试和 Chromium 真实鼠标交互验证。

## v8.4.30.10-alpha

- 修复多题题目卡正确答案按钮在鼠标停留时仍被 hover 样式覆盖的问题；点击正确选项后立即显示绿色，再次点击恢复默认。
- “完整 / 紧凑”改为只刷新当前题目卡的正文、按钮状态和自动高度，不再重建全部卡片 DOM；其他题目卡、解析面板、选择状态和关系线保持稳定。
- 解析内容与“显示内容”配置默认收起；关闭按钮改为居中的 SVG 图标，并允许同时打开两个解析面板，打开第三个时自动关闭最早打开的面板。
- 归纳生成的来源关系默认不显示文字；旧版自动生成的“归纳”标签会在 schema v6 迁移时清除，用户手工关系文字保持不变。
- 关系线改为稳定 DOM 增量更新：拖动、平移和缩放时只更新 SVG 几何，不反复销毁重建路径和标签，减少闪烁与偏移。
- 单击关系线新增轻量菜单，可设置实线 / 虚线 / 点线、直线 / 折线 / 曲线、颜色、添加或清除文字，以及删除关系。空文字关系不渲染标签背景。
- 工作区 schema 由 v5 升级至 v6，新增关系颜色、线型和路径样式字段，并保持旧工作区兼容。
- 新增 v8.4.30.10 专项回归测试与 Chromium 内存页面交互验证。

## v8.4.30.9-alpha

- 评估并取消多题题目卡“学”跳转，以及双击题目卡和解析面板中的深学入口；多题画布不再维护脆弱的跨页题目上下文拼装，顶部独立“单题深学”入口继续保留。
- “解”解析面板新增题目解析、正确答案、判断主线、知识点、关键词和选项提示六类勾选项；默认精简显示，选择按用户保存。
- 解析面板增加指向所属题目卡的小三角，并根据当前可视区域在卡片左右自动定位；长内容限制在面板内部滚动。
- 修复 A/B/C/D 按钮与选项文字列宽不一致；取消 hover 上浮、正确外发光和错误左右抖动，只保留红/绿颜色反馈。
- 悬浮栏“归纳”生成归纳卡后，自动为全部来源题目建立指向归纳卡的“归纳”关系线，并作为一次操作接入撤销/重做。
- 分组右上角折叠、适配、编辑和解散操作全部改为 SVG 图标，修复文字按钮错位。
- 修复分组标题双击编辑被拖动手势截获的问题。
- 分组拖动改为“先点击激活、后整体拖动”；取消旧顶部/边框智能拖动热区，激活后可从容器空白区域或成员卡标题拖动。
- 工作区 schema 保持 v5；新增 v8.4.30.9 专项回归测试和 Chromium 内存页面交互验证。

## v8.4.30.8-alpha

- 修复多题题目卡“进入深学”无响应：按钮轻量化为“学”，跳转前同时保存当前发布试卷、题目与题库上下文，并在 URL 中携带 `paper / question / bank / workspace / source` 参数；训练页新增路由恢复兜底。
- 新增题目解析按钮“解”：点击后在题目卡相邻一侧打开解析伴随面板，再次点击关闭；面板不改变卡片高度、不写入画布布局，并会随缩放、平移和卡片移动重新定位。
- 题目选项 A/B/C/D 改为独立字母按钮：错误答案仅在字母按钮处短暂闪红后恢复，正确答案保持绿色，再次点击可取消；不会改变选项正文、高亮或正式深学进度。
- 多选题目卡后在选区上方显示轻量悬浮工具栏，提供“成组、归纳、颜色”入口；悬浮栏会跟随选区、缩放和平移定位，并在拖动期间自动隐藏。
- 点击“成组”可立即创建默认分组，不再先打开弹窗；新分组名称自动进入原位编辑，后续双击名称也可直接修改。
- 修复分组容器拖动热区被卡片层拦截的问题；现在可从标题空白处或四周边框拖动整个容器，成员卡片保持相对位置，并继续支持撤销、重做和持久化。
- 点击“归纳”可根据当前多选题目创建可追溯的结构化归纳卡，记录来源题目 ID；标题和正文支持双击原位编辑，避免频繁打开模态框。
- “颜色”支持直接调整当前共同分组或所选卡片的强调色，操作接入统一历史记录。
- 工作区 schema 升级为 v5，向后兼容 v4；归纳卡新增 `sourceNodeIds / autoGenerated`，题目卡新增可持久化强调色。
- 新增 v8.4.30.8 专项静态回归测试与真实 Chromium 内存页面交互验证。

## v8.4.30.7-alpha

- 多题画布新增原则卡、套路卡、陷阱卡和笔记卡，支持标题、正文、标签、颜色与“待验证 / 已验证 / 已掌握”状态，并可继续编辑或删除。
- 新增题目分组容器：框选至少两张卡片后可建组，支持命名、改色、折叠/展开、整体移动、适配成员范围、解散，以及通过卡片落点自动拖入或拖出。
- 新增有向关系连线：恰好选择两张卡片即可建立同类、对比、因果、例外、易混淆、支持或自定义关系；支持点击连线编辑、改类型、改文字和删除。
- 新增轻量布局诊断，只在拖动、缩放、字号变化、排列和内容变化结束后检查重叠、越界、异常大卡与分组溢出，不进行实时物理推动。
- 新增“一键解决重叠”“修复越界”“适配分组”和逐项定位；重叠检测使用空间分桶，只比较附近卡片，降低大画布计算量。
- 新增一键智能整理，支持整齐网格、按主题、按状态和按卡片类型分列；分组作为整体单元移动，不拆散用户已有结构。
- 结构操作接入统一撤销/重做与批量持久化；工作区 schema 升级为 v4，旧版 v3 数据会自动补齐归纳卡、关系线和分组字段。
- 修复归纳卡首次接入 Canvas Kernel 时卡片类型与策略白名单不匹配，导致归纳卡无法参与多选和关系连线的问题。
- 修复中等宽度桌面下新增结构工具栏与右侧视图控制重叠，以及无布局问题时仍显示红色“0”角标的细节问题。
- 新增 v8.4.30.7 数据结构回归测试和真实 Chromium CDP 内存页面交互验证。

## v8.4.30.6-alpha

- 多题题目卡“完整模式”改为按题干和全部选项自动增长高度，移除卡片内部纵向滚动条；字号变化、宽度统一和重新渲染后都会重新计算所需高度。
- 完整卡与紧凑卡继续保留独立显示模式；紧凑模式保持固定高度和摘要展示，完整模式保证正文与选项全部可见。
- 修复排列工具的交互和持久化不一致：选择排列方式后立即执行，不再需要二次点击“应用”。
- 排列前刷新多选记录，排列后保留选择；若卡片已经处于目标状态，会给出明确提示而非表现为无响应。
- 统一 Canvas Kernel 与 CanvasWorkspaceStore 的宽高约束，并将存储层归一化后的最终布局回写画布，避免统一尺寸后刷新恢复或看似失效。
- 统一宽度后自动重算完整卡高度；统一高度/宽高时以所有所选卡片可完整显示内容所需的最大高度为准。
- 移除多题卡片上的“第 N 步”标记；该标记原本仅镜像单题五步学习进度，不参与多题归纳逻辑。
- 新增 v8.4.30.6 专项回归测试和 Chromium DOM 交互验证。

## v8.4.30.5-alpha

- 将多题画布的框选、多选状态、组拖动、对齐、等距分布和统一尺寸下沉到 Canvas Kernel 的共享 Selection Controller。
- 新增通用 Canvas History Controller，为单卡移动、多卡整体移动及排列操作提供布局撤销/重做。
- 多题画布新增顶部布局工具栏，支持左/中/右、顶/中/底对齐，横向/纵向等距，以及统一宽度、高度和宽高。
- 新增 `Ctrl/Command+Z` 撤销、`Ctrl/Command+Shift+Z` 或 `Ctrl/Command+Y` 重做。
- CanvasWorkspaceStore 新增批量布局写入，排列、组拖动及历史恢复只写入一次工作区，减少重复存储和事件抖动。
- 新增可配置认证提供方：默认保留 `local-demo`，正式部署可切换 `remote`，通过 HTTPS 后端接口登录、注册、退出和恢复会话。
- 远程认证令牌仅保存在 `sessionStorage`；默认推荐使用后端 HttpOnly Cookie，不再要求页面持久保存令牌。
- 所有认证页面统一加载运行时认证配置文件，便于部署时集中切换认证模式。

## v8.4.30.4-alpha - 多题可读性、登录与框选修复

- 多题页面默认使用舒适字号，并提供右上角 Aa 三档字号切换，按用户保存。
- 放大题目列表、试卷选择器、画布标题、题目卡正文、按钮和错误提示等文字。
- 新增独立页面认证控制器，修复点击“登录”无法打开登录框，并支持登录、注册、退出和微信登录回调。
- 登录或退出后自动切换到对应用户的多题画布，避免把访客视口误写入登录用户空间。
- 箭头模式下可在画布空白处拖拽框选题目卡；Ctrl/Command 点击可增减选择。
- 拖动任一已选题目卡可整体移动，选择数量会显示在顶部。

## v8.4.30.3-alpha - 发布试卷数据源统一与高亮切换修复

- 单题、多题和题库管理现在统一读取同一用户作用域下的已发布试卷。
- 修复多题页面因旧认证函数缺失而无法读取发布试卷的问题。
- 前端明确显示已组题数、目标题数和实际可用题数。
- 单题题目抽屉新增发布试卷选择器；无发布试卷时保持空列表。
- 题库管理在发布前检查失效和重复题目引用。
- 多题已有高亮再次点击同色即可清除，无需单独删除按钮。

## v8.4.30.2-alpha - 画布交互、发布试卷与高亮稳定性修复

- 单题/多题画布补齐与知识图谱一致的箭头/手型切换、V 快捷键、空格临时平移和鼠标右键临时平移。
- 多题画布题目库只读取已发布试卷，可切换试卷；没有已发布试卷时前端不展示后台题库题目。
- 多题画布功能栏、高亮栏和关键操作文字整体放大。
- 文字高亮改为仅刷新受影响文字区域，高亮样式不再改变文字宽度，避免新增高亮时排版闪烁。
- 高亮菜单展开箭头改为向右，主高亮按钮会记住用户最近选择的颜色。

## v8.4.30.1-alpha - 多题画布题目列表与完整题目卡修复

本修复版解决多题画布题目库可能显示空列表的问题，并恢复完整题目卡、完整/紧凑切换及工作区独立文字高亮。题目列表现在具备题库读取兜底、错误提示、手动重试和登录状态恢复后的自动同步。

## v8.4.22 - 文件卡片布局与操作状态修复

- 标签入口改为卡片右下角圆点；已打标签显示对应颜色，未打标签仅在悬浮或选中时显示空心圆。
- 标签菜单取消复选框，选择“不打标签”即可清除标签。
- 创建标签改为悬浮或点击展开的二级菜单，创建后立即应用。

## v8.4.1 - 标签即收藏轻量化版

文件管理页新增文件／文件夹收藏、收藏视图、标签创建与颜色管理、标签筛选，以及右键、详情和批量标签操作。收藏和标签保存在轻量索引中，不会增加图谱正文读取。相关交互已拆分为独立组织模块。

## v8.3.5 - 账号菜单层级修复

- 修复文件管理页右上角账号菜单被文件卡片和缩略图遮挡的问题。
- 为顶部工具栏建立明确堆叠层级，使账号菜单始终位于文件内容之上。
- 顶部栏仍低于右键菜单、详情抽屉、模态框和处理中遮罩，不影响其他浮层。

## v8.3.4 - 文件管理稳定性与性能补丁

本版本减少文件管理页的重复完整性扫描，修复多选右键与失效选择问题，并增强非法拖放反馈、二级菜单键盘操作和当前图谱初始定位。

## v8.3.3 - 可见多选与移动菜单补丁

- 右键菜单的“移动到”区域明确增加“选择其他位置…”入口。
- 最近目标文件夹最多显示 4 个，不会随文件夹数量无限扩展。
- 顶部增加“选择”按钮，进入多选模式后每个项目显示可点击复选标记。
- 批量工具栏支持移动、导出、移入回收站和取消选择。
- 保留 Ctrl/Command 与 Shift 多选快捷操作。
- 批量删除增加父子层级去重和失败回滚。

## v8.3.1 - 轻量移动与批量操作

- 文件与文件夹可直接拖拽到目标文件夹、面包屑或左侧“我的文件”。
- Ctrl/Command 点击和 Shift 点击可进行多选，拖动任一已选项目即可批量移动。
- 右键“移动到”下方显示根目录和最近使用的目标文件夹；“选择其他位置”继续提供完整目录树。
- 移动后显示轻提示并可在 5 秒内撤销。
- 批量移动失败时自动回滚已完成的项目。
- 选中父文件夹及其子文件夹时，只移动父文件夹，避免破坏原有层级。

## v8.3.0 - 文件夹管理

文件管理页现支持多级文件夹、面包屑导航、文件/文件夹移动、文件夹内联重命名，以及文件夹与其内容的回收站恢复。旧图谱没有 `folderId` 时自动视为根目录文件，无需迁移。

## v8.2.8 - 文件管理稳定性补丁

文件名内联编辑现在可安全跨越后台列表刷新，不会在尚未输入完成时被自动提交；存储空间统计也增加了异步防串线保护，账号切换或连续刷新时不会显示旧账号结果。

## v8.2.7 - 内联重命名

文件管理页现在可在文件名原位置直接重命名：Enter 保存、Esc 取消、失焦保存；不再弹出重命名对话框。重命名会同步图谱标题与页签名称，并适用于网格和列表视图。

## v8.2.6 - 手动保存

图谱页面左上角保存图标现可点击立即保存，并支持 Ctrl+S / Command+S。自动保存机制继续保留。

## v8.2.4 - 页签会话状态修复

关闭的图谱页签会按用户保存在当前浏览器会话中。进入独立文件管理页再返回编辑器时，不会重新显示已经关闭的页签；从文件管理器主动打开文件时会自动恢复对应页签。

## v8.2.3 - 文件预览性能优化

- 移除文件详情栏中的重复缩略图。
- 预览仅在图谱结构发生变化时重新生成。
- 文件列表缩略图使用视口懒加载，降低大量文件时的 DOM 和绘制负担。

## v8.2.2 - 文件管理交互与动态图谱缩略图

- 修复文件卡片第一次单击后被重新渲染，导致浏览器无法识别连续双击的问题；现在双击卡片可直接打开图谱。
- 右上角头像升级为可操作账号菜单，提供身份信息、返回编辑器、系统设置、管理员用户管理和登录/退出入口。
- 图谱缩略图改为依据实际节点位置、节点颜色、关系线和核心节点标题生成，不同文件会呈现不同结构。
- v2 文件索引新增轻量 `preview` 元数据；新建与保存时自动更新，旧文件进入文件管理页后会在空闲时一次性补齐。
- 缩略图最多保存 18 个节点和 30 条关系，只渲染轻量 SVG，不在文件列表中反复加载完整图谱正文。
- 优化深色和浅色主题下的缩略图层次、标签、节点光晕、连线和悬浮效果。

## v8.2.1 - 文件管理回归修复

- 修复 100 字符文件名发生重名时无法正确追加序号的问题；批量导入同名长标题不再陷入无限循环。
- 修复未打开的导入文件和副本也被列入“最近打开”的问题；仅实际打开或设为当前文件时记录打开时间。
- 修复内容缺失的回收站文件恢复后被错误移回正常文件列表的问题；恢复前会验证内容并保持事务一致。
- 修复双击或用键盘操作卡片“更多”按钮时意外打开图谱的问题。
- 文件批量导入期间增加忙碌遮罩和重复操作保护，并减少逐文件重复刷新。
- 登录账号在当前页或其他标签页发生变化时，文件管理页会同步切换用户空间和身份显示。
- 完善图谱内容缺失时的错误提示，避免显示上一次操作遗留的错误信息。

## v8.2.0 - 文件管理功能第一阶段

- 新增独立页面 `file-manager.html`，文件管理与图谱编辑器解耦，方便后续持续扩展。
- 程序页签栏最左侧房子按钮改为文件管理入口；进入前会保存当前图谱，保存失败则取消跳转。
- 文件管理页提供最近打开、我的文件和回收站三个入口。
- 支持网格/列表视图、关键词搜索以及按更新时间、最近打开、名称、大小和创建时间排序。
- 支持新建、打开、重命名、创建副本、导入 ZIP/JSON、导出学习包和移入回收站。
- 回收站支持恢复、永久删除和清空，沿用 v2 存储层 30 天自动清理策略。
- 文件卡片和详情栏显示更新时间、节点数、关系数、文件大小、标签及当前打开状态。
- 左侧展示图谱文件库大小、回收站大小、节点/关系总数和浏览器存储配额估算。
- 文件列表只读取 v2 轻量索引；仅在打开或导出时读取完整图谱正文。
- 支持浅色/深色主题切换并记住用户选择，同时提供桌面端与移动端响应式布局。
- 导入继续执行 ZIP 安全限制，并额外验证 `nodes` / `links` 结构和节点、关系数量上限。

# 通用知识点关系图谱工具｜模块化开发版

这是从上传的单文件 HTML 拆分出来的低风险模块化版本。


## v7.9.6 维护优化记录

- 将首页“搜索定位”按钮的事件绑定迁回 `src/10-graph-editor.js`，避免 `src/20-flashcards-toolbar.js` 与图谱搜索产生隐式耦合。
- 为图谱搜索增加轻量缓存索引，节点字段只在索引失效后统一拼接和转小写，降低大图谱连续输入搜索时的重复计算。
- 合并左上角图谱信息悬浮栏相关 CSS，移除 v7.9.5 追加式覆盖块，降低后续样式维护成本。
- 保持 v7.9.5 的界面行为：图谱信息栏只显示图谱名称，搜索入口在图谱信息栏内，详情窗口不显示尺寸字段。


## v7.9.7 用户偏好与高性能模式

- 首页左上角图谱名称悬浮栏新增“偏好设置”入口。
- 新增浏览器级用户偏好存储 `kg_graph_user_preferences_v1`，不会写入图谱文件或学习包。
- 支持“精致 / 平衡 / 高性能 / 自定义”视觉模式。
- 支持单独关闭阴影、玻璃模糊、动画过渡、关系线标签和卡牌渐变背景。
- 高性能模式通过根节点 `kg-perf-*` class 统一覆盖 CSS，避免逐节点修改 DOM，适合大图谱和低性能设备。


## v7.9.8 多选拖动连接线延迟刷新

- 针对多选卡牌拖动卡顿，新增“拖动时延迟刷新连接线”偏好项。
- 多选拖动过程中默认只移动卡牌，暂停 SVG 关系线 path/text 的实时几何刷新。
- 松手后统一刷新一次关系线，避免 50 张左右图谱中关系线密集时拖动卡顿。
- 拖动中会临时淡化连接线，减少连接线停留在旧位置带来的视觉干扰。




## v7.9.10 手动大图模式开关

- 偏好设置中的“大图模式”改为真正的手动开关，默认开启。
- 开启后，任何数量卡牌的图谱都会使用大图渲染策略。
- 关闭后，当前图谱按普通图谱模式显示，关系线较多时可能变卡。
- 切换开关后会立即重新渲染首页图谱，无需刷新页面。


## v7.9.11 大图模式选中标识与框选删除

- 修复大图模式下卡牌选中状态不明显的问题，单选和框选都会显示外框与“已选”标识。
- 在大图模式的局部关系/只看相关状态下，已选卡牌会保持可见。
- Delete / Backspace 现在支持删除框选的多个知识点，并同步删除相关关系线。


## v7.9.12 快捷操作增强

- 支持 Ctrl/Command + 点击卡牌进行复选，再次点击已选卡牌可取消选择。
- 支持 Ctrl/Command + C 复制当前选中的卡牌到应用内部剪贴板。
- 支持 Ctrl/Command + V 在鼠标所在画布位置粘贴卡牌；只复制卡牌本身，不复制关系线。
- 支持 Ctrl/Command + Z 撤回删除和粘贴等快捷操作，主要用于误删恢复。
- 删除单张卡牌、框选批量删除、删除关系线和粘贴卡牌都会写入撤回栈。


## v7.9.13 箭头编辑模式 / 手型演示模式

- 默认进入箭头编辑模式：可点击卡牌和关系线、拖拽卡牌、框选、复制粘贴、编辑弹窗；左键不再拖动画布。
- 按住空格键可临时进入手型演示模式，松开后恢复箭头编辑模式。
- 在图谱区域按住鼠标右键可临时进入手型演示模式，松开后恢复，并禁用图谱区域默认右键菜单。
- 工具栏“居中”按钮右侧新增箭头/手型切换按钮，可长期切换编辑/演示模式。
- 手型模式下卡牌和关系线不触发选择、拖拽、编辑等操作，只用于平移浏览画布。


## v7.9.14 编辑模式直接框选

- 编辑模式下，空白画布区域按住鼠标左键拖动即可框选卡牌，不再需要按住 Shift。
- 单击空白画布仍会清空选择并关闭详情。
- 手型演示模式保持不变，仍用于拖动画布浏览。


## v7.9.15 选中状态圆点标识

- 已选卡牌不再显示“已选”文字。
- 单选和多选卡牌右上角改为显示卡牌自身颜色的小圆点。
- 大图模式和普通模式保持一致的轻量选中标识。


## v7.9.16 连线起点提示位置优化

- 双击卡牌设为连线起点后，“连线起点”提示移到卡牌外部上方，避免遮挡卡牌内容。
- 提示文字保持单行显示，并根据卡牌宽度自动缩小字号。
- 小卡、默认卡和大卡分别做了上方间距微调。


## v7.9.17 连线选中状态与快捷线型

- 单击关系线后，选中关系线会显示更明显的高亮状态。
- 选中关系线旁边新增“实线 / 虚线”快捷按钮。
- 可直接在线旁切换线型，无需回到左侧工具栏。
- 画布平移、缩放或关系线端点移动时，快捷按钮会跟随关系线位置更新。


## v7.9.18 连线快捷按钮位置与图标优化

- 关系线快捷线型按钮改为纯图标，不再显示“实线 / 虚线”文字。
- 快捷按钮显示在关系线旁边，并尽量靠近鼠标点击位置。
- 画布缩放和平移后，快捷按钮仍会跟随点击处所在的关系线位置。


## v7.9.19 双击连线就地编辑文字

- 双击关系线可在线旁直接输入 / 修改线上文字。
- Enter 保存，Esc 取消，输入框失焦自动保存。
- 输入为空并保存时，会清空线上文字。
- 修改前会写入撤回栈，可通过 Ctrl / Command + Z 撤回误改。
- Alt + 双击关系线保留原完整关系线编辑弹窗入口。


## v7.9.20 大图模式连线文字编辑修复

- 修复大图模式下双击可见连线无法弹出线上文字输入框的问题。
- 大图模式下已渲染的主干线 / 骨架线现在可单击选中、双击就地编辑文字。
- 保持手型演示模式规则不变：手型模式下连线仍不触发编辑。


## v7.9.21 普通模式关系文字按需显示

- 普通模式下，关系线文字默认不再全部显示，减少大量 SVG 文本渲染带来的卡顿。
- 单击关系线时，该关系线文字会显示。
- 单击卡牌或框选卡牌时，与所选卡牌相连的关系文字会显示。
- 大图模式保持原有按局部关系显示文字的策略。


## v7.9.22 工具栏快捷键梳理

- V：切换箭头编辑模式 / 手型演示模式。
- N：新增知识点。
- T：打开模板。
- F：重点聚焦。
- K：记忆闪卡。
- Q：考题训练。
- 1 / 2：小卡 / 大卡。
- S / D：实线 / 虚线。
- C：打开线条 / 卡牌颜色选择器。
- - / +：缩小 / 放大。
- 0：居中显示。
- L：心流模式。
- G：主干线 / 骨架线。
- R：只看相关。
- 工具栏按钮的鼠标悬浮提示会自动追加对应快捷键；输入框、弹窗和文字编辑状态下不会触发这些快捷键。


## v7.9.23 工具提示简化与高风险快捷键移除

- 工具栏悬浮提示统一简化为“功能 + 快捷键”，例如“新增 N”“选择/手型 V”。
- 取消导出、导入、清空图谱的单键快捷键，降低误触风险。
- 导出、导入、清空图谱仍保留工具栏按钮操作。
- 其余常用编辑和视图快捷键保持不变。


## v7.9.24 工具栏提示清理

- 修复工具栏按钮悬浮时同时出现黑色自定义提示和浏览器原生 title 提示的问题。
- 心流、主干线、只看相关三个动态按钮的提示改为简短格式：`心流 L`、`主干线 G`、`相关 R`。
- 选择 / 手型按钮保持简短提示：`选择/手型 V`。
- 导入、导出、清空仍不绑定快捷键，降低误触风险。


## v7.9.25 居中选中卡牌

- 点击工具栏“居中”按钮或按 `0` 时，若当前选中了卡牌，会优先将选中卡牌居中并缩放到合适大小。
- 多选 / 框选多张卡牌时，会以选中卡牌组的整体范围居中并适配缩放。
- 未选中卡牌时，保持原有全画布居中逻辑。


## v7.9.26 居中视角平滑过渡

- 点击“居中”或按 `0` 时，不再瞬间跳转视角。
- 会从当前视角平滑过渡到目标视角，并包含缩放过程。
- 选中单张 / 多张卡牌居中、全画布居中、空画布居中均使用同一套平滑过渡。
- 若系统开启“减少动态效果”，会自动退回即时定位。


## v7.9.27 大图模式悬浮详情

- 大图模式下，鼠标悬浮卡牌约 650ms 后会自动显示详情窗口。
- 鼠标快速划过卡牌不会触发详情，减少大图模式下频繁重绘。
- 若开启主干线 / 骨架线，悬浮卡牌仍会保留局部关系预览。
- 拖拽卡牌、框选、手型 / 临时手型、居中视角动画、线上文字编辑时，不触发悬浮详情。


## v7.9.28 聚焦画面柔和过渡

- 重点聚焦、心流模式、只看相关、主干线 / 骨架线切换时，卡牌明暗、关系线透明度和画布背景会缓起缓停。
- 重点聚焦切换不再重建卡牌 DOM，改为保留现有卡牌并做状态过渡，视觉更柔和。
- 过渡期间关系线会做轻量淡入，减少突然闪变。
- 若系统或性能偏好开启“减少动画”，会自动禁用这些过渡。


## v7.9.29 图谱页签拖拽排序

- 首页顶部图谱页签支持鼠标拖拽排序。
- 拖拽页签到目标页签左侧 / 右侧后松手，即可调整顺序。
- 排序结果会持久化保存，刷新页面或切换图谱后仍保持。
- 拖拽关闭按钮不会触发排序；拖拽结束后不会误触打开页签。
- 新建图谱默认追加到当前页签列表末尾。


## v7.9.30 图谱页签拖拽让位动画

- 优化图谱页签拖拽排序体验。
- 拖拽经过目标页签时，相邻页签会向左 / 向右轻微让位，形成“被挤过去”的反馈。
- 插入位置蓝色竖线仍保留，同时目标页签会有轻微高亮。
- 拖拽结束后自动清理动画状态，不影响点击切换页签。


## v7.9.31 图谱标题原处编辑与文件信息

- 首页左上角图谱名称改为单击后直接在原位置编辑，不再弹窗。
- 标题输入 Enter / 失焦保存，Esc 取消。
- 双击图谱名称仍打开弹窗，但弹窗仅保留“标题”可编辑。
- 弹窗新增只读信息：创建时间、最后更新、文件大小、知识点数量、文件字数。


## v7.9.32 超大图谱完整适配视野

- 修复超大图谱（例如 1000 张卡）在“居中 / 全画布适配”后仍有卡牌超出屏幕的问题。
- 主画布最小缩放从原来的约 0.2 降到 0.035，允许超大图继续缩小到完整视野。
- 全画布居中时增加更舒适的屏幕留白，避免卡牌贴边。
- 滚轮缩放、工具栏缩放按钮、快捷键 `0` 会使用同一套超大图缩放范围。
- 兼容旧的 `fitBoundsToView(options)` 调用，避免相关卡牌聚拢适配被同名全局函数覆盖影响。


## v7.9.33 左下角缩放与全屏控制

- 将放大 / 缩小按钮从左侧浮动工具栏移到画布左下角。
- 左下角缩放条包含：缩小、实时缩放百分比、放大、全屏。
- 缩放百分比会随滚轮、按钮、居中适配、动画过渡实时更新。
- 点击百分比会将画布缩放恢复到 100%。
- 悬浮 / 聚焦百分比区域时会弹出滑轨，可拖动滑轨控制缩放比例。
- 新增画布全屏按钮，可进入 / 退出当前画布全屏。


## v7.9.34 缩放栏交互与更新日志

- 新增独立更新日志文件 `CHANGELOG.md`，后续版本优化会同步记录。
- 左下角缩放滑轨默认隐藏，点击缩放百分比后出现。
- 滑轨不再因鼠标悬浮 / 移开而自动显示或隐藏，方便连续拖动调整。
- 缩放功能栏更靠近画布左下边缘，释放画布空间。
- 消息提示移动到缩放功能栏右侧，避免与缩放按钮重叠。


## v7.9.35 关系线路径形态与圆弧快捷菜单

- 关系线新增路径形态：曲线、直线、折线。
- 旧图谱没有 `pathStyle` 字段时，默认仍按曲线显示，保持兼容。
- 点击关系线出现的快捷菜单改为圆弧形：上半部为实线 / 虚线，下半部为直线 / 折线 / 曲线。
- 快捷菜单出现时增加弹出缩放动效。
- 临时相关画布也会按关系线的路径形态显示。


## v7.9.36 关系线圆弧菜单轻量化

- 去掉关系线快捷菜单的背景圆盘，仅保留五个样式按钮。
- 上半部实线 / 虚线按钮改为圆弧排布，不再水平并排。
- 下半部直线 / 折线 / 曲线继续保持圆弧排布，并微调间距。
- 保留菜单弹出缩放动效和当前样式高亮。


## v7.9.37 关系线圆弧菜单加入文字与颜色

- 关系线圆弧菜单新增 `T` 按钮，点击后打开关系文字就地编辑框。
- 新增颜色圆点按钮，点击后可快速修改当前关系线颜色。
- 实线 / 虚线 / T / 颜色 / 直线 / 折线 / 曲线共七个按钮统一沿圆弧排布。
- 文字编辑不再主要依赖双击连线，入口更稳定。


## v7.9.38 关系线菜单对称布局与双虚线

- 关系线快捷菜单调整为更对称的圆弧排布。
- 顶部线型改为三颗：长虚线、实线、短虚线，实线位于中间。
- 中部左右分别为 `T` 文字编辑和实时颜色圆点。
- 颜色圆点改为菜单内原生色盘输入，点击后色盘出现在菜单按钮旁，不再跳回左侧工具栏。
- 底部继续保留直线、折线、曲线路径形态。


## v7.9.39 关系线菜单正圆排布与虚线直角化

- 修复关系线快捷菜单看起来偏椭圆的问题，八个按钮改为同一半径的正圆排布。
- `T` 文字按钮增加浅蓝底色，颜色按钮直接使用当前关系线颜色，避免左右两侧按钮看起来像空白按钮。
- 长虚线 / 短虚线的小段端点改为直角，不再使用圆角端点，视觉更简洁，也减少额外绘制成本。

## 如何运行

方式一：直接打开 `index.html`。  
方式二：用本地服务器运行，更接近正式开发环境：

```bash
python3 serve.py
```

然后打开：

```text
http://127.0.0.1:8000/index.html
```

## 目录结构

```text
kg_graph_modularized/
  index.html
  question-training.html
  knowledge-recall.html
  question-bank.html
  user-management.html
  system-settings.html
  serve.py
  styles/
    main.css
    global-shortcuts.css
    question-bank-admin.css
    question-training.css
    knowledge-recall.css
    user-management.css
    user-center.css
    account-menu.css
    home-file-library.css
    system-settings.css
    subscription.css
  src/
    29-auth-core.js
    30-auth-guards.js
    33-user-center.js
    34-role-permissions.js
    35-user-management-service.js
    35-user-management.js
    36-system-settings.js
    37-subscription-plans.js
    37-subscription-orders.js
    37-subscription-redeem-codes.js
    37-subscription-core.js
    39-global-shortcuts.js
    41-account-menu.js
    ...
  docs/
    MODULE_MAP.md
    MODULE_AUDIT.md
    AUTH_CORE_REFACTOR.md
    USER_MANAGEMENT_SERVICE.md
    BASELINE_CLEANUP.md
    SCRIPT_LOAD_ORDER.md
    SUBSCRIPTION_MODEL.md
    BASELINE_REFACTOR_A.md
    BASELINE_REFACTOR_B.md
    BASELINE_REFACTOR_C.md
```


## 脚本加载基线

当前活跃页面的本地脚本已统一使用 `defer`。加载顺序遵循：

```text
认证核心 KGAuthCore
→ 角色权限 KGRolePermissions
→ 订阅基础层（plans / orders / redeem-codes / core）并汇总为 KGSubscription
→ 全局组件（微信登录 / 用户中心 / 全局快捷栏）
→ 页面业务脚本
```

首页中 `src/30-auth-guards.js` 需要包装图谱和工具栏函数，所以仍保留在图谱编辑与工具栏脚本之后、启动入口 `src/90-bootstrap.js` 之前。

## 订阅模型基线

第七步后，学员订阅套餐已统一为：

```text
free / monthly / quarterly / half_year / lifetime
```

旧版占位套餐 `free_student/basic_student/pro_student` 会通过订阅基础层自动兼容映射。订阅仍只对学员 `student` 生效，管理员和教师/教研绕过订阅限制，游客只保留公开示例体验。详细说明见 `docs/SUBSCRIPTION_MODEL.md`。

## 本次拆分做了什么

- 保留原 HTML 结构，不重写页面 DOM，降低功能回归风险。
- 从 `<style>` 拆出 `styles/main.css`。
- 从原来的单个巨大 `<script>` 拆成多个按功能命名的 JS 文件。
- 删除外链 Cloudflare beacon 统计脚本，避免重复加载和离线不可用问题。
- 把原来依赖 IIFE 函数提升的启动语句移动到 `src/90-bootstrap.js`。
- 历史单文件与临时解析文件已从当前整理版中移除，避免误维护。


## 本次界面调整

- 将原顶部操作按钮迁移为屏幕左侧悬浮图标菜单。
- 悬浮菜单可通过顶部手柄拖拽，位置保存在浏览器本地。
- 顶部保留图谱信息、升级会员和账号胶囊；使用说明、用户中心、登录/退出集中到账号下拉菜单。
- 登录后双击顶部图谱信息文字即可编辑；移动端仍保留底部操作栏。
- 所有原按钮 ID、数据结构和业务功能保持兼容。


## 基线重构 C / C-1 / C-1.1

首页学习包文件处理已拆到 `src/21-home-package-service.js`，早期快照文件库服务位于 `src/22-home-file-library.js`。

当前版本继续新增：

- `src/23-graph-file-store.js`：正式图谱文件库服务层。
- `src/24-graph-file-autosave.js`：3 分钟 dirty 自动保存、切换与离开前保存。
- `src/25-graph-file-tabs.js`：固定宽度文件页签、新建与切换交互。
- `styles/graph-file-tabs.css`：对标式双层顶部布局。

旧单图谱 localStorage key 继续保留并镜像写入；首次进入用户空间时会迁移为第一个图谱文件。

C-1.1 修复版进一步修复了存储失败误报成功、保存失败仍切换/退出、标题与文件名不同步、旧悬浮工具栏位置重叠、文件数量上限截断等问题。详细说明见 `docs/BASELINE_REFACTOR_C1_1_FIX.md`。

C-1.4 将画布顶部左右信息模块改为直接悬浮定位，移除横跨画布的整行背景容器。详细说明见 `docs/BASELINE_REFACTOR_C1_4.md`。

C-1.4.2 修复了解压到新目录后自动新手引导遮罩拦截左右悬浮模块点击的问题，并为悬浮模块增加显式画布事件隔离。详细说明见 `docs/BASELINE_REFACTOR_C1_4_2.md`。

## 下一步建议

当前版本是“安全拆分版”。后续可以继续做更彻底的工程化：

1. 把 `state`、`storage`、`sanitize` 抽成显式 API。
2. 把 graph / flashcard / question-bank 改成独立模块对象。
3. 建立统一事件总线，减少跨文件直接改全局状态。
4. 用 Vite 或 Parcel 管理构建，最终输出单文件发布版。
5. 增加 Playwright 冒烟测试，覆盖新增节点、导入导出、闪卡、题库管理。

## 深度知识回忆页面

- 新增 `knowledge-recall.html`，用于从考题进入独立的“知识寻宝地图”。
- 在考题破案模式的题目页点击“深度回忆”，会把当前题目写入本地缓存并打开独立页面。
- 深度回忆页以完整题目和选项为中心卡片，点击题目/选项中的关键词后，会在关键词附近生成三维圆形探索按钮。
- 圆形按钮显示对应知识点的首字，点击按钮后弹出引导式提问；选择能回忆到的知识点后，新按钮会以动画方式生成，并与父按钮自动连线。
- 画布支持拖拽、滚轮缩放、双击回到中心题目；每次生成新节点后会自动居中跟随。
- 预设知识网位于 `src/85-knowledge-recall-data.js`，交互逻辑位于 `src/86-knowledge-recall.js`，样式位于 `styles/knowledge-recall.css`。

### 新增：独立题库认知标注管理页

打开 `question-bank.html` 可进入独立题库管理页面。该页面面向项目管理类认证科目，默认支持 PMP、CSPM、P2、ACP、NPDP，并预留 PgMP、PfMP 等扩展入口。

当前支持：
- 题库列表与题目列表管理
- 题目基础信息、选项、答案与解析维护
- 题干关键词标记，并生成与深度回忆页面兼容的 `stemParts` / `clues`
- 知识点绑定，并生成与训练页兼容的 `concepts`
- 推理步骤维护，支持后续深度回忆和推理链训练扩展
- 设为当前训练题
- 一键打开深度回忆预览
- JSON 导入、导出与模板下载


### 题库管理页本轮增强

- 题库管理页左侧已改为“题库管理 / 题目管理”标签栏，题库选择和题目选择分区更清晰。
- 关键词标记、知识点绑定新增“编辑”按钮，已添加条目可再次修改。
- 关键词来源支持题干与选项，选中题干或选项文本时会出现“标记关键词”小按钮，并可通过悬浮操作台快速保存。

### 题库管理页布局优化

- 题库信息已内聚到左侧“题库管理”栏，中间题目编辑区不再显示题库信息表单。
- “题目管理”栏新增搜索与按章节/领域/难度/题型归集，适合大量题目快速定位。
- 关键词标记、知识点绑定、推理逻辑已整合为“认知标注工作台”标签栏。
- 左侧新增布局导航，可快速跳转题库管理、题目管理、题目基础、关键词、知识点、推理逻辑和完成度。

### 题库管理页体验优化

- 新增“删除题库”入口：题库信息区、右侧当前题库操作区、题库列表项均可删除当前/指定题库。
- 删除内置 PMP 示例题库或示例题后，会记录本地隐藏状态，刷新后不会反复自动恢复。
- 顶部导出按钮改名为“导出当前题库”，并新增“导出全部题库”。
- 右侧“项目管理类科目扩展”已改为“科目快捷管理”，可直接按科目筛选、新建题库或启用 PgMP/PfMP 扩展科目。
- 标注完成度改为可点击工作流：未完成项会显示“去补齐”，点击后自动跳到对应的题目基础、关键词、知识点或推理逻辑区域。
- 题库列表项新增“导出 / 删除”小操作，题库级管理更清晰。

## 组卷与发布（新增）

题库管理独立页面 `question-bank.html` 已新增“组卷与发布”模块，支持从同一科目下的多个题库按知识领域/主题配额抽题，生成综合训练试卷。

典型流程：

1. 在 PMP、CSPM、P2、ACP、NPDP 等科目下维护题库和题目。
2. 进入题库管理页的“组卷发布”。
3. 新建试卷，选择科目，例如 PMP。
4. 设置目标题量，例如 180。
5. 按知识领域/主题填写配额，例如范围、进度、成本、质量等。
6. 点击“按配额组卷”。
7. 检查试卷题目预览。
8. 点击“发布试卷”。
9. 回到首页打开“考题训练”，在“综合试卷”下拉框中选择已发布试卷并开始训练。

试卷数据保存在浏览器本地 localStorage，key 为 `kg_exam_papers_v1__<scope>`；当前选择的试卷保存在 `kg_exam_current_v1__<scope>`。


## 2026-07 题库管理主工作区布局调整

- `question-bank.html`：左侧收敛为纯纵向导航；题库列表、题目列表、组卷发布和题目基础编辑迁移到中间主工作区。
- 中间主工作区新增三个主标签：题库管理、组卷与发布、题目基本信息。
- `styles/question-bank-admin.css`：新增主标签栏、双栏题库/题目管理区和左侧窄导航响应式样式。
- `src/65-question-bank-admin.js`：新增主工作区标签状态 `activeMainTab`，并调整左侧导航跳转逻辑。
- `question-training.html`：独立考题训练页面，替代首页弹窗模式，可选择已发布试卷进行综合训练。

## 2026-07 用户管理系统（新增）

- 新增 `user-management.html`，作为独立用户管理页面。
- 新增 `styles/user-management.css` 与 `src/35-user-management.js`。
- 首页左侧悬浮菜单新增“用户管理”入口；题库管理页、考题训练页也新增跳转入口。
- 当前版本仍基于浏览器 `localStorage`，用于本地多用户原型管理：
  - 用户列表、搜索、角色筛选、状态筛选
  - 新建用户、编辑用户资料、重置密码
  - 设置角色：管理员、教师/教研、学员、只读用户
  - 设置状态：正常、暂停、已归档
  - 用户归档、恢复、删除账号
  - 用户数据概览：图谱节点、关系线、题库、题目、试卷
  - 操作日志：注册、登录、退出、创建、编辑、归档、恢复、导入导出等
  - 用户 JSON 导入、导出
- `src/30-auth-guards.js` 和 `src/72-question-training-page.js` 已同步增强：
  - 注册用户自动补齐角色、状态、科目、资料字段。
  - 登录时更新最近登录与最近活跃时间。
  - 已暂停或已归档账号将无法继续登录。
- `src/30-auth-guards.js` 与 `src/72-question-training-page.js` 已逐步接入 `src/29-auth-core.js`，优先复用统一的用户读取/保存、密码校验、会话和日志能力。

> 注意：这仍是本地浏览器版用户管理，不具备服务器级安全与跨设备同步。正式网络版需接入后端账号、数据库、权限和审计日志。


## 角色权限与主题模块

本版本新增 `src/34-role-permissions.js`，统一管理管理员、教师/教研、学员、只读用户的前端权限模板和角色主题色。教师/教研允许进入题库管理、维护题目、组卷并发布/取消发布试卷；用户管理和角色主题修改仅限管理员。首页与独立训练页会显示当前登录角色，并按角色自动切换主题色。

## 2026-07 角色权限与只读示例题修复

- 修复“只读用户仍可看到题库管理 / 用户管理入口”的问题：首页悬浮工具栏、考题训练页顶部入口、题库管理页顶部入口会按当前角色自动隐藏无权限按钮。
- 只读用户保留“考题训练”入口，但只能完整操作内置 PMP 示例题；其他题目、试卷或深度回忆会被拦截。
- 示例题判断改为同时校验 `bank-pmp-demo` 与 `pmp-agile-change-001`，避免仅凭题目 ID 误判。
- 修复唯一管理员被暂停后可能无法进入用户管理的问题：只有 `active` 状态管理员才算有效管理员。


## 微信扫码登录说明

本版本新增 `src/32-wechat-login.js`，在登录弹窗中提供“微信扫码登录”入口。由于当前项目仍是纯前端 localStorage 架构，微信登录分为两种模式：

- 本地演示扫码：用于原型体验，会创建/登录一个本地微信演示账号。
- 正式微信开放平台：在用户管理页配置 AppID、redirect_uri 和后端 code 换取 openid/unionid 的接口后，可跳转微信开放平台扫码授权页。真实上线时，AppSecret 与 token 交换必须放在后端完成，不能写在前端。

用户管理页新增“微信登录配置”面板，可设置是否启用演示扫码、正式微信模式、首次登录自动创建用户、默认角色和默认科目。

## 本次更新：用户分页与题库/组卷列表体验优化

- 用户管理左侧用户列表新增分页控件，支持上一页/下一页与每页 10/20/50/100 人切换；批量勾选改为“全选当前页”。
- 题库管理页的题库列表新增序号，便于后台核对与沟通。
- 题目列表区域强化为独立可滚动区域，适合 10 题以上、多题库场景；双击题目可直接进入“题目基本信息”标签。
- 组卷与发布页的试卷列表改为横向卡片网格，减少纵向占用，提升多试卷管理体验。

## 订阅功能进度

当前基线已完成订阅功能第八步 UI 基础层：

- 用户中心显示“我的订阅”。
- 用户管理页支持管理员手动开通、续期、停用学员订阅。
- 系统设置页支持编辑订阅套餐展示配置。
- 管理员和教师/教研绕过订阅限制，订阅仅聚焦学员角色；游客不进入订阅体系。

当前仍为纯前端 localStorage 原型，正式收费前必须接入后端支付和订阅校验。


## 订阅第七步补充修复

- 用户中心续费入口会自动关闭用户中心，再打开会员权益弹窗。
- 首页「使用说明」右侧新增「升级会员」入口。
- 会员权益弹窗可作为后续订阅购买流程的统一入口。
- 订阅第七步补充：会员权益弹窗支持点击卡片购买，权益展示文案和每日训练文案可在系统设置中配置。


### 第八步 A：权益限制接入

当前版本已开始把学员订阅权益接入真实功能入口：免费学员首页图谱卡牌上限 50，深度回忆单题知识点上限 30；付费会员不限制这两项。学习包导入/导出不再按次数限制，所有学员均可使用。详见 `docs/SUBSCRIPTION_STEP8A.md`。



## 订阅第八步 B

已新增本地订阅申请 / 管理员确认开通流程，详见 `docs/SUBSCRIPTION_STEP8B.md`。当前仍不接真实支付，适合作为前端原型和内测流程。


### 订阅卡密

- 新增卡密兑换与后台卡密管理说明：`docs/SUBSCRIPTION_CARD_CODES.md`。
- 系统设置中的卡密列表已分页显示，每页 10 条。

## 基线重构 C-1.2

首页右上角登录区域已改为账号胶囊下拉菜单：已登录只显示用户名称，未登录显示“访客只读”；菜单包含用户中心、帮助中心，以及登录或退出登录。原外置“使用说明”和“退出”按钮不再显示。实现文件为 `src/41-account-menu.js` 与 `styles/account-menu.css`，详细说明见 `docs/BASELINE_REFACTOR_C1_2.md`。


## C-1.3 更新

- 文件页签高度缩减至约 80%，悬停显示关闭按钮。
- 关闭页签不删除文件，当前页签关闭前自动保存并切换。
- 访客可通过全局快捷入口只读查看考题训练与深度回忆页面。
## C-1.4.1 悬浮模块交互修复

- 修复左右悬浮模块被画布拖拽事件拦截，导致账号菜单、升级会员和图谱信息无法点击的问题。
- 悬浮模块内滚轮操作不再缩放画布。
- 画布空白区原有拖拽、缩放和双击操作保持不变。


## C-1.4.3 会员权益弹窗优化

- 会员卡片按实际文案高度自然排版，避免短文案产生大面积空白。
- 全部会员方案保持单行展示，超出区域可横向滚动或滑动查看。
- 卡密区域改为“卡密使用”，删除说明文字并压缩整体高度。

## C-1.4.4：移动端账号入口与触摸页签修复

- 移动端账号入口固定吸附在屏幕顶部右侧，入口简写为“账号”。
- 下拉菜单在移动端使用“用户 / 帮助 / 升级 / 登录或退出”的两字短标签。
- 桌面端继续显示完整账号菜单文字。
- 触摸设备及无 hover 设备的文件页签关闭按钮保持常显。
- 页签栏为移动端账号入口预留空间，避免入口遮挡文件页签。
## v7.9.40 关系线菜单紧凑与虚线比例优化

- 关系线快捷菜单缩小为更紧凑的正圆排布，八个按钮继续保持对称。
- 修复文字按钮被旧样式隐藏的问题，按钮内明确显示 `T` 图标。
- 短虚线按线宽生成正方形小段；长虚线按线宽生成约 4:1 的长方形小段，并保持直角端点。
- 关系文字编辑框移除外围白色底板、白边和额外留白，仅保留输入区域。

## v7.9.41 缩放平滑恢复与 4%-400% 范围

- 点击左下角缩放百分比恢复 100% 时，改为从当前视角平滑过渡，不再生硬跳变。
- 主画布缩放范围调整为 4%～400%，滑轨、滚轮、按钮、触控缩放统一使用该范围。
- 旧文件中的视口缩放读取也同步兼容 4%～400% 范围。

## v8.4.25-alpha 学习架构阶段1

考题训练页已接入中央学习会话、题目仓储、学习事件仓储和流程控制器。界面与题库格式保持兼容。详见 `docs/LEARNING_ARCHITECTURE_STAGE1.md`。

## v8.4.26-alpha 卡片运行时

训练页已建立可插拔学习卡片平台，并完成第一张“独立作答卡”迁移。

新增浏览器全局模块：

```javascript
window.KGCardContract
window.KGCardContext
window.KGCardRegistry
window.KGCardRuntime
```

独立预览：

```text
dev/answer-card-demo.html
```

详细说明见 `docs/CARD_RUNTIME_STAGE2.md`。

## v8.4.27-alpha 无限学习画布

五个学习步骤已经放入同一无限画布，并支持：

- 引导学习与自由探索
- 卡片拖拽
- 画布平移和缩放
- 当前卡聚焦
- 全览与小地图
- 每题布局保存
- 移动端单卡模式

独立预览：

```text
dev/infinite-canvas-demo.html
```

详细说明见 `docs/INFINITE_CANVAS_STAGE3.md`。

## v8.4.27.1 轻量顶部与学习路径

训练页已进一步突出无限画布：

- 标题区和五步里程碑轻量化
- 画布工具改为悬浮栏
- 五张卡通过 SVG 主干线连接
- 卡片拖动时连线实时跟随
- 自由探索时点击卡片突出相关路径
- 提供连通卡片和可达路径 API

核心五步主干线本版不可删除。自定义扩展线和按路径跳过将在后续版本开放。

详细说明见：

```text
docs/COMPACT_SHELL_PATH_EDGES_V84271.md
```

## v8.4.27.2 动态进度与跨题工作区

本版新增：

- 五点动态进度轨道
- 悬浮步骤说明
- 画布内题目坞
- 从题目列表拖题到画布
- 跨题 `CanvasWorkspaceStore`
- 可持久化题目引用卡
- 跨题库重新进入深学
- 题目卡去重、定位和移除

新增全局模块：

```javascript
window.KGCanvasWorkspaceStore
window.KGWorkspaceQuestionCards
```

独立预览：

```text
dev/progress-workspace-demo.html
```

详细说明：

```text
docs/PROGRESS_WORKSPACE_V84272.md
```

## v8.4.27.3 画布缩放与界面修复

本版修复：

- 缩放范围和档位与知识图谱统一
- 滚轮与按钮平滑缩放
- 15px精细进度节点
- 移除完成对勾
- 桌面浏览器缩放时悬浮工具不再消失
- 点阵背景跟随画布平移和缩放
- 切题时取消旧缩放动画和延迟保存

详细说明：

```text
docs/CANVAS_ZOOM_UI_FIXES_V84273.md
```

## v8.4.27.4 移动只读与稳定性修复

本版重点修复：

- 移动端仅允许查看、切题和完成五步作答
- 移动端禁止自由探索和画布编辑
- 移动悬浮栏稳定显示
- 1%～400%极端缩放保存后不再回跳
- 题目拖入画布增加拖拽把手和多层数据降级
- 浏览器隐藏自定义拖动类型时仍可放置
- 放置后完整清理拖动状态

详细说明：

```text
docs/MOBILE_READONLY_ZOOM_DRAG_FIXES_V84274.md
```

## v8.4.28-alpha 双画布架构

学习空间现在分为两个独立页面：

```text
question-training.html
单题深学：当前题目的五步学习闭环

question-workspace.html
多题归纳：多道题的整理、比较和规律沉淀
```

主要变化：

- 单题画布只包含五张核心学习卡
- 多题卡不再干扰单题自动聚焦、全览和小地图
- 左右悬浮工具移到不可滚动的外层 Overlay
- 支持多个独立多题画布
- 支持新建、切换、改名和删除画布
- 单题题目坞可选择目标多题画布
- 拖题操作只在多题归纳页面中进行
- 移动端仅查看和作答，不参与画布编辑

新增全局模块：

```javascript
window.KGMultiQuestionWorkspace
```

详细说明：

```text
docs/DUAL_CANVAS_ARCHITECTURE_V8428.md
```

## v8.4.29-alpha 统一 Canvas Kernel

双画布继续保留独立业务文档，但开始共用同一套底层画布平台：

```text
CanvasPolicy
CanvasViewportController
CanvasCardController
CanvasKernel
```

新增全局 API：

```javascript
window.KGCanvasPolicy
window.KGCanvasViewportController
window.KGCanvasCardController
window.KGCanvasKernel
```

当前统一能力：

- 画布能力策略
- 平移与缩放
- 自适应点阵
- 坐标转换
- 全览与聚焦
- 卡片注册
- 卡片类型白名单
- 卡片拖动
- 卡片边界
- 视口和卡片保存回调
- 移动端只读策略
- 插件生命周期

独立预览：

```text
dev/unified-canvas-kernel-demo.html
```

详细说明：

```text
docs/UNIFIED_CANVAS_KERNEL_V8429.md
```

