## v8.4.23

- 修复列表视图文件行文字上下错位：将选择框、文件名、修改时间、节点、关系、大小、标签和操作按钮固定在同一 CSS Grid 行。
- 解决 `display: contents` 与操作按钮 DOM 顺序共同触发隐式第二行布局的问题，列表内容重新与每行中线对齐。
- 网格视图“当前打开”标识改为相对整张卡片定位，移动到左下角，并与右下角圆点标签使用相同的 14px 底部基线。
- 回收站标识继续保留在缩略图左上角。
- 不修改文件数据结构、存储逻辑和缩略图生成逻辑。

## v8.4.22

- 文件列表视图中的文件名、修改时间、节点、关系、大小、标签和操作统一垂直居中。
- 列表行高度调整为 64px，减少内容上方空白并保持表头列对齐。
- 网格视图中的三点操作按钮默认隐藏，与复选框采用相同的悬浮显示节奏。
- 文件卡片悬浮、键盘聚焦、选中或菜单操作期间，三点按钮保持可见。
- 列表视图中的三点按钮继续常驻，保证表格式操作效率。
- 网格视图“当前打开”状态移动到缩略图左下角，不再与左上角复选框重叠。
- 回收站状态仍保留在缩略图左上角。
- 不修改文件数据、缩略图生成逻辑及目录结构。

## v8.4.21 — 网格视图运行时依赖修复

- 修复旧函数清理时误删仍在使用的 `COVER_PALETTES`、`stableCoverHash()` 与 `coverSerial`。
- 该问题会在网格模式创建占位图或 SVG 缩略图时触发 `ReferenceError`，导致文件卡片无法正常渲染；列表模式因不生成缩略图而未受影响。
- 恢复稳定配色选择、文件结构散列和 SVG 渐变唯一 ID 计数器。
- 保持 v8.4.20 的轻量真实结构预览、扁平视觉、响应式列表和主题一致性不变。

## v8.4.20 — 响应式列表与占位背景修复

- 修复宽度小于 1250px 时列表模式仍使用旧九列结构，导致文件名压缩和字段错位的问题。
- 响应式列表统一为八列：选择、文件名、标签、节点、关系、大小、时间、操作。
- 懒加载占位背景不再混入页面主题色，浅色与深色模式下保持一致。
- 保持 v8.4.19 的缩略图懒加载、轻量真实结构、扁平视觉和列表模式无缩略图设计不变。

## v8.4.19 — 缩略图懒加载与焦点修复

- 修复卡片缩略图占位元素缺少 `fm-file-cover-lazy` 与 `data-preview-file`，导致 SVG 永远不加载的问题。
- 恢复缩略图加载占位提示，继续沿用现有 `IntersectionObserver` 懒加载机制。
- 恢复文件卡片的 `:focus-visible` 键盘焦点样式。
- 删除不再使用的旧配色辅助函数，减少重复逻辑。
- 保持 v8.4.18 的轻量真实结构、扁平视觉、深浅主题一致和列表模式无缩略图设计不变。

## v8.4.18 — 缩略图收尾清理

- 占位图与最终 SVG 使用完全相同的稳定散列和配色选择，消除懒加载前后的颜色跳变。
- 删除已不再使用的旧 `fallbackPreview()` 回退函数，避免重复逻辑和维护歧义。
- 保持 v8.4.17 的轻量真实结构、扁平视觉、深浅主题一致和列表模式无缩略图设计不变。

## v8.4.17 — 扁平轻量内容缩略图

- 保留文件轻量 preview 中的节点分布、连接关系和结构密度。
- 移除所有文字标签、胶囊、投影、阴影、柔光和节点高光。
- 节点仅保留扁平填充、细白色描边和少量大小层级。
- 连线继续区分普通关系与主干关系，但整体降低视觉噪声。
- 缩略图 SVG 元素数量进一步减少，代码和维护复杂度下降。
- 不读取完整图谱，不做实时布局；浅色与深色模式缩略图保持一致。
- 列表模式继续不生成、不显示缩略图。

## v8.4.16 — 轻量真实内容高级缩略图

- 缩略图重新使用文件索引中已有的轻量 preview 数据，体现节点分布、连接关系、结构密度和少量关键标签。
- 每张卡片最多绘制 14 个节点、20 条关系和 4 个标签，不读取完整图谱，不执行实时力导向布局。
- 加入分位数归一化和 5 轮轻量避让，降低离群点、重叠节点对缩略图的影响。
- 视觉升级为同色系柔和渐变、主干关系层次、节点投影、柔光、高光和半透明标签胶囊。
- 文件间差异由真实 preview 结构、结构哈希、节点数和关系数共同决定。
- 浅色与深色模式使用相同缩略图；列表模式继续不生成、不显示缩略图。

## v8.4.15 — 缩略图区分度与主题一致性修正

- 扩充为 8 套配色、6 套明显不同的图谱构图，并加入镜像、缩放、位移和节点样式变化。
- 使用文件 ID、名称、时间与统计字段生成稳定散列，避免多个文件落入相同封面组合。
- 浅色与深色模式使用完全相同的缩略图颜色和亮度，页面主题不再修改封面。
- 保持固定节点数量与固定 SVG 复杂度，不读取真实图谱内容，不增加运行时分析成本。
- 列表模式继续不生成、不显示缩略图。

## v8.4.14 — 卡片预览视觉优化

- 卡片模式改用轻量、固定复杂度的装饰型图谱封面，采用低饱和同色系、柔和渐变、留白和简化节点。
- 缩略图仅依据文件标识选择配色与布局，不读取或计算真实图谱结构，降低维护成本并保持稳定性能。
- 列表模式不再创建或显示缩略图，释放文件名空间并减少 DOM/SVG 渲染。
- 深色模式使用独立配色处理，不再整体压暗缩略图。

## v8.4.13

- 右侧子文件夹区域与左侧文件树统一使用中文数字自然名称排序。
- 清理旧版卡片式列表视图 CSS，仅保留当前表格式列表实现。
- 左侧侧栏滚动时自动关闭文件、文件夹、标签等悬浮菜单，避免菜单与目标行脱离。
- 文件夹拖放时增加“移动到某文件夹”“移动到根目录”和禁止目标文字提示。
- 拖放结束、取消或离开目标后统一移除提示文字及高亮状态。
- 当前目录没有子文件夹时隐藏右侧收起箭头，只保留文件夹标题和新建按钮。
- 当前目录重新出现子文件夹时恢复用户此前保存的展开或收起状态。
- 不修改文件存储结构，也不引入同级文件夹自由排序。

## v8.4.12

- 左侧文件树的文件夹菜单优先显示在鼠标或更多按钮的右下方。
- 移除旧版通过固定负偏移将菜单放到左侧的定位方式，避免遮挡文件树。
- 文件夹右键菜单与更多按钮菜单统一增加 7px 间距。
- 靠近屏幕右侧时自动翻转到左侧，靠近屏幕底部时自动翻转到上方。
- 右侧核心区域子文件夹菜单同步采用相同的智能定位规则。
- 不修改菜单内容、文件夹数据结构及拖拽逻辑。

## v8.4.11

- 右侧“文件夹”分区标题栏增加新建文件夹加号按钮。
- 增加文件夹区域收起/展开箭头，并保存用户的收起状态。
- 即使当前目录没有子文件夹，也保留文件夹标题栏与新建入口。
- 文件夹卡片进一步压缩高度、间距和图标尺寸，使核心区域排布更简洁。
- 回收站视图隐藏新建文件夹按钮，但保留文件夹区域展开与收起能力。
- 重新校验左侧文件树同级文件夹拖拽：将同级文件夹拖到另一个文件夹上会移动为其子文件夹。
- 拖到当前父文件夹或根目录时会识别“已在该位置”，避免重复移动。
- 继续禁止拖入自身、自己的子文件夹及“未分类文件”。
- 同级自由排序仍未启用；本版本仅处理层级移动，不引入 sortOrder 数据结构。

## v8.4.10

- 右侧核心区域增加独立的“文件夹（数量）”分区，仅展示当前目录的直接子文件夹。
- 子文件夹采用紧凑横向卡片，显示名称、直接子文件夹数量和直接文件数量。
- 子文件夹卡片支持点击进入、右键菜单、更多操作以及文件或文件夹拖入。
- 文件夹与图谱文件不再混在同一列表中，文件区域独立显示“文件数（数量）”。
- 列表视图增加固定列标题：文件名、最近修改时间、节点、关系、文件大小、标签和操作。
- 列表行改为表格式结构，统一缩略图、文件名、日期、统计、标签及菜单的列对齐。
- “当前打开”在列表模式中改为文件名旁的小型状态标签，不再覆盖缩略图。
- 列表行去除大圆角卡片和行间空隙，改为轻量分隔线与整行悬浮底色。
- 网格视图保持原有图谱卡片展示，不受列表样式调整影响。
- 清理已废弃的核心区域大型文件夹卡片视觉样式。
- 不修改文件存储结构，也不引入同级文件夹排序。

## v8.4.9

- 修复左侧文件夹区域“新建文件夹”加号图标不可见的问题，改为稳定的 SVG 图标。
- 文件夹拖拽不再在悬停过程中整体重绘目录树，避免拖拽被浏览器中断。
- 拖放完成、取消或失败后统一清理拖拽载荷与高亮状态。
- 展开箭头和更多菜单按钮不再触发文件夹拖拽，降低误操作。
- 开始拖动文件夹时不再改变右侧详情选中状态。
- 文件夹不能拖入“未分类文件”；图谱文件仍可拖入该入口并回到根目录。
- 文件夹已处于目标目录时不再重复执行移动。
- 继续保留拖入其他文件夹、拖回根目录、循环目录防护和回收站禁用拖拽。
- 为保证稳定性，本版本暂时取消拖拽悬停自动展开，后续可在不重绘目录树的前提下重新实现。

## v8.4.8

- 移除左侧“我的文件”入口，普通文件浏览由常驻文件夹树统一承担。
- 文件夹树在“最近打开”和“我的收藏”等视图中继续显示，不再随入口切换隐藏。
- 点击“全部文件”“未分类文件”或任意文件夹时，自动切回普通文件浏览视图。
- 移除文件夹树中“全部文件”“未分类文件”及每个文件夹后的数量显示。
- 活动文件夹支持在左侧树中直接拖拽。
- 文件夹可拖入另一个文件夹，成为其子文件夹。
- 文件夹可拖到“全部文件”或文件夹树空白区域，移动回根目录。
- 严格禁止文件夹拖入自身或自己的子文件夹，避免形成循环目录。
- 拖动到折叠且包含子目录的目标文件夹上停留约 600ms，可自动展开目标目录。
- 回收站内文件夹不支持层级拖拽，避免影响恢复路径。
- 本版本不引入 sortOrder，也不支持同级自由排序；排序能力留待后续独立版本实现。

## v8.4.7

- “知识图谱”品牌区固定在左侧顶部，其余侧栏内容统一进入纵向滚动区域。
- 左侧侧栏支持收起与展开，并保存用户的侧栏状态。
- 收起后保留新建、最近打开、我的文件、我的收藏和回收站图标入口。
- 回收站入口移动到左侧靠下区域，与主要导航分离。
- 文件夹树取消内部高度限制，由整个侧栏统一滚动，文件夹较多时仍可查看存储信息和底部入口。
- “我的收藏”标签筛选从固定侧栏区域改为工具栏弹窗。
- 收藏标签弹窗展示全部标签、标签文件数量，并支持直接创建标签。
- 创建新标签的二级菜单增加 320ms 延迟隐藏和透明悬浮连接区，避免鼠标移动过程中菜单提前消失。
- 标签弹窗和二级菜单均禁止横向滚动，长名称使用省略显示。

## v8.4.6

- 修复标签菜单外层横向裁剪导致“创建新标签”二级菜单可能不可见的问题。
- 标签列表继续只允许纵向滚动，超长名称与内联编辑保持宽度约束。
- 恢复文件时优先返回原文件夹；原文件夹已删除或仍在回收站时统一恢复到根目录。
- 恢复文件后自动切换到实际恢复位置并选中文件。
- 恢复文件夹时，原父文件夹不可用则恢复到根目录，并自动打开恢复后的文件夹。
- 当前目录失效时统一回退到“全部文件”，避免误入“未分类文件”。
- 移除核心内容区已经废弃的文件夹卡片渲染和相关事件分支。
- 文件夹新建入口统一保留在左侧文件夹树标题栏，去除重复入口。
- 手机端执行只读策略：进入时显示提示，并拦截新建、编辑、移动、删除等操作。

## v8.4.5

- 标签选择菜单禁止横向滚动，仅保留纵向滚动条。
- 超长标签名称使用省略号显示，编辑控件不会撑开菜单宽度。
- 文件管理核心内容区不再混合展示文件夹卡片，只显示图谱文件。
- 左侧新增文件夹树，包含“全部文件”“未分类文件”和多级文件夹。
- 文件夹树支持展开、折叠、选中目录、新建文件夹和更多操作菜单。
- 文件可拖放到左侧文件夹树完成移动。
- 回收站中的文件夹也在左侧目录树中管理，可恢复或永久删除。
- 文件夹重命名改为轻量对话框，不再依赖核心区域中的文件夹卡片。

## v8.4.4

- 移除独立标签管理弹窗，降低标签管理操作层级。
- 标签选择菜单中，悬浮标签即可显示编辑与删除图标。
- 点击编辑后在当前标签下方原位展开名称、颜色、保存与取消控件。
- 点击删除可直接删除标签，并自动清除相关文件的标签与收藏状态。
- 触屏设备默认显示管理图标，避免依赖悬浮操作。
- 标签菜单继续保持与文件右键菜单一致的视觉规范。

## v8.4.3

- 标签圆点弹出的标签菜单全面匹配文件右键菜单样式。
- 统一菜单背景、边框、圆角、阴影、字号、字重、行高与悬浮底色。
- 搜索框、标签条目、创建入口和二级创建菜单采用同一套视觉规范。
- “创建并应用”继续使用对勾图标，并调整为与右键菜单按钮一致的 35px 尺寸。
- 后续开发版本将按版本号顺序递增，便于记录和识别。

## v8.4.2 标签菜单视觉微调

- 文件卡片标签圆点弹出菜单改为更轻的悬浮底色与阴影。
- 标签项、创建入口的悬浮态降低对比度，视觉更柔和。
- 二级菜单“创建并应用”文字按钮改为对勾图标，并保留无障碍标签与提示。

## v8.4.2

- 文件卡片标签入口改为右下角圆点：已打标签显示对应颜色实心圆，未打标签仅在悬浮、选中或键盘聚焦时显示空心圆。
- 标签选择菜单移除复选框和勾选符号，选择“ 不打标签 ”即取消标签并退出收藏。
- 创建标签改为轻量二级菜单，支持鼠标悬浮、点击与键盘焦点展开，创建后立即应用。

# v8.4.1 标签即收藏轻量化版

- 标签与收藏合并：文件拥有标签即收藏，移除标签即取消收藏。
- 每个图谱文件最多一个标签；文件夹不支持收藏或标签。
- 移除卡片星标，标签图标移至卡片右下角。
- 标签图标可直接打开轻量选择面板，支持搜索、替换、移除和内联创建。
- 我的收藏仅展示已打标签文件，并显示标签筛选。
- 兼容 v8.4.0：仅收藏文件自动迁移到“收藏”标签，多标签保留第一个。

## v8.4.0（收藏与标签管理）

- 文件和文件夹支持收藏／取消收藏，左侧新增“收藏”入口。
- 文件卡片和文件夹卡片增加轻量星标按钮，右键菜单、详情抽屉和批量工具栏均可操作收藏。
- 新增标签定义库，支持创建、重命名、改色和删除标签。
- 标签重命名会事务式同步所有关联图谱文件；删除标签不会删除文件。
- 左侧新增标签筛选，文件卡片和详情抽屉按标签颜色显示。
- 文件右键菜单、详情抽屉与批量工具栏可快速添加或移除标签。
- 收藏和标签均写入轻量索引，文件列表无需加载完整图谱正文。
- 收藏与标签交互拆分到 `27-file-manager-organize.js` 和独立样式文件，降低主文件继续膨胀的速度。

## v8.3.5（账号菜单层级修复）

- 修复头像账号菜单被下方文件预览卡片覆盖的问题。
- 明确顶部栏的堆叠上下文和层级。
- 保持右键菜单、详情抽屉、模态框等更高优先级浮层正常显示。

## v8.3.4（稳定性与性能补丁）

- 文件库完整性检查改为浏览器空闲阶段执行并缓存，不再每次渲染扫描全部图谱正文。
- 手动刷新可强制重新执行完整性检查。
- 多选后右键点击已选项目会保留整组选区；右键未选项目才切换为单选。
- 数据刷新、跨页面删除和视图切换后自动清理无效多选项。
- 拖动文件夹到自身或子文件夹时提前显示禁止反馈，并阻止放置。
- 右键主菜单及“移动到”二级菜单支持上下、左右、Home、End、Escape 键。
- 首次进入文件管理页时优先定位并选中当前打开图谱。

## v8.3.3（交互精简）

- “移动到”改为二级菜单，主菜单保持简洁，最近目标最多 4 个。
- 文件信息改为按需打开的右侧抽屉，支持工具栏、右键“查看详情”和 Alt+Enter。
- 选中项目后仅显示底部轻量信息条，详情抽屉状态会被记住。
- Esc 优先关闭详情抽屉，二级菜单支持边缘自动向左展开。

# v8.3.2

- 修复右键菜单未明确展示“选择其他位置…”的问题。
- 最近文件夹快捷目标限制为最多 4 个。
- 新增显式多选模式、复选标记与批量操作栏。
- 多选模式支持批量移动、导出和移入回收站。
- 批量删除时自动忽略已被选中父文件夹包含的项目，并在失败时回滚。
- 修复文件信息面板重复的“创建时间”字段 ID。

# v8.3.1

- 新增文件/文件夹拖拽移动。
- 支持拖到文件夹卡片、面包屑和根目录。
- 新增 Ctrl/Command、Shift 多选和批量拖动。
- 新增最近移动目标快捷入口。
- 新增移动撤销和批量失败回滚。
- 增强拖放目标、选中标记和深浅色样式。

## v8.3.0 - 文件夹与层级管理

- 文件管理页新增多级文件夹，支持在根目录或任意文件夹中创建子文件夹。
- 新增面包屑导航，可快速返回上级目录或根目录。
- 图谱文件支持“移动到文件夹”，文件夹支持移动并阻止移动到自身或子文件夹。
- 文件夹卡片支持双击进入、右键菜单、内联重命名、详情查看和浅色/深色主题。
- 文件夹移入回收站时会连同子文件夹和其中图谱一起处理；恢复时保持原层级，原父目录不存在时恢复到根目录。
- 单独恢复位于已删除文件夹中的图谱时自动放回根目录，避免形成不可见文件。
- 清空回收站会同时清理图谱文件和文件夹元数据。
- 文件索引沿用 `folderId`，新增独立文件夹索引 `kg_graph_folders_v1`，不复制图谱正文。

## v8.2.8 - 文件管理稳定性补丁

- 后台文件刷新、缩略图补齐和跨页面存储事件不再自动提交正在输入的文件名。
- 重命名输入内容、光标位置和选区会在列表刷新后恢复；文件被外部删除时才取消编辑并提示。
- 存储空间统计增加请求序号与用户校验，避免快速刷新或切换账号时旧异步结果覆盖当前账号。
- 存储配额读取失败时保持页面可用，并输出可诊断日志。

## v8.2.7 - 文件名内联重命名

- 文件管理页的“重命名”不再打开弹窗，文件名会在卡片或列表原位置直接进入编辑状态。
- Enter 保存、Esc 取消、点击其他区域或输入框失焦时保存；空名称会保留原文件名。
- 编辑期间阻止双击打开，避免重命名和打开文件发生冲突。
- 重名继续自动追加序号，并同步更新图谱标题、详情栏和编辑器页签名称。
- 网格视图、列表视图、右键菜单和右侧操作按钮共用同一套内联重命名逻辑。

## v8.2.6 - 手动保存按钮与快捷键

- 左上角保存状态图标升级为可点击的立即保存按钮，同时继续显示已保存、未保存、保存中和保存失败状态。
- 新增 Ctrl+S / Command+S 快捷键，阻止浏览器默认“保存网页”行为并立即保存当前图谱。
- 保存中暂时禁用按钮，避免重复触发；手动保存成功、无修改或失败时提供明确状态提示。
- 增加悬停、按下、键盘焦点和保存中动画反馈。

# v8.2.4

- 修复关闭图谱页签后进入文件管理器，再返回编辑器时已关闭页签重新出现的问题。
- 已关闭页签现在按登录用户保存在当前浏览器会话中，页面间往返和刷新不会丢失。
- 从文件管理器主动打开已关闭文件时，该文件会自动重新加入页签栏。
- 删除或移入回收站时同步清理对应的页签关闭记录。

# v8.2.3

- 移除右侧文件信息栏中的重复图谱缩略图。
- 图谱预览只在节点、关系、布局、颜色或节点标题变化时重新生成；学习状态、描述和文件级信息变化不再触发重算。
- 文件卡片缩略图改为 IntersectionObserver 懒加载，仅渲染视口附近的预览。
- 旧预览首次进入文件管理页时补充结构指纹，后续保存可复用缓存。

## v8.2.2 - 文件管理交互与动态图谱缩略图

- 修复文件卡片第一次单击后被重新渲染，导致浏览器无法识别连续双击的问题；现在双击卡片可直接打开图谱。
- 右上角头像升级为可操作账号菜单，提供身份信息、返回编辑器、系统设置、管理员用户管理和登录/退出入口。
- 图谱缩略图改为依据实际节点位置、节点颜色、关系线和核心节点标题生成，不同文件会呈现不同结构。
- v2 文件索引新增轻量 `preview` 元数据；新建与保存时自动更新，旧文件进入文件管理页后会在空闲时一次性补齐。
- 缩略图最多保存 18 个节点和 30 条关系，只渲染轻量 SVG，不在文件列表中反复加载完整图谱正文。
- 优化深色和浅色主题下的缩略图层次、标签、节点光晕、连线和悬浮效果。

## v8.2.1 - 文件管理回归修复

- 修复 100 字符文件名发生重名时无法正确追加序号的问题；批量导入同名长标题不再陷入无限循环。
- 修复未打开的导入文件和副本也被列入“最近打开”的问题；仅实际打开或设为当前文件时记录打开时间。
- 修复内容缺失的回收站文件恢复后被错误移回正常文件列表的问题；恢复前会验证内容并保持事务一致。
- 修复双击或用键盘操作卡片“更多”按钮时意外打开图谱的问题。
- 文件批量导入期间增加忙碌遮罩和重复操作保护，并减少逐文件重复刷新。
- 登录账号在当前页或其他标签页发生变化时，文件管理页会同步切换用户空间和身份显示。
- 完善图谱内容缺失时的错误提示，避免显示上一次操作遗留的错误信息。

## v8.2.0 - 文件管理功能第一阶段

- 新增独立页面 `file-manager.html`，文件管理与图谱编辑器解耦，方便后续持续扩展。
- 程序页签栏最左侧房子按钮改为文件管理入口；进入前会保存当前图谱，保存失败则取消跳转。
- 文件管理页提供最近打开、我的文件和回收站三个入口。
- 支持网格/列表视图、关键词搜索以及按更新时间、最近打开、名称、大小和创建时间排序。
- 支持新建、打开、重命名、创建副本、导入 ZIP/JSON、导出学习包和移入回收站。
- 回收站支持恢复、永久删除和清空，沿用 v2 存储层 30 天自动清理策略。
- 文件卡片和详情栏显示更新时间、节点数、关系数、文件大小、标签及当前打开状态。
- 左侧展示图谱文件库大小、回收站大小、节点/关系总数和浏览器存储配额估算。
- 文件列表只读取 v2 轻量索引；仅在打开或导出时读取完整图谱正文。
- 支持浅色/深色主题切换并记住用户选择，同时提供桌面端与移动端响应式布局。
- 导入继续执行 ZIP 安全限制，并额外验证 `nodes` / `links` 结构和节点、关系数量上限。


## v7.9.56 - 导出格式扩展

- 文件菜单中的“导出”升级为三级指向菜单。
- 保留原有“学习包 ZIP”导出，用于备份和跨设备恢复。
- 新增“PNG 图片”导出，自动导出整张图谱画面。
- 新增“PDF 文档”导出，自动将整张图谱画面嵌入单页 PDF，方便归档和分享。
- 导出 PNG / PDF 时会使用当前图谱名称生成文件名，并在状态消息中提示生成进度。


## v7.9.55 - 图谱设置指向式菜单微调

- 左上角图谱名称后的设置按钮点击后显示一级菜单：文件、偏好设置。
- 鼠标悬浮“文件”时，右侧滑出新建 / 导入 / 导出面板。
- 鼠标悬浮“偏好设置”时，右侧滑出原偏好设置面板。
- 调整一级菜单宽度，适配“偏好设置”完整文案。


## v7.9.54
- 左下角缩放栏加长后，状态消息会根据缩放栏实际宽度自动右移，避免遮挡。
- 图谱设置入口改为指向式二级菜单：点击出现“文件 / 偏好”，悬浮“文件”显示新建、导入、导出，悬浮“偏好”显示原偏好设置面板。
- 修复快速生长落点函数与框选重名导致的框选异常，并将框选范围框调整为直角矩形。

# 更新日志

本文件记录首页知识图谱工具的版本优化历史。后续每次优化应同步更新本文件。

## v7.9.6 维护优化记录

- 将首页“搜索定位”按钮的事件绑定迁回 `src/10-graph-editor.js`，避免 `src/20-flashcards-toolbar.js` 与图谱搜索产生隐式耦合。
- 为图谱搜索增加轻量缓存索引，节点字段只在索引失效后统一拼接和转小写，降低大图谱连续输入搜索时的重复计算。
- 合并左上角图谱信息悬浮栏相关 CSS，移除 v7.9.5 追加式覆盖块，降低后续样式维护成本。
- 保持 v7.9.5 的界面行为：图谱信息栏只显示图谱名称，搜索入口在图谱信息栏内，详情窗口不显示尺寸字段。


## v7.9.7 用户偏好与高性能模式

- 首页左上角图谱名称悬浮栏新增“偏好设置”入口。
- 新增浏览器级用户偏好存储 `kg_graph_user_preferences_v1`，不会写入图谱文件或学习包。
- 支持“精致 / 平衡 / 高性能 / 自定义”视觉模式。
- 支持单独关闭阴影、玻璃模糊、动画过渡、关系线标签和卡牌渐变背景。
- 高性能模式通过根节点 `kg-perf-*` class 统一覆盖 CSS，避免逐节点修改 DOM，适合大图谱和低性能设备。


## v7.9.8 多选拖动连接线延迟刷新

- 针对多选卡牌拖动卡顿，新增“拖动时延迟刷新连接线”偏好项。
- 多选拖动过程中默认只移动卡牌，暂停 SVG 关系线 path/text 的实时几何刷新。
- 松手后统一刷新一次关系线，避免 50 张左右图谱中关系线密集时拖动卡顿。
- 拖动中会临时淡化连接线，减少连接线停留在旧位置带来的视觉干扰。




## v7.9.10 手动大图模式开关

- 偏好设置中的“大图模式”改为真正的手动开关，默认开启。
- 开启后，任何数量卡牌的图谱都会使用大图渲染策略。
- 关闭后，当前图谱按普通图谱模式显示，关系线较多时可能变卡。
- 切换开关后会立即重新渲染首页图谱，无需刷新页面。


## v7.9.11 大图模式选中标识与框选删除

- 修复大图模式下卡牌选中状态不明显的问题，单选和框选都会显示外框与“已选”标识。
- 在大图模式的局部关系/只看相关状态下，已选卡牌会保持可见。
- Delete / Backspace 现在支持删除框选的多个知识点，并同步删除相关关系线。


## v7.9.12 快捷操作增强

- 支持 Ctrl/Command + 点击卡牌进行复选，再次点击已选卡牌可取消选择。
- 支持 Ctrl/Command + C 复制当前选中的卡牌到应用内部剪贴板。
- 支持 Ctrl/Command + V 在鼠标所在画布位置粘贴卡牌；只复制卡牌本身，不复制关系线。
- 支持 Ctrl/Command + Z 撤回删除和粘贴等快捷操作，主要用于误删恢复。
- 删除单张卡牌、框选批量删除、删除关系线和粘贴卡牌都会写入撤回栈。


## v7.9.13 箭头编辑模式 / 手型演示模式

- 默认进入箭头编辑模式：可点击卡牌和关系线、拖拽卡牌、框选、复制粘贴、编辑弹窗；左键不再拖动画布。
- 按住空格键可临时进入手型演示模式，松开后恢复箭头编辑模式。
- 在图谱区域按住鼠标右键可临时进入手型演示模式，松开后恢复，并禁用图谱区域默认右键菜单。
- 工具栏“居中”按钮右侧新增箭头/手型切换按钮，可长期切换编辑/演示模式。
- 手型模式下卡牌和关系线不触发选择、拖拽、编辑等操作，只用于平移浏览画布。


## v7.9.14 编辑模式直接框选

- 编辑模式下，空白画布区域按住鼠标左键拖动即可框选卡牌，不再需要按住 Shift。
- 单击空白画布仍会清空选择并关闭详情。
- 手型演示模式保持不变，仍用于拖动画布浏览。


## v7.9.15 选中状态圆点标识

- 已选卡牌不再显示“已选”文字。
- 单选和多选卡牌右上角改为显示卡牌自身颜色的小圆点。
- 大图模式和普通模式保持一致的轻量选中标识。


## v7.9.16 连线起点提示位置优化

- 双击卡牌设为连线起点后，“连线起点”提示移到卡牌外部上方，避免遮挡卡牌内容。
- 提示文字保持单行显示，并根据卡牌宽度自动缩小字号。
- 小卡、默认卡和大卡分别做了上方间距微调。


## v7.9.17 连线选中状态与快捷线型

- 单击关系线后，选中关系线会显示更明显的高亮状态。
- 选中关系线旁边新增“实线 / 虚线”快捷按钮。
- 可直接在线旁切换线型，无需回到左侧工具栏。
- 画布平移、缩放或关系线端点移动时，快捷按钮会跟随关系线位置更新。


## v7.9.18 连线快捷按钮位置与图标优化

- 关系线快捷线型按钮改为纯图标，不再显示“实线 / 虚线”文字。
- 快捷按钮显示在关系线旁边，并尽量靠近鼠标点击位置。
- 画布缩放和平移后，快捷按钮仍会跟随点击处所在的关系线位置。


## v7.9.19 双击连线就地编辑文字

- 双击关系线可在线旁直接输入 / 修改线上文字。
- Enter 保存，Esc 取消，输入框失焦自动保存。
- 输入为空并保存时，会清空线上文字。
- 修改前会写入撤回栈，可通过 Ctrl / Command + Z 撤回误改。
- Alt + 双击关系线保留原完整关系线编辑弹窗入口。


## v7.9.20 大图模式连线文字编辑修复

- 修复大图模式下双击可见连线无法弹出线上文字输入框的问题。
- 大图模式下已渲染的主干线 / 骨架线现在可单击选中、双击就地编辑文字。
- 保持手型演示模式规则不变：手型模式下连线仍不触发编辑。


## v7.9.21 普通模式关系文字按需显示

- 普通模式下，关系线文字默认不再全部显示，减少大量 SVG 文本渲染带来的卡顿。
- 单击关系线时，该关系线文字会显示。
- 单击卡牌或框选卡牌时，与所选卡牌相连的关系文字会显示。
- 大图模式保持原有按局部关系显示文字的策略。


## v7.9.22 工具栏快捷键梳理

- V：切换箭头编辑模式 / 手型演示模式。
- N：新增知识点。
- T：打开模板。
- F：重点聚焦。
- K：记忆闪卡。
- Q：考题训练。
- 1 / 2：小卡 / 大卡。
- S / D：实线 / 虚线。
- C：打开线条 / 卡牌颜色选择器。
- - / +：缩小 / 放大。
- 0：居中显示。
- L：心流模式。
- G：主干线 / 骨架线。
- R：只看相关。
- 工具栏按钮的鼠标悬浮提示会自动追加对应快捷键；输入框、弹窗和文字编辑状态下不会触发这些快捷键。


## v7.9.23 工具提示简化与高风险快捷键移除

- 工具栏悬浮提示统一简化为“功能 + 快捷键”，例如“新增 N”“选择/手型 V”。
- 取消导出、导入、清空图谱的单键快捷键，降低误触风险。
- 导出、导入、清空图谱仍保留工具栏按钮操作。
- 其余常用编辑和视图快捷键保持不变。


## v7.9.24 工具栏提示清理

- 修复工具栏按钮悬浮时同时出现黑色自定义提示和浏览器原生 title 提示的问题。
- 心流、主干线、只看相关三个动态按钮的提示改为简短格式：`心流 L`、`主干线 G`、`相关 R`。
- 选择 / 手型按钮保持简短提示：`选择/手型 V`。
- 导入、导出、清空仍不绑定快捷键，降低误触风险。


## v7.9.25 居中选中卡牌

- 点击工具栏“居中”按钮或按 `0` 时，若当前选中了卡牌，会优先将选中卡牌居中并缩放到合适大小。
- 多选 / 框选多张卡牌时，会以选中卡牌组的整体范围居中并适配缩放。
- 未选中卡牌时，保持原有全画布居中逻辑。


## v7.9.26 居中视角平滑过渡

- 点击“居中”或按 `0` 时，不再瞬间跳转视角。
- 会从当前视角平滑过渡到目标视角，并包含缩放过程。
- 选中单张 / 多张卡牌居中、全画布居中、空画布居中均使用同一套平滑过渡。
- 若系统开启“减少动态效果”，会自动退回即时定位。


## v7.9.27 大图模式悬浮详情

- 大图模式下，鼠标悬浮卡牌约 650ms 后会自动显示详情窗口。
- 鼠标快速划过卡牌不会触发详情，减少大图模式下频繁重绘。
- 若开启主干线 / 骨架线，悬浮卡牌仍会保留局部关系预览。
- 拖拽卡牌、框选、手型 / 临时手型、居中视角动画、线上文字编辑时，不触发悬浮详情。


## v7.9.28 聚焦画面柔和过渡

- 重点聚焦、心流模式、只看相关、主干线 / 骨架线切换时，卡牌明暗、关系线透明度和画布背景会缓起缓停。
- 重点聚焦切换不再重建卡牌 DOM，改为保留现有卡牌并做状态过渡，视觉更柔和。
- 过渡期间关系线会做轻量淡入，减少突然闪变。
- 若系统或性能偏好开启“减少动画”，会自动禁用这些过渡。


## v7.9.29 图谱页签拖拽排序

- 首页顶部图谱页签支持鼠标拖拽排序。
- 拖拽页签到目标页签左侧 / 右侧后松手，即可调整顺序。
- 排序结果会持久化保存，刷新页面或切换图谱后仍保持。
- 拖拽关闭按钮不会触发排序；拖拽结束后不会误触打开页签。
- 新建图谱默认追加到当前页签列表末尾。


## v7.9.30 图谱页签拖拽让位动画

- 优化图谱页签拖拽排序体验。
- 拖拽经过目标页签时，相邻页签会向左 / 向右轻微让位，形成“被挤过去”的反馈。
- 插入位置蓝色竖线仍保留，同时目标页签会有轻微高亮。
- 拖拽结束后自动清理动画状态，不影响点击切换页签。


## v7.9.31 图谱标题原处编辑与文件信息

- 首页左上角图谱名称改为单击后直接在原位置编辑，不再弹窗。
- 标题输入 Enter / 失焦保存，Esc 取消。
- 双击图谱名称仍打开弹窗，但弹窗仅保留“标题”可编辑。
- 弹窗新增只读信息：创建时间、最后更新、文件大小、知识点数量、文件字数。


## v7.9.32 超大图谱完整适配视野

- 修复超大图谱（例如 1000 张卡）在“居中 / 全画布适配”后仍有卡牌超出屏幕的问题。
- 主画布最小缩放从原来的约 0.2 降到 0.035，允许超大图继续缩小到完整视野。
- 全画布居中时增加更舒适的屏幕留白，避免卡牌贴边。
- 滚轮缩放、工具栏缩放按钮、快捷键 `0` 会使用同一套超大图缩放范围。
- 兼容旧的 `fitBoundsToView(options)` 调用，避免相关卡牌聚拢适配被同名全局函数覆盖影响。


## v7.9.33 左下角缩放与全屏控制

- 将放大 / 缩小按钮从左侧浮动工具栏移到画布左下角。
- 左下角缩放条包含：缩小、实时缩放百分比、放大、全屏。
- 缩放百分比会随滚轮、按钮、居中适配、动画过渡实时更新。
- 点击百分比会将画布缩放恢复到 100%。
- 悬浮 / 聚焦百分比区域时会弹出滑轨，可拖动滑轨控制缩放比例。
- 新增画布全屏按钮，可进入 / 退出当前画布全屏。


## v7.9.34 缩放栏交互与更新日志

- 新增独立更新日志文件 `CHANGELOG.md`，后续版本优化会同步记录。
- 左下角缩放滑轨默认隐藏，点击缩放百分比后出现。
- 滑轨不再因鼠标悬浮 / 移开而自动显示或隐藏，方便连续拖动调整。
- 缩放功能栏更靠近画布左下边缘，释放画布空间。
- 消息提示移动到缩放功能栏右侧，避免与缩放按钮重叠。

## v7.9.35 关系线路径形态与圆弧快捷菜单

- 关系线新增路径形态：曲线、直线、折线。
- 旧图谱没有 `pathStyle` 字段时，默认仍按曲线显示，保持兼容。
- 点击关系线出现的快捷菜单改为圆弧形：上半部为实线 / 虚线，下半部为直线 / 折线 / 曲线。
- 快捷菜单出现时增加弹出缩放动效。
- 临时相关画布也会按关系线的路径形态显示。

## v7.9.36 关系线圆弧菜单轻量化

- 去掉关系线快捷菜单的背景圆盘，仅保留五个样式按钮。
- 上半部实线 / 虚线按钮改为圆弧排布，不再水平并排。
- 下半部直线 / 折线 / 曲线继续保持圆弧排布，并微调间距。
- 保留菜单弹出缩放动效和当前样式高亮。

## v7.9.37 关系线圆弧菜单加入文字与颜色

- 关系线圆弧菜单新增 `T` 按钮，点击后打开关系文字就地编辑框。
- 新增颜色圆点按钮，点击后可快速修改当前关系线颜色。
- 实线 / 虚线 / T / 颜色 / 直线 / 折线 / 曲线共七个按钮统一沿圆弧排布。
- 文字编辑不再主要依赖双击连线，入口更稳定。

## v7.9.38 关系线菜单对称布局与双虚线

- 关系线快捷菜单调整为更对称的圆弧排布。
- 顶部线型改为三颗：长虚线、实线、短虚线，实线位于中间。
- 中部左右分别为 `T` 文字编辑和实时颜色圆点。
- 颜色圆点改为菜单内原生色盘输入，点击后色盘出现在菜单按钮旁，不再跳回左侧工具栏。
- 底部继续保留直线、折线、曲线路径形态。

## v7.9.39 关系线菜单正圆排布与虚线直角化

- 修复关系线快捷菜单看起来偏椭圆的问题，八个按钮改为同一半径的正圆排布。
- `T` 文字按钮增加浅蓝底色，颜色按钮直接使用当前关系线颜色，避免左右两侧按钮看起来像空白按钮。
- 长虚线 / 短虚线的小段端点改为直角，不再使用圆角端点，视觉更简洁，也减少额外绘制成本。
## v7.9.40 关系线菜单紧凑与虚线比例优化

- 关系线快捷菜单缩小为更紧凑的正圆排布，八个按钮继续保持对称。
- 修复文字按钮被旧样式隐藏的问题，按钮内明确显示 `T` 图标。
- 短虚线按线宽生成正方形小段；长虚线按线宽生成约 4:1 的长方形小段，并保持直角端点。
- 关系文字编辑框移除外围白色底板、白边和额外留白，仅保留输入区域。

## v7.9.41 缩放平滑恢复与 4%-400% 范围

- 点击左下角缩放百分比恢复 100% 时，改为从当前视角平滑过渡，不再生硬跳变。
- 主画布缩放范围调整为 4%～400%，滑轨、滚轮、按钮、触控缩放统一使用该范围。
- 旧文件中的视口缩放读取也同步兼容 4%～400% 范围。
- 优化：关系线快捷菜单中当前选中的线型 / 路径按钮由蓝色改为深灰色，视觉更稳，不再与关系线颜色选择形成干扰。

## v7.9.43
- 新增：首页单击选中卡牌后，在四边中点显示快速生长蓝点；悬浮蓝点可预览虚框卡牌和连线，点击后复制原卡牌尺寸与颜色创建“未命名知识点”，并自动建立关系线。
- 优化：快速创建时不弹出编辑框，新卡牌保持选中，方便连续向四个方向扩展知识结构。

## v7.9.44
- 修复：首页卡牌四向快速生长小点在部分浏览器中只能显示虚框、点击无法创建新卡牌的问题；创建逻辑改为在按下小点时直接执行，不再依赖后续 click。
- 优化：四向小蓝点视觉尺寸缩小，并从卡牌边线向外偏移 12px，减少对选中卡牌视觉的遮挡。

## v7.9.45
- 修复：首页卡牌四向快速生长小点点击仍不生效的问题；小点由 button 改为 div role=button，并在小点自身直接绑定 pointerdown / click / 键盘事件。
- 稳定：创建时使用小点记录的源卡牌 id，不再在点击瞬间依赖 selectedNodeId / canShowNodeGrowthHandles 二次判断，避免悬浮预览重绘或事件顺序导致创建被拦截。

## v7.9.46
- 修复：首页卡牌四向快速生长的虚框预览在鼠标移开后可能持续保留的问题；新增小点 pointerleave 与全局 pointermove 兜底清理。
- 说明：快速创建时 sourceId 仅用于定位原卡牌，新卡牌仍通过 makeNode() 生成全新 ID，不会复用原卡牌 ID。

## v7.9.48
- 修复：v7.9.47 落点算法替换范围过大，误删 `renderCardElement`、`renderCards`、`refreshCardClasses`、`cardFromEvent` 等首页核心函数的问题。
- 优化：重新基于稳定的 v7.9.46 精确加入“四向快速生长就近落点”算法；遇到遮挡时优先在当前可视区域附近寻找最近空位，不再沿直线推到远处。
- 校验：保留小点点击创建、虚框移开消失、新卡牌唯一 ID、连线快捷菜单深灰色等前序稳定优化。

## v7.9.49
- 修复：首页卡牌四向快速生长在就近绕位预览时，预览线仍按单段直线绘制导致没有连接到虚框卡牌的问题。
- 优化：预览连线改为 SVG 折线，始终从原卡牌边缘连接到虚框卡牌边缘，并保留虚线与箭头效果。

## v7.9.50
- 优化：首页卡牌四向快速生长预览线的箭头三角形尺寸缩小，视觉更轻、更精致，并调整 marker 对齐避免箭头与折线衔接偏移。

## v7.9.51
- 优化：首页左下角缩放控制范围调整为 1%～400%，滑轨最小值同步改为 1%。
- 优化：鼠标滚轮缩放改为每次固定 10 个百分点，避免高倍率下单次滚轮跳动过大。
- 优化：左下角缩放按钮与快捷键 +/- 也改为每次 10 个百分点，缩放操作更精细。

## v7.9.52
- 优化：首页左侧工具栏合并“小卡 / 默认 / 大卡”为“尺寸”二级菜单。
- 优化：首页左侧工具栏合并“实线 / 虚线 / 点线 / 直线 / 折线 / 曲线”为“线型”二级菜单，颜色按钮继续保留为独立入口。
- 优化：“主干线 / 骨架线”和“只看相关”合并为“关系”二级菜单。
- 调整：导入 / 导出从左侧工具栏移出，升级图谱标题后的设置按钮，新增“偏好设置”和“图谱文件”入口；图谱文件支持新建文件、导入、导出。
- 调整：移除左侧工具栏“清空图谱”按钮；新增 Ctrl+A 全选画布卡牌，配合 Delete 可批量删除。
- 调整：左侧工具栏“居中”移动到左下角缩放栏，缩放栏新增居中按钮。

## v7.9.58 缩放丝滑过渡优化

- 优化：首页左下角缩放按钮与键盘 +/- 缩放不再瞬间跳变，改为短时缓动过渡。
- 优化：鼠标滚轮缩放改为围绕鼠标指针进行平滑补间，连续滚动会基于目标缩放档位继续推进，不会因为动画未完成而卡在同一档。
- 稳定：拖拽画布、框选、滑轨缩放、适配全画布等操作会自动中断缩放补间，避免多个视口动画互相抢控制权。
- 优化：缩放动画期间保持左下角百分比实时更新，并提示浏览器启用合成层，减少顿挫感。

## v7.9.59 - 图谱文件存储底层 v2

- 将图谱文件库升级为“轻量索引 + 单文件独立内容”存储。
- 自动迁移 `kg_graph_file_library_v1` 与 `kg_home_file_library_v1`。
- `KGHomeFileLibrary` 改为 `KGGraphFileStore` 兼容代理，消除双文件库。
- 新增 schemaVersion、revision、节点数、关系数、文件大小元数据。
- 新增回收站、恢复、永久删除、过期清理与清空回收站能力。
- 新增存储容量估算和内容完整性检查接口。
- 新建文件初始化失败时改为永久清理，避免回收站残留。

## v8.1.1

- 修复旧版文件库迁移失败时可能残留损坏 v2 索引的问题。
- ZIP/JSON 导入增加文件大小、条目数量、单条目解压大小和总解压大小限制。
- ZIP 解析增加中央目录、局部条目和压缩数据边界检查。
- 学习包导入增加 manifest 格式与入口文件校验。
- 永久删除和清空回收站时记录孤立内容清理失败，便于后续诊断。
