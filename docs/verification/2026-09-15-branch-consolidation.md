# 2026-09-15 分支整合审计与 UAT 验收

## 目标与边界

用户授权保全所有工作、审计差异、将有效改动整合并部署到 UAT。UAT 业务验收必须由用户明确确认；本次不修改 main 指针、不部署正式环境、不删除任何功能分支或原工作树。

## 初始状态与恢复

- main：219478a；uat：2ca6574；当前主工作区 feat/question-comments：600537e。
- 14 个本地分支、5 个远端分支、10 个工作树（整合前）。main 独有 23、uat 独有 85 个提交；部分功能已分别移植，提交数量不能等同功能差异。
- 主工作区：165 个已跟踪文件修改，51 个未跟踪状态项（目录展开后归档 477 个文件）。
- Codex 71eb detached 工作树：91 个已跟踪文件修改、41 个未跟踪状态项；归档 177 个文件。
- main 工作树：33 个文件修改；旧 release 工作树：2 个文件修改。
- 本地完整恢复资料：主仓库 `backups/branch-consolidation-20260915/`，包含全引用 Git bundle、四份工作树文件归档、binary patch、原始 status 和逐归档 SHA256 manifest。目录被 Git 忽略，不向远端推送本地素材。
- Git bundle 已验证完整历史；四份归档均已重新打开读取。原工作树保持原样。

## 改动去向

| 来源/文件组 | 处理 | 依据 |
| --- | --- | --- |
| main 23 个独有提交 | 正常合入整合分支，保留父历史 | 保留 SEO/百度验证、anchorless HTML、微信关联、图谱退出、正式备份与回滚镜像能力；与 UAT 同功能部分合并去重 |
| UAT 混合题、案例材料、配对题、答题排版、简化导航和部署缓存 | 全部保留 | main 及旧工作区不具备完整最新行为 |
| 主工作区及 71eb 小程序改动 | 已纳入/已被 UAT 后续实现替代；原件留档 | 多数文件与 UAT 相同；其余旧实现缺少 matching、selectedPairs、question-assets 等后续能力 |
| learning/practice services 与会话测试 | 采用 UAT 后续实现 | 新增行对比显示恢复、pendingSelections、重试幂等及测试已在 UAT；差异为 matching 扩展等更完整实现 |
| 108/77 跨试卷错题插入、成绩回顾、手机标记 | 保留现有 UAT 实现 | 已发布修复，不重复复制旧文件 |
| 题目评论/点赞/弹幕 | 精确移植并补齐一致性校验 | 独有功能；修复跨题评论路径、并发点赞；迁移接 UAT 最新 head |
| rate_limit 及四类 API 依赖/测试 | 精确移植 | 独有防重复请求能力；保留 UAT mixed 题型和访客目录 |
| 五个适配器和两个登录对话框 | 成套移植 | kg:auth-required 统一在 document 派发和监听 |
| 主工作区 deploy/update.sh 的旧备份清理 | 不采用，原件留档 | prune_remote_backups 删除正式旧备份，违反项目明确规则；main 中已有备份可读校验与 rollback image，已保留 |
| main 和旧 release 工作树的 35 个脏文件 | 由统一源重新生成 | 变化是 release/version/hash/sync 等生成产物；不作为手改源复制 |
| refactor/重构UI-待重做 的 11 个提交 | 保留原分支和 bundle，不纳入运行版本 | 是独立待重做设计，包含删除课程/训练模块等未验收行为；直接合并会扩大范围并覆盖当前 UI |
| maintenance/marketing/output/.local-review/.mini-review、手工十账号脚本、历史修正记录 | 原文件与备份留存，不推送临时资料 | 包含文档缓存、渲染图、历史素材；非遗漏业务运行代码 |
| 71eb 微信身份测试和修复 | 提炼两条硬回归并修复 mini 标识持久化 | 补充核对纠正了初判：外部接口仍适用；回归实证 unionid 首次命中 PC 用户未保存 miniOpenid，补三字段持久化后微信 45 项专项通过 |

## 执行记录

- 从 uat 建立 `codex/branch-consolidation-20260915`，位于 `.worktrees/branch-consolidation-20260915`。
- 合并 main：223698e。源冲突按行为保留两侧有效功能；前端产物从源生成。
- 实际 UAT 容器 active release 已核实为 v9.0-p4.1.230，site 992 文件，包含 admin-console.html。宿主机遗留 release 目录不是当前容器运行内容，核对以容器配置为准。
- 第一轮前端测试受到同步产物并发改写干扰，出现一次 ENOENT；后续固定产物并顺序重跑，不降低断言。

## 待用户验收

1. 游客目录、登录/退出、登录引导。
2. 练习、随机顺序、保存退出后继续、成绩筛选与全部题目回顾。
3. 单选/多选/配对题、案例材料与图片，保持现有 UAT 体验。
4. 解析后的讨论区、发送、点赞/取消、自己删除、管理员删除、报告卡片展开。
5. 教师题库/试卷与跨试卷错题放入画布。
6. 小程序现有答题、恢复、复仇流程（本次不重新发布微信小程序包）。

验收通过前，所有原工作树和功能分支继续保留；main 不前移。

## 补充验证与限制

- 评论新增跨题操作 404、同账号并发点赞幂等、删题级联清理三类回归；正常专项 6 passed，浏览器模块契约通过。
- 限流/公开题目目录专项 11 passed；保留匿名学习目录，不把旧分支的登录限制回退带入。限流作用于取题/进场等接口，不计逐题答案保存。
- 微信标识持久化专项含现有 mini/PC 关联测试，共 45 passed，无 xfail。测试覆盖顺序跨端登录；未证明旧有 PC/mini 同时修改 wechat JSON 的并发覆盖问题已经解决。
- 原 UAT site 992 文件全部保留，候选 998 文件；新增恰为百度校验/robots/sitemap 和评论 JS/CSS/浏览器测试，无文件删除。
- UAT 远端部署前备份已校验：`/home/ubuntu/lszl-uat-backups/branch-consolidation-20260915_110712`（代码 tar、UAT 数据库 dump、manifest、SHA256SUMS），运行镜像保留标签 `lszl-kg-backend:uat-rollback-20260915_110712`。未删除既有备份。

## 浏览器与构建验证记录

- 最终候选版本：v9.0-p4.1.232。v231 未部署；SEO 旧测试断言修正后递增版本，未覆盖不可变 site。
- `agent-browser` 四页（index/practice-mode/knowledge-recall/question-workspace）逐页检查 authModal、document 事件引导、账号菜单登录和退出，通过。图谱未编辑退出、保存后重开、保存失败阻止退出并保留草稿也通过。
- 核心浏览器预检：完整练习保存/恢复矩阵、答题卡和结果报告通过；桌面/手机自由图谱与练习共四个视觉场景，原生与集成差异均为 0.000%。该预检不是全量发布验收的替代。
- 混合题专项：教师配对题保存重开、案例题成组生成和共享材料编辑、七题混合试卷、部分配对保存恢复、键盘操作、图片放大、桌面手机案例材料、7/7 报告、复仇保留历史配对均通过。
- 答题页设计专项：题型、折叠/滚动、标记筛选/保存/刷新、导航边界、简化侧栏、列表字体和六种宽度均通过。
- 全新工作树首发的回归测试支持 `KG_E2E_ACTIVE_RELEASE_ROOT`，校验脚本显式传入自己的冻结候选目录；候选构建清单检查继续执行。相对真实 UAT 是否遗漏文件由单独的远端容器 992 → 998 清单验证负责。
- 在更改校验脚本后主动取消了过时上下文中的测试任务，并在固定提交 b9a31d7 + 仅文档记录的状态下重新启动全量校验；没有修改校验报告以复用过期证明。

- 固定版本最终后端全套：720 passed，1 项已有 python_multipart 弃用提示，耗时 459.41 秒。

## 最终全量发布校验

- `manage-new-legacy.js update new-legacy` 全量校验于 2026-09-15 11:39（Asia/Shanghai）通过；`passed=true`、`profile=full`、未跳过检查，耗时 688 秒。
- 后端 720 passed；前端 Node 主契约 276 passed；前端 Python 契约 9 项通过；UAT 部署保护脚本、扩展共享模块和 content-prep 契约通过。
- 核心及跨业务浏览器（题库录入/加载/并发、会员结算、流程指示、多题归纳）通过；最后四组视觉对比均 0.000%。
- 小程序既有 205 项测试通过；本次小程序端代码保持 UAT 现有版本，仅补服务端身份持久化。
- sourceHash：`8761be693d35fa825808a8a90436c4dac76aa8ed8559b1e4d99aa4181abf1944`。
- adapterHash：`f20f3a524cb3a4236a8b0efa62b60215ee497e2ca4a7f40a52907cc27a2cfe40`。
