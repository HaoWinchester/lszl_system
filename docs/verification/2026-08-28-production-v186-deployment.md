# 正式环境 v186 部署与备份验证

用户明确要求部署到正式环境，并在部署前备份。目标为 `https://lszl.aihuanpu.com`，目录 `/home/ubuntu/lszl-kg`，Compose 项目 `lszl-kg`；UAT 未更新或重启。

## 版本与切换

- 应用提交：`210747b148fa1c7f1e13049032f1a998bcf9d5c4`，部署前本地 main 与远端引用一致，工作区干净。
- 正式 active：`v9.0-p4.1.174 → v9.0-p4.1.186`。候选来自先前已由正式发布工具 promote 并在 UAT 核验的不可变 release，没有重新修改业务代码或手工覆盖 site。
- 正式旧版 960 个文件全部保留，新增 13 个，候选共 973 个可部署文件。两个 Finder `.DS_Store` 按既有 rsync/Docker 规则排除；管理页、做题页、图谱工作区等关键页面存在。
- 按既有流程分步执行 runtime 打包、rsync dry-run/同步、生产 `build backend`、停写备份、`up -d --no-deps backend`。未运行全局 Docker 清理、历史试卷回填或 Nginx 配置修改。
- 2026-08-28 17:34:52 开始切换，17:35:11 后端健康恢复（北京时间），间隔 19 秒；不是全链路用户中断时长的精确测量。
- 数据库迁移：`d4f8a1b2c3e4 → f7a2c4e6b810 → a8c1d4e7f920`，当前 head 已核对。

## 备份

仅存于生产服务器，不下载数据库或私密配置到本地：

`/home/ubuntu/lszl-backups/20260828_172834/`

目录权限 `700`，归档、清单与核验报告权限 `600`。位于部署目录之外，不受 rsync 删除影响。

| 文件 | 大小（字节） | 用途与验证 |
| --- | ---: | --- |
| `repo.tar.gz` | 511239115 | 旧版代码、release、`.env.prod` 和 Compose 配置；完整读取归档目录并核对关键文件 |
| `db.dump` | 40103241 | 在线一致性 PostgreSQL custom 备份；已实际恢复并演练迁移 |
| `external-config.tar.gz` | 3150 | 正式 Nginx 配置与支付配置/密钥目录；归档可读，不输出私密内容 |
| `backend-image.tar.gz` | 103944500 | 旧版镜像导出；gzip 完整性和 SHA-256 通过 |
| `db-cutover.dump` | 40158664 | 停旧后端后、迁移前的最终数据库快照；优先用于部署回退，完整解码及 SHA-256 通过 |

五个归档均经 `SHA256SUMS` 复核。`manifest.txt` 记录前后版本、镜像、迁移与时间；`RESTORE-NOTES.txt` 说明恢复边界，`public-verification.json` 记录公网检查结果。

备份恢复演练使用服务器内一次性 PostgreSQL 15 容器和独立数据卷，数据库网络为 `none`。迁移容器仅共享验证数据库的隔离网络命名空间，没有生产连接配置、支付凭据或对外端口。完整恢复 `db.dump` 后执行两次迁移，原有 **65 张表记录数全部保持一致**，仅新增空 `practice_sessions` 表。演练容器与卷均已清理。首次演练发现临时数据库尚未完成初始化，改为实际 TCP SQL 就绪检查后重新执行成功；生产数据库不受影响。

`db-cutover.dump` 在旧后端停写后补做，捕获初次备份之后新增的数据；对其执行了完整 `pg_restore --file=/dev/null` 解码与目录检查，没有将其再次恢复到数据库。不要将它的归档检查表述为第二次实际恢复演练。

### 回退注意事项

旧镜像保留为 `lszl-kg-backend:rollback-20260828_172834`，并另有归档。旧 Alembic 不认识新迁移，不能只切旧镜像而继续连接已升级的库。若需要回退，应先停写并保存上线后的数据，明确回退时间窗口，再将备份恢复到独立库验证；不要直接覆盖现有生产库或在运行目录解压旧代码。本次没有执行回退或破坏性恢复。

## 上线核验

- 生产镜像：`sha256:7c592b59a8ecdb2cf4c9081b6b624a5632f7c35976cb70a791b62e18792b612e`。与已验证 UAT 镜像的全部文件层相同，运行配置相同，差别仅为 `com.docker.compose.project` 标签，所以镜像 ID 不同。
- 生产容器内 active 指针、全部 **973 个 site 文件**和 **160 个后端/迁移 Python 文件**逐文件 SHA-256 与本机候选完全一致。
- 公网 8 个关键资源（`100/111/112/113/115/116` 六个做题模块、`practice-learning-adapter.js`、`styles/practice-mode.css`）逐文件 SHA-256 一致。
- 公网首页、做题页、图谱工作区、知识回忆页均返回 200 且 `data-release` 为 `186`。Prep Studio 嵌套 HTML 不含该属性，通过服务端 bootstrap 的 `releaseVersion` 验证；其与候选的差异仅为这段预期注入。
- 做题 HTML 的 GET 响应为 `Cache-Control: no-store`。当前该路由不支持 HEAD，不能把 HEAD 的 405 当作网站不可用。
- 未登录时，练习活动会话、经验汇总、试卷目录 API 返回预期 401；管理台、题库页返回预期 403，与既有服务端页面权限一致。管理页的文件完整性已在容器逐文件校验中覆盖，不宣称未登录访问管理页返回 200。
- 本机 18086 与公网 HTTPS 健康检查均为应用/数据库正常。上线后检查的日志窗口无 ERROR、Traceback 或 HTTP 500；教学 seed 为 created=0、updated=0、unchanged=18。
- 上线后只读核对：用户 556、试卷发布 21、发布题目 1992，均与发布前一致；新建会话表为 0。该核对不替代全业务数据一致性审计。
- 生产后端容器更新为 `a913569910d8`；生产数据库容器仍为 `282eae570aeb`，数据卷仍为 `lszl-kg_kg_pg_data`。`.env.prod`、Compose 配置的前后校验和一致。
- UAT 后端 `d3cdb78b60f0`、数据库 `5cb760f8e3a8` 均未重建。没有清理回退镜像或既有备份。
- 本次额外重跑做题核心/保存/草稿/适配器单测：21 passed。此前完整功能测试见 [做题模式验收](2026-08-28-practice-modes-explanations-experience.md)。本次生产验证为部署、迁移、HTTP、文件和权限检查，没有登录真实账户新增试答或支付操作，不宣称已在生产重做完整 180 题。
