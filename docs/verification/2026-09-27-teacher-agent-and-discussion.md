# 教师整理助手与答题后讨论：开发验证记录

状态：开发自检完成，UAT 部署与服务器内实际调用仍待验证；不代表用户 UAT 验收。正式环境未改动。

## 已验证
- 后端全量测试 868 passed（3 条依赖弃用警告）；其后恢复选择、混合题试卷升级、图片边界修改有专项回归与独立审查 16 passed，导入服务 51 passed，现有试卷 API 4 passed。
- 前端 pnpm test 全部通过（280 Node 项及 Python、部署脚本契约），最终回忆材料改动另跑设计与浏览器回归。
- 真实浏览器与服务器 Claude Code 套餐调用：原 JSON 12+13 道题、11 项原则，保存独立习题课副本；教师/学员免费，仅回忆和归纳模式。
- 真实 Word、PPTX、扫描 PDF 共 4 题：更正 Word 答案 A→B、移除多选题、保存草稿、恢复多选题并发布，保持同一题库/试卷，旧发布快照不变。浏览器发现的模型宣称恢复但漏选、标准试卷不能恢复多选题、回忆缺图片均已修复并回归。
- 实际学生端回忆/归纳授权图片 GET、加载、放大正常；两个画布讨论与弹幕 DOM 为 0。
- 讨论浏览器 35 项、移动端 4 项：历史弹幕、点赞、收藏、回复、刷新和账号隔离；答题前无讨论，报告仅当前题挂载，切题清理。
- 单个重任务串行，重复请求幂等，上传/提取/模型均有限额，取消保留当前步骤回执，后续步骤停止；来源文件 30 天和详细回执 180 天清理覆盖。

## 限制与待验证
- OCR/模型输出必须逐题核对；恢复请求有最多一次结构化修复，不能代替教师语义判断。
- 扫描页完整保留原件图片；回忆/归纳材料中可能含原件答案，不自动裁剪。
- 图片导出为本系统授权资源引用，不是离线内嵌图片包。
- 模型固定 glm-5.3-flash[1m]，复用现有 Coding Plan，不回退按量接口。服务器容器内实际鉴权/多轮对话、旧 DOC/PPT 转换及资源峰值待部署后记录。
- UAT 备份：/home/ubuntu/lszl-uat-backups/20260926_174340，代码与数据库归档均非空并完成归档目录验证；旧镜像保留 rollback tag。

本地证据位于 artifacts/teacher-assistant/，不包含模型凭据。最终 UAT 状态在部署完成后更新。
