# UAT 重复验证：同步差异报告误失效修复

## 现场证据

答题页 v9.0-p4.1.228 的完整验证于 2026-09-10 16:44:26 UTC 开始、16:55:58 UTC 通过，耗时约 11 分 32 秒。随后提交代码并运行 `deploy/update-uat.sh`，部署前同步把 `frontend/new-legacy-sync-report.json` 从 v227→v228 的差异改写成 v228→v228、空差异，管理器因此再次完整验证。

第二轮完整验证实际耗时 676 秒（11 分 16 秒）；紧随其后的服务器传输只用了 5 秒。

只读计算确认当时工作树的验证上下文 hash 是 `634649d67df8f13e5da997b9c665704179a045443b84beac6d915ca1bfda4078`；仅在内存中将该报告替换为提交中的原始字节，hash 就恢复成已通过报告的 `1660b5c6cba2e19002774d2db1d8b04014bedf0a6c9a1c737ee49bc90531b8a2`。没有改动另一个任务的文件或中断它的部署。

此前 21 秒的结果只验证了同一部署脚本连续运行两次；未覆盖“开发先验证、提交、部署前再次同步”的常用路径，不能代表该路径已提速。

## 修复与开发自检

公共输入策略精确排除这一个生成的同步差异报告。实际 source、site、manifest、后端、依赖、测试、验证脚本、运行环境与未知文件仍参与校验；UAT 快速路径白名单没有扩大。

- 新回归模拟验证成功、Git 提交、差异报告变空、再次发布。修复前失败：validator 调用 2 次；修复后通过：仅调用 1 次，成功报告字节不变。
- manifest 与未知名字的报告变更仍触发重验。
- 缓存专项 22 项通过。
- `pnpm -C frontend test`：276 项 Node 测试、9 项 Python 契约、4 组 UAT shell 夹具全部通过。
- 独立只读审查通过。

## 真实发布路径验证

修复提交 `976f764`。真实管理器完整验证成功，用时 677 秒：707 项后端测试通过（1 个已有 warning）、276 项 Node 测试、9 项 Python 契约、4 组部署脚本测试，以及内置浏览器/视觉校验通过。

验证完成后提交修复，再执行真实同步脚本，报告确实从 v227→v228 的非空差异变为 v228→v228 的空差异。再次执行管理器 `update new-legacy` 仅耗时 **0.450 秒**；`validation.json` 与 `validation-run.log` 的 SHA-256 和纳秒修改时间均未改变，证明确实复用了刚通过的验证。

成功报告 SHA-256：`dd32b899dfc8ef15ff31782f57278817e37b631bc8b26525b759ef85f56641be`。原始证据保存在功能工作树 `.superpowers/sync-report-reuse-proof.json`、`.superpowers/sync-report-full-validation.log` 和 v228 的 `validation-run.log`。

本次候选与另一个任务刚发布的 v228 均为 992 个 site 文件，逐文件内容相同，sourceHash 均为 `d4b321aa77dfd03146582fcd5c3cae17464b3b310ac88825dc08b8359f80a92a`。

UAT 部署结果在完成后追加。未合入 main，未部署正式环境；用户 UAT 业务验收仍待确认。
