# 正式站缺图题目替换记录

## 结果

《有分歧不站队试卷01》已从发布版本 1 切换到版本 2，题量保持 60。旧版本转为 `superseded` 并完整保留；新版本不再包含缺失表格图片的题目，改用相邻《先内后外试卷01》的完整题目 `q_1c3489935ed9449c9cf4bbb0555f6420`。

- 旧版本：`paper-e8ab20b0-77b2-40ea-b0c7-e8a57efedcf3-v1-msvlb1b0`
- 新版本：`pr_hotfix_pmp000111_20260829`
- 屏蔽题目：`q_cdebc94b84c149c889b1d5a0312ba7cc`
- 替补题目：`q_1c3489935ed9449c9cf4bbb0555f6420`
- 替补位置：数据库 `order_index=50`，即学员侧第 51 题

旧冻结快照中的屏蔽题教师题号为 `PMP-000111`；当前题库实体经过后续重新编号后为 `PMP-000414`，但稳定题目 ID、标题和题干一致。替补冻结快照中的教师题号为 `PMP-000245`；当前题库实体重新编号为 `PMP-000202`，稳定题目 ID、标题和题干同样一致。

## 安全措施

- 写入前确认目标卷和来源卷均为唯一 `published v1`，各 60 题。
- 确认目标旧版本没有 `active` 或 `paused` 会话；仅有一条 `completed` 和一条 `abandoned` 记录。
- 备份仅保存在正式服务器：`/home/ubuntu/lszl-backups/20260829_105840_question-replacement/`。
- `db-before.dump` 为 PostgreSQL custom 格式，42,231,712 字节，权限 `600`；已用 `pg_restore --list` 完整解码。
- 备份 SHA-256：`4d895e2c21046e4776e462ee9767e6e0114fc4bfe4f21f8b7176d702c3db5432`。
- 正式写入前用完全相同的数据库事务执行一次 `ROLLBACK` 演练，并再次确认线上状态未变。
- 正式事务持有教学内容全局 advisory lock；版本、题量、顺序、题目身份、快照完整性、草稿引用、当前会话和唯一 active 版本均在锁内重新校验。任一条件变化会整笔回滚。

## 写入内容

- 复制版本 1 的 59 道其它冻结快照，并在第 51 题写入来源卷已经发布的完整替补快照。
- 新版本保持原名称、说明、会员权限、开放模式、角色和发布元数据，`question_count=60`。
- 版本 1 状态改为 `superseded`，版本 2 成为唯一 `published`。
- `exam_papers` 发布投影和 `paper_questions` 第 51 题同步到替补题。
- 屏蔽题题库生命周期改为 `deleted`，保留题目实体、旧冻结快照和恢复信息，不做物理删除。
- 教学内容修订从前值递增到 338，并记录题目删除与试卷发布两项变化。

## 验证

- 版本 1：60 行、60 个不同题目，仍包含原题，历史快照未改写。
- 版本 2：60 行、60 个不同题目，原题数量 0，替补题数量 1。
- 第 51 题为 `PMP-000245 / 问题日志或解决问题｜Q022`。
- `source_payload.questions` 和 `questionSnapshots` 均为 60 项，原题数量 0、替补题数量 1。
- 运行中后端服务读取学员目录得到 `published v2 / 60题`。
- 使用正式冻结行执行 60 题顺序选题：返回 60 个不同题目，原题 0、替补题 1、替补位置 51。该验证为只读服务层选题，没有创建真实学员会话。
- 公网 `GET /api/v1/health` 返回应用和数据库 `ok`。
- 操作后 10 分钟日志窗口无 `ERROR`、`Traceback` 或 HTTP 500。

本次只进行数据库内容维护，没有部署或重启后端，也没有修改前端 active release。
