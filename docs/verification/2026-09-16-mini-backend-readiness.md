# 小程序后端完整性与发布门禁（2026-09-16）

## 问题与改动

正式容器仍运行旧后端。只读检查确认缺少 9 个 mini 登录/成长/错题资料相关接口、4 张表，且未加载小程序独立凭证。UAT 已具备对应业务代码；问题不能靠再写一套成长接口解决。

- 正式部署复用 UAT 的独立 mini Compose overlay，保留 PC 登录、付款、数据库和端口设置；强制禁用 demo 登录。
- 新增 `python -m app.cli.check_mini_readiness`：只读核对 29 个方法/路径、13 张表、Alembic head、小程序凭证和 demo=false。两条部署入口在健康检查后执行，失败时停止。
- 正式部署使用官方 release manager 验证/构建；保留课程 seed 导出和发布前代码/数据库备份。
- rsync 排除本地 artifacts、嵌套工作树和临时 mini 测试目录，防止测试凭证随代码同步。
- 新增真实 UAT 成长审计，必须使用全新隔离账号，避免用历史会话推断当天去重集合。

## 已完成验证

- 后端定向测试：48 通过（readiness、growth、mini auth、恢复、客户端视图）；1 项既有 python_multipart 弃用警告。
- 小程序：237 通过；PC 法律文档同步检查通过。
- 部署配置测试：6 通过；覆盖独立凭证预检、PC/付款/数据库配置保留、排除敏感产物、课程 seed 导出。
- 现有 UAT shell 测试：脏工作树阻断、快速路径、版本冲突重建、验收失败禁止 promote 均通过。
- 真实 UAT 综合审计：34 项通过，315 次 HTTP 请求。覆盖免费/会员/过期权限、报告与 PC 一致性、错题巩固/恢复、180 题大卷保存恢复、异常恢复、当前设备退出登录。
- 真实 UAT 成长：三个新账号分别作答 10 题，计数均由 0 到 10；重复完成和新会话重复同题均不重复计数；目标分别 5/20/30、次日生效，互不串号；首页和成长页数据一致。
- UAT 运行时检查：29 个接口、13 张表、数据库迁移和配置全部通过。正式容器只读检查按预期失败，未修改正式数据或配置。

综合/成长审计调用真实页面处理函数和真实 HTTP，微信宿主由 harness 模拟；它们不等于微信真实授权或用户手机 UAT 验收。前序原生开发者工具三账号双语试卷证据见 `2026-09-16-mini-bilingual-three-accounts.md`。

## 发布边界

开发版 1.0.3 已在此前上传；本轮后端门禁不会自动将其设为体验版或发布。正式环境尚未更新。用户明确 UAT 验收通过后才能合入 main，随后先备份正式代码与数据库，再加载小程序独立凭证、部署后端、迁移并运行同一门禁。生产发布前仍必须核对实际 active release 与候选内容，禁止以版本号代替内容核验。

本地明细在被 Git 和部署排除的 `artifacts/mini-readiness/`；账号凭证文件权限为 600，不进入报告。

## 后续核验

- 首轮全量后端：737 通过；前端契约 275/276 通过，唯一失败为 rsync 新增排除项未加入精确预期清单。发布脚本按预期在同步前中止；已同步契约，重新检查。
- 原生微信开发者工具使用真实 `wx.login` code 连接 UAT，返回 `200 authenticated`。使用正常登录返回的 session 后，首页、成长、目录、我的和会员页均读取真实 UAT 服务成功；未修改该真实账号的练习记录或目标。
- 正式 active 为 225 / 988 文件；候选 233 / 999 文件。逐路径比较无缺失，新增 11 个业务/测试文件，admin-console 存在。正式部署时必须再次核验，当前检查不替代届时状态。
- 正式独立 mini 凭证文件及 overlay 均尚不存在。预检通过 stdin 读取本地 overlay，数据库备份只依赖已有生产 base Compose，避免首次部署依赖尚未同步的文件。凭证需在正式发布备份后安全配置，内容不得进入日志或 Git。
- 本次 UAT 代码/数据库备份已验证：`/home/ubuntu/lszl-uat-backups/mini-readiness-20260916_102723`。首次误选已停止的旧 UAT DB 容器导致备份中止，未继续部署；核实架构后从共享 PostgreSQL 容器只导出独立 `kg_graph_uat` 数据库，重新完成有效备份。

## 最终 UAT 部署结果

- 部署代码：`8950a8ef9e2733acc77882ec8184925d2b9f0225`；active release `v9.0-p4.1.233`，999 文件。
- 第二轮官方完整发布检查成功：后端 737 通过，前端契约 276 通过；登录、作答、保存恢复、报告、会员、资料及跨域浏览器检查通过。桌面/移动端四组视觉差异均 0%。
- 修正后的部署/打包定向测试合计 11 通过，小程序 237 通过。
- `deploy/update-uat.sh` 全流程退出 0（780 秒）；公网版本匹配，试卷回填检查成功。
- 新 UAT 容器 readiness：`ok=true`，29 路由、13 表、迁移和配置全部通过。
- 部署后真实 HTTP 再查三个测试账号：身份正确；configuredGoal=5/20/30，today.answered 均10，completed均true；匿名成长请求为401。记录未因更新而回退或串号。
- 开发者工具保持连接真实 UAT，部署后首页和成长页刷新成功，停在成长页供用户验收。该真实微信账号为0题，和隔离测试账号的10题记录不同属正常隔离。
- `main` 保持 `ebb18274c5cfec0ea775bd1f32d787d2a8441fc9`；仅 UAT 已更新，正式发布与手机业务验收尚未完成。后续纯验证文档提交不改变已部署的业务代码。
