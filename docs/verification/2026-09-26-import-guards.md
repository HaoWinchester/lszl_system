# 导入重复操作保护

用户要求补齐导入按钮限制。沿用现有导入、冲突确认和数据格式，不改变重复题业务政策。

## 实现范围

- 公共 `new-legacy/src/00-import-guard.js`：按操作复用进行中的 Promise，文件读取前锁定，直到提交及必要刷新结束；按钮/输入禁用并标记 aria-busy，成功或失败都释放。无时间防抖窗口，也不缓存文件正文。
- 原则与归纳卡、题库（首页、教师旧入口、批量对话框）、试卷、图谱/学习包、闪卡、用户、活动包、知识树、联想库使用公共保护。文件管理原有 busy 保留，公共锁延长至导入后列表刷新。
- 题库/试卷进行中不能通过取消、重新打开、换文件、修改策略清除锁。试卷预检合并；成功后重复确认复用结果。
- 试卷不确定结果重试复用完整 payload 和幂等键；明确的版本/预检/权限策略拒绝后可重新预检。重试按钮区分“重试导入”和“重试预检”。
- Prep Studio 11 类文件输入共用同一锁，防止不同文件同时覆盖工作区；服务器同步单独复用 Promise。离线构建直接内嵌同一公共模块，无复制变体。
- 后端扩展现有 `idempotency_service`，独立事务 PostgreSQL try-advisory-lock，在多个进程/标签页之间拒绝同一账号并发导入，返回 `409 IMPORT_IN_PROGRESS`，不排队执行重复大请求。锁贯穿业务 commit/rollback，在失败或连接关闭时释放；不同账号不互相阻塞。
- 覆盖题库批量、题库内题目批量、试卷预检/导入、原则预览/应用/组合导入、活动导入、备课批次/草稿同步、共享目录与联想库写入、用户导入、旧图谱迁移、图谱文件导入创建。
- 后端新增的是并发准入控制；跨刷新/进程重启的结果重放继续依赖各业务已有幂等或版本机制，不宣称所有接口都有永久幂等回执。

## 验证

- 首轮回归测试修复前四项均失败：原则 10 次选择产生 10 请求；取消后重复题库提交；试卷重复预检；试卷重试换键。修复后 7 项定向测试通过，包含真实 retry() 路径、明确版本冲突恢复、控件恢复和 Prep 多文件输入互斥。
- 实际 Chromium + 原则控制器 + 原生 FileReader：连续 10 次 change 只调用 1 次 API，按钮/输入禁用，无效 JSON 不提交，网络失败解锁，同一文件可重试成功。API 边界使用受控响应，未对正式数据反复写入。
- PostgreSQL 实际双连接验证业务 commit 后锁仍有效、同账号并发 409、不同账号通过、异常释放；通过实际 API 路由验证批量入口均挂接保护。
- 后端全套 741 项通过（353.64 秒）；定向导入/原则/幂等测试 16 项通过；前端 `pnpm test` 280 项、Python 契约 9 项、部署脚本 4 项通过；备课构建、服务/UI契约、图谱导入兼容测试通过。
- 额外手动执行的两份旧静态测试 `question-import-persistence.test.js` / `content-prep-question-bank-integration.test.js` 暴露既有测试与当前适配器不一致（缺失 KGDomainApi fixture、断言已退役的直接 URL 文本）；相关代码和断言在 main 上相同。本次使用现行发布验证链及行为回归，不将这些旧测试宣称为通过。
- 独立审查发现并修复：试卷重试策略漂移、明确 409 后旧 revision 无法恢复、Prep 同步控件恢复覆盖新状态、重试标签不准确。

本次按 AGENTS.md 先合入/部署 UAT，开发自检不替代用户验收；未经本次验收不合入 main、不发布正式。

本地证据：功能工作树 `artifacts/import-guards/`。正式环境不在本轮变更范围。
